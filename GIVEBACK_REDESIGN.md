# GIVEBACK_REDESIGN — 승자 반납(give-back) 축 재설계 (V7, 측정 완료·§11 오너 무장)

> **결론 요약.** DOGE 사례를 바이트 단위로 재구성해 원인을 확정했다: 트레일 폭이
> **ATR 단위**로, 포지션의 리스크 단위(**R**)와 분리되어 있다 — 같은 `trail_atr=10`이
> 어떤 트레이드에선 0.84R, 다른 트레이드에선 9.35R(중앙값 3.91R)이고, 러너의 ATR이
> 팽창하면 **이길수록 반납 허용치가 커진다**. 설계구간 311건 중 **90.4%는 트레일이
> 단 1원의 이익도 잠그지 못한다**(peak_r < 트레일 폭).
>
> 이 진단에서 사전 등록한 **Y-A(R 단위 트레일 폭 상한)** 를 측정했고 — **기각**.
> 1차 셀(a-priori `trail_r=4.0`)에서 터미널 부 26.27→11.24(−57%), **양 반기 모두 악화**,
> MDD·avg_r 악화. 이어서 EXIT_REDESIGN에 사전 등록만 되고 미측정으로 남아 있던 마지막
> 후보 **X-B(whale 시간컷)** 도 측정 — **기각**(반쪽 부호 갈림). 이로써 사전등록 후보 전부 소진.
>
> 그리고 이 축이 **왜** 안 되는지를 파라미터가 아니라 **분리 불가능성**으로 확정했다
> (§4): 러너가 최종 고점까지 가는 길에 **견뎌낸** 중간 되돌림(중앙 2.32R, p90 4.05R,
> 최대 6.77R)이 중간승자의 **최종 반납**(중앙 1.90R, p90 3.09R, 최대 4.53R)을 완전히
> 덮는다. 어떤 임계 K도 둘을 가르지 못한다 — 최선의 셀(K=4)조차 **1.3R을 구하려고
> 56.2R을 건다(1:43)**, K≥5는 아무것도 못 구한다. DOGE의 반납은 2.85R이므로 **이 거래
> 하나를 잡으려면 러너의 절반 이상을 죽여야 한다.**
>
> **채택된 것은 로직 변경이 아니라 계측뿐**(§6): `peak_r`를 트레이드 기록·전방 원장·
> 알림에 남긴다(어떤 분기도 읽지 않음). 매매 규칙 바이트 불변, pytest 69/69.
>
> ---
>
> **§10 (2차 캠페인, 소유자 지시 "청산 집중·올인 유지" 이후).** §4의 경계는 *peak 기준
> · 전량 청산 · 가격 거리* 규칙에만 적용되므로, 그 조건을 깨는 축 3개를 찾아 전부
> 심판했다. **Z-B(4H 구조 청산)·Z-C(부분 익절)는 설계 단계에서 자기 진단으로 사망** —
> 특히 Z-C는 **모든 K에서 `E[r | peak ≥ K] > K`** 이라는 산수로 죽는다(꼬리가 무거운
> 분포에서 부분 익절은 구조적으로 진다).
>
> 살아남은 **Z-A(높은 곳에서 무장하는 비율 giveback 바닥)** 가 이 캠페인의 발견이다.
> 사전 진단 Q1: **무장 수준을 올리면 러너가 필요로 하는 숨쉬기 비율이 무너진다** —
> 2R 무장 시 러너는 러닝 피크의 **112%**까지 되돌리고도 살아 돌아오지만, **8R을 증명한
> 뒤엔 어떤 러너도 40%보다 깊게 되돌린 적이 없다.** 즉 **E16의 기각은 giveback이라는
> 객체가 아니라 무장 수준에 대한 기각이었고**, 같은 코드로 무장만 8R→1R로 내리면
> 26.5x가 **1.22x**로 붕괴하는 대조군이 그것을 확증한다.
>
> **측정 판정: G5 미완주.** 10개 변형 중 **처음으로 상자 안 어떤 셀도 baseline 아래로
> 가지 않는 축**이고(8R 미만 306건을 아예 안 건드림), MDD는 격자 전체에서 단조
> 개선한다. 하지만 1차 셀(arm8/gb0.5)은 baseline 근방 노이즈, 최고 셀(arm8/gb0.4,
> ret 26.27→**33.76**, LOO 9/10)은 **argmax·발화 4건·임계가 표본 최대치와 동일**
> = SEL-A 실패 형태. 소진된 홀드아웃에서 **두 셀 모두 baseline과 바이트 동일**
> (57건 중 8R 도달 1건) — 표본 밖 증거가 유리·불리 양쪽 다 **0**이다.
>
> **§11 — 오너 오버라이드로 채택 (2026-07-26).** 살릴 수익이 있는 곳은 8R 이상
> 구간뿐이고 거기서는 분리가 가능하다는 결론과, 소유자의 "청산 개선에 집중, 올인
> 유지" 재확인을 근거로 `hold_giveback_arm=8.0, hold_giveback=0.4`(최고 셀)를
> whale 프로파일에 **실제로 무장했다** — G5를 통과해서가 아니라 X-R과 같은 계열의
> 오너 결정이다. 트레일이 이미 트레이드의 92%를 손대지 않으므로 손실 상한이
> 바운디드고(8R+ 러너의 40% 초과 반납 구간에만 작용), 소진된 홀드아웃이 유리·불리
> 어느 쪽 증거도 못 낸 것이 무장의 안전판이지 근거는 아니다. §10d의 되돌림 규칙이
> 이제부터 이 결정을 전방 데이터로 심판한다.

---

## 1. 사례 재구성 — DOGE PBK_S (2026-07-22 13:00 → 07-25 17:00 UTC)

패치(`b8fafec`, trail_atr 8→10 + hold_minr_strict, V6 ON) 이후 **첫 트레이드**.
엔진 리플레이가 실거래 기록과 일치(r −0.019191, pnl −3.444340)하므로 아래는 재구성이
아니라 **동일 계산**이다.

| 항목 | 값 |
|---|---|
| 슬롯 / 방향 / conf / regime | PBK_S(experimental, risk_scale 0.20) / SHORT / 0.7641 / TREND_DOWN |
| 진입 / 초기 SL | 0.072361 / 0.07366 → stop_frac **1.79% = 2.30 ATR** |
| 레버리지 / 명목 / 증거금 / 1R | 5x / 10,000 USDT / 2,000 USDT(계좌의 20%) / **179.48 USDT** |
| 최고점 (07-24 13:00 종가 0.06868) | **+2.834R** = +509 USDT = **증거금 대비 +25.4%**, **계좌 대비 +4.8%** |
| 청산 (07-25 17:00, Trail @ 0.072339) | **−0.019R** = −3.44 USDT = 계좌 −0.03% |
| 반납 | **2.853R** (77봉 보유) |

사용자가 본 "최대 +20%"는 위 표의 **증거금 대비 25.4%** 다(1H 마크가 07-23 15:00~07-25
11:00 내내 +18~25% 구간에 머물렀다). 이 수치의 정체는 §6에서 다룬다.

### 1a. 왜 아무 청산도 발동하지 않았나 (전수)

| 경로 | 상태 | DOGE에서 |
|---|---|---|
| Trail (10 ATR 샹들리에) | 유일하게 무장 | **아래 §2 — 구조적으로 이익을 잠글 수 없었다** |
| SignalFade · giveback 레그 | **off** (`hold_giveback=1.0`, E16) | 미발화 |
| SignalFade · conviction collapse | 임계 0.50 × 2봉 | hold_conf가 0.747 → **0.988** 로 *상승*, 청산봉에서도 **0.804**. 근처도 못 감 |
| LossFade | `unreal_r < 0` 필요 | 마지막 봉에서야 음수 — 그때는 트레일이 이미 발화 |
| BiasFlip | 4H 바이어스 역전 ×2봉 | bias4h가 77봉 내내 −1(정렬 유지) |
| Timeout | 트렌드 슬롯 `time_stop_bars=0` | 미무장 |

**77봉 동안 무장된 이익-은행 메커니즘이 하나도 없었다.** EXIT_REDESIGN.md §1이 AVAX
(+0.97R)에서 진단한 "정체 축 부재"와 같은 구멍이며, 이번엔 peak가 2.83R로 3배 컸다.

## 2. 기계적 원인 — 청산 단위(ATR)와 리스크 단위(R)의 분리

트레일 폭은 **현재 봉의 ATR**로, 리스크 1R은 **진입 시점의 스탑 거리**로 표시된다.
둘 사이엔 아무 고정 관계가 없다.

DOGE: `sl0 = 진입 + 2.30 ATR`. 트레일이 sl0보다 나아지려면 best_close가 진입 대비
`10 − 2.30 = 7.70 ATR` = **3.35R** 만큼 유리하게 가야 하고, 손익분기(sl = 진입)에
닿으려면 **4.35R** 이 필요하다. peak는 **2.83R** — 트레일이 이익을 잠그기 시작하는
선의 **65% 지점에서 멈췄다**. 실제로 스탑은 07-25 01:00에야 처음 움직였고, 그것도
가격이 전진해서가 아니라 **ATR이 수축(0.00057→0.00048)해서** 였다.

### 2a. 설계구간 기하 감사 (whale, 2023-06-01~2026-01-31, n=311)

| 지표 | min | p10 | p25 | 중앙 | p75 | p90 | max |
|---|---|---|---|---|---|---|---|
| 스탑 폭 (ATR 배수) | 1.34 | 1.95 | 2.04 | **2.43** | 3.13 | 4.17 | 8.71 |
| **트레일 폭을 R로 환산** | **0.84** | 2.08 | 2.85 | **3.91** | 4.91 | 5.63 | **9.35** |
| peak_r (종가 기준 MFE) | −1.77 | — | 0.06 | **0.47** | 1.59 | 4.72 | 37.88 |

세 가지가 동시에 드러난다.

1. **동일 파라미터가 트레이드마다 11배 다른 규칙이 된다** — `trail_atr=10`이 0.84R
   ~ 9.35R. 스탑이 좁게 잡힌(=`sl_floor_atr` 바닥에 걸린) 트레이드일수록 트레일이
   R 기준으로 넓어지고, 그런 트레이드가 정확히 **레버리지가 높은**(liq 캡이 널널한)
   트레이드다. 즉 **%스윙이 가장 큰 포지션이 보호가 가장 없다.** DOGE가 그 사례다.
2. **peak_r ≥ 트레일 폭인 트레이드는 30/311 = 9.6%뿐** — 나머지 **90.4%에서 샹들리에는
   이익을 단 한 번도 잠그지 못하며, 사실상 정적 스탑이다.**
3. **이길수록 반납 허용치가 커진다** — 폭이 `10 × ATR_현재`이고 러너의 ATR은 추세가
   진행될수록 팽창하므로, R로 환산한 허용 반납이 트레이드 도중 **증가**한다.
   peak≥6R 구간의 실제 반납 중앙값이 4.26R, 최대 11.89R인 이유다.

## 3. 사전 등록 후보 Y-A — R 단위 트레일 폭 상한 · **기각**

**설계.** 트레일 폭을 `min(trail_atr × ATR, trail_r × 진입스탑거리)`로 **상한**만
건다. 상한이므로 (a) 기존 샹들리에를 조이기만 하고 넓히지 않으며, (b) 트레일이 없는
슬롯(`trail_atr=0`)에 새 트레일을 만들지 않는다. `trail_r=0`이면 완전 off.

**과적합 통제 — 파라미터를 결과보다 먼저 고정했다.** 1차 셀 **`trail_r = 4.0`** 은
§2a 기하 감사의 *현행* 트레일 폭 중앙값(3.91R)에서 온 값이다. 즉 **평균적으로 아무것도
조이지 않는 셀**이고, 가설은 "조이기"가 아니라 **"트레이드 간 이질성 제거"** 다.
스윕 {2,3,4,5,6}은 평탄성 확인용이며 **argmax를 고르지 않는다**(K<1은 E5 스탑-스윕
실패 모드라 애초 제외). 이 문단은 측정 실행 전에 확정됐다.

**측정** (whale, 설계구간, 반쪽 교차 MID=2024-09-30 12:00). `ret`은 총수익률(최종자산 =
(1+ret)배).

| trail_r | ret | MDD | avg_r | pf | n | H1 avg_r / ret | H2 avg_r / ret |
|---|---|---|---|---|---|---|---|
| **OFF (현행)** | **26.27** | **−57.3%** | **+0.193** | 1.96 | 310 | +0.011 / 3.70 | +0.234 / 4.28 |
| 2.0 | 0.44 | −67.0% | +0.004 | 1.10 | 411 | −0.148 / −0.48 | +0.133 / 1.11 |
| 3.0 | 2.39 | −62.0% | +0.033 | 1.34 | 369 | −0.111 / −0.06 | +0.137 / 2.17 |
| **4.0 (1차 셀)** | **11.24** | −58.5% | +0.171 | 1.60 | 342 | +0.074 / 1.65 | +0.245 / 3.50 |
| 5.0 | 23.76 | −50.5% | +0.212 | 1.80 | 321 | −0.021 / 3.56 | +0.302 / 4.35 |
| 6.0 | 24.47 | −51.0% | +0.191 | 1.85 | 317 | −0.011 / 4.14 | +0.264 / 4.03 |

G6 비용 2배(1차 셀): 14.64 → **6.17**, pf 1.74 → 1.43. 역시 악화.

**판정: 기각.** 1차 셀이 터미널 부를 −57% 파괴하고, **양 반기 모두 악화**하며(H1
3.70→1.65, H2 4.28→3.50), MDD·avg_r·pf도 전부 악화. 격자는 단조(조일수록 나쁨)이고
K≥5는 baseline으로 수렴할 뿐 — **도움 되는 셀이 없다.** n이 310→342/411로 증가한 것이
직접 증거다: 조임은 **추세 중간 눌림에서 대량 발화**한다(X-A·E9·E16과 동일 서명).

**기록해 둘 함정 두 개.**
- **신기루 재현.** `trail_r=5.0`은 avg_r가 **올라가고**(+0.212 vs +0.193) 터미널 부는
  **내려간다**(23.76 vs 26.27). E9가 경고한 "avg_r 상승 = 신기루"가 또 나왔다.
- **경로 무시 반사실의 함정.** 경로를 무시한 단순 반사실(`peak_r − K`로 대체)은 4R
  상한이 총 R을 **67 → 113으로 거의 두 배**로 만든다고 말한다. 실제 엔진에서는 절반
  이하로 붕괴한다. **이 축이 매번 매력적으로 보이는 이유가 정확히 이 반사실이다** —
  복리·경로를 넣기 전엔 믿지 말 것.

## 4. 왜 이 축 전체가 죽었는가 — 분리 불가능성 (파라미터가 아닌 구조)

파라미터를 하나 더 시도하는 대신, **peak를 기준점으로 삼는 모든 청산**이 풀어야 하는
분류 문제를 직접 측정했다. 각 트레이드에서 최종 고점에 닿기 **전에 견뎌낸** 러닝-피크
대비 최대 되돌림(= 러너가 숨쉬는 데 필요한 여유)과, 중간승자의 **최종 반납**(= 우리가
막고 싶은 것)을 R 단위로 비교한다.

| 집단 | n | 실현 R | 되돌림/반납 중앙 | p75 | p90 | max |
|---|---|---|---|---|---|---|
| 러너 (peak ≥ 6R) — **견뎌낸 중간 되돌림** | 25 | **+185.3** | 2.32R | 3.29R | 4.05R | **6.77R** |
| 중간승자 (1R ≤ peak < 4R) — **최종 반납** | 72 | +21.3 | 1.90R | 2.63R | 3.09R | 4.53R |

**두 분포가 거의 완전히 겹친다.** 임계 K를 스캔하면:

| K (R) | 조기 사망하는 러너 | 위태로워지는 러너 실현 R | 구제되는 중간승자 | 구제 R(반사실) |
|---|---|---|---|---|
| 1.5 | 18/25 | 145.8 | 44/72 | +47.2 |
| 2 | 14/25 | 127.8 | 34/72 | +27.8 |
| 3 | 8/25 | 95.5 | 10/72 | +6.4 |
| **4** | 3/25 | **56.2** | 3/72 | **+1.3** |
| 5 | 2/25 | 39.3 | **0/72** | 0 |
| 6 | 1/25 | 26.0 | 0/72 | 0 |
| 8 | 0/25 | 0 | 0/72 | 0 |

- **이 계열의 최선(K=4)이 1.3R을 구하려고 56.2R을 건다 — 1:43.**
- **K≥5는 중간승자를 하나도 구하지 못한다** — 안전해지는 순간 무용해진다.
- **DOGE 자신**: 반납이 2.85R이므로 잡으려면 K ≤ 2.8이어야 하고, 그 값은 러너 중간
  되돌림 분포의 **60번째 백분위 아래**다. **이 한 건을 잡는 대가는 러너 절반이다.**

집중도가 이 계산을 되돌릴 수 없게 만든다: **25건(8%)이 185R을 실현하고 전체 합은
67R** — 나머지 286건은 합쳐서 −118R이다. tail을 조금이라도 깎는 규칙은 구하는 것보다
많이 잃는다. EXIT_REDESIGN.md의 "봉 단위에서 죽은 횡보는 러너의 깊은 휴식과 구분
불가"라는 정성 결론이, 이제 **R 단위 수치 경계**로 확정됐다.

## 5. X-B (whale 시간컷) — 사전등록된 마지막 미측정 후보 · **기각**

EXIT_REDESIGN.md §2에 등록만 되고 측정되지 않은 채 남아 있던 후보. 기존 배관 그대로
whale에 `time_stop_min_r=0.3`으로 무장(코드 변경 0).

| time_stop_bars | ret | MDD | avg_r | n | H1 avg_r / ret | H2 avg_r / ret |
|---|---|---|---|---|---|---|
| OFF | **26.27** | −57.3% | +0.193 | 310 | +0.011 / **3.70** | +0.234 / 4.28 |
| 72 | 24.41 | −55.6% | +0.175 | 346 | +0.030 / 3.22 | +0.196 / 4.48 |
| 96 | 25.01 | −54.2% | +0.198 | 325 | +0.047 / 2.65 | +0.216 / 5.49 |
| 120 | 23.44 | −55.5% | +0.205 | 313 | −0.007 / 2.22 | +0.282 / 5.91 |

**판정: 기각.** 세 셀 모두 터미널 부 하락, 그리고 **H1 수익률은 전 셀 악화·H2는 전 셀
개선** — 반쪽 부호 갈림 = 경로 노이즈(E19 선례로 즉시 기각). MDD는 개선되지만 G5는
전 지표 비악화를 요구한다. 여기서도 avg_r만 상승(120셀 +0.205)하는 신기루가 반복된다.

**EXIT_REDESIGN.md에 사전 등록된 후보는 이로써 전부 소진됐다**(X-A·순수giveback·X-C·
X-D·X-E·X-G·X-B) + 본 문서의 Y-A. **9개 변형, 전부 기각.**

## 6. 채택 — 로직이 아니라 계측·보고 (매매 규칙 바이트 불변)

측정이 "바꾸지 말라"고 했으므로 청산·진입 규칙은 **한 줄도 바꾸지 않았다.** 대신
이번 진단에서 드러난 두 개의 계측 공백만 메웠다.

**(a) `peak_r`를 기록한다.** 엔진은 이미 `pos.peak_r`를 추적하고 있었지만 **어디에도
기록하지 않았다** — 그래서 전방 원장으로는 반납을 아예 측정할 수 없었고, 이번 진단도
리플레이 재구성으로만 가능했다. 이제 트레이드 기록·전방 원장(`peak_r` 열)·Telegram·
Notion에 남는다. **어떤 분기도 이 값을 읽지 않는다**(V5.1 `hold_entry`와 동일한
attribution-only 패턴). 기존 원장 1행(DOGE)은 동일 리플레이 값 2.8343으로 소급 기입
했으며, 이 한 행만 **재구성값**임을 여기 명시한다.

**(b) 알림에 peak를 표시한다.** 반납이 알림만 보고도 보이도록 청산 라인에 peak를
붙였다(계좌 대비 % 표기는 소유자 지시로 제외 — 증거금 기준 `levered %` 유지).

```
❌ V1 EXIT DOGE SHORT 5x — Trail
-0.02R (peak +2.83R) | price +0.03% | levered +0.15%
```

엣지에 대한 주장이 아니라 계측이다. pytest 69/69.

## 7. (V7 1차 결론 — §10에서 갱신됨) 레버리지 축

9개 변형이 확인한 하나의 사실: **반납을 줄이는 것과 tail을 갖는 것은 같은 축의 양
끝이다.** 현행 whale은 8%의 트레이드가 전체 R의 276%를 만드는 tail 기계이고, 그 tail을
받으려면 러너가 4~7R 숨쉬는 것을 견뎌야 한다 — 5x 풀마진에서 그건 **증거금 대비 ±20~35%
스윙**이다. 이번 DOGE는 그 스윙의 정상 사례이지 고장이 아니다.

축을 옮기고 싶다면 청산 파라미터가 아니라 **레버리지 구조**가 손잡이이고, 그 두 점은
이미 측정돼 있다(TAIL_REDESIGN.md §1):

| | ret | MDD | exp | H1/H2 exp | MC P(MDD>50%) |
|---|---|---|---|---|---|
| **현행** (conf 티어 2/3/5/10x) | **26.3** | −57% | +0.193 | +0.011/+0.234 | 0.48 |
| **LEV-A flat 2x** (티어 폐지) | 5.16 | **−40%** | +0.180 | **+0.044/+0.207** | **0.035** |

LEV-A는 "생존 최적이나 소유자 방향과 불일치"로 **보류**된 것 — 측정에서 떨어진 게
아니다. **효과:** 스윙 ~2.5배 축소, 파산 확률 21배 축소. **대가:** 터미널 부 5배 축소.

> **소유자 결정 (2026-07-26): 기각.** "코인 종류 상관없이 기회를 포착하면 올인하는
> 고래 전략" 유지 — 디레버리징 계열은 논외. 따라서 남은 작업은 전부 **청산 축**이고,
> §10이 그 2차 캠페인이다.

## 8. 정직한 유보

- **n=1로 로직을 바꾸지 않았다.** DOGE는 1건이다. 그래서 가설을 설계구간 311건으로
  심판했고 결과는 "바꾸지 말라"였다. 1건이 한 일은 **가설을 제공한 것**뿐이다.
- **표본 외 검증 수단이 없다.** G3 홀드아웃(2026-02-01~07-10)은 이미 1회 개봉·소진됐다
  (TAIL_REDESIGN §3-홀드아웃). 따라서 설령 Y-A가 G5를 통과했더라도 **표본 외 확인
  없이 라이브 청산 파라미터를 바꾸는 것 자체가 과적합 리스크**였다. 설계구간에서
  기각됐으므로 그 문제는 발생하지 않았다. 앞으로의 유일한 표본 외 심판은 전방 페이퍼(G8).
- **진입 측.** 사용자가 진입도 물었으므로 정직하게: **이번 DOGE 진입은 실패가 아니다**
  (+2.83R / 증거금 +25%까지 갔다). 진입 선택 품질 레버는 이미 측정되어 없다 —
  conf⊥R(E9), 동일-스케일 tiebreak 무선택력(E19c), SEL-A 표본 외 반전(E20). 진입에서
  남은 미측정 축은 **유니버스/커버리지**뿐이고(EXIT_REDESIGN §최종종합 (b)) 별도 주제다.
- **이 문서의 반증 조건.** 전방 페이퍼가 n≥30을 쌓았을 때 `peak_r − r`(이제 원장에
  기록된다)의 분포가 설계구간과 **다르게** 나온다면 — 특히 러너 중간 되돌림이 실제로
  더 얕다면 — §4의 분리 불가능성은 재측정 대상이다. 그 전까지 청산 축은 닫혔다.

## 9. 재현

```bash
# §2a 기하 감사 · §4 분리 불가능성: 설계구간 whale 리플레이 후 트레이드별
#   stop_dist/ATR, peak_r, 러닝-피크 대비 중간 되돌림을 집계
# §5 X-B: 코드 변경 없이 replace(profile("whale"), time_stop_bars=N, time_stop_min_r=0.3)
python3 -m pytest tests/ -q
```

§3 Y-A 재현용 패치(기각됐으므로 프로덕션에 없음 — house rule: 기각 변형은 코드에서
나간다). `engine.py`의 트레일 블록에 아래를 넣고 `Params`에 `trail_r: float = 0.0`을
추가하면 위 표가 재현된다:

```python
if trail_w > 0 and not np.isnan(a):
    width = trail_w * a
    if p.trail_r > 0 and pos.init_stop_frac > 0:                 # Y-A
        width = min(width, p.trail_r * pos.init_stop_frac * pos.entry)
    trail = pos.best_close - d * width
```

---

# 10. 2차 캠페인 — §4 경계를 **우회하는** 청산 축 (소유자 지시: 청산 집중, 올인 유지)

§4의 분리 불가능성은 **peak를 기준점으로 삼는·전량 청산하는·가격 거리 규칙**에만
적용된다. 그 세 조건 중 하나라도 깨는 축은 경계 밖이다. 셋을 찾아 전부 심판했다.

## 10a. 파라미터를 고정한 사전 진단 (결과를 보기 전)

| 질문 | 측정 | 답 |
|---|---|---|
| **Q1** 러너가 **견뎌낸** 최악 되돌림을, 러닝-피크의 **비율**로 보면? | 무장 수준별 | 아래 |
| **Q2** 4H 스윙 앵커 트레일의 폭은? | R 환산 | 중앙 **0.62R** (샹들리에 3.91R) |
| **Q3** 러너가 4H EMA를 역방향 종가 돌파하는가? | 피크 이전 발생 | EMA20 **13/25**, EMA50 3/25 |

**Q1이 이 캠페인의 핵심이다.** 무장 수준을 올릴수록 러너가 필요로 하는 "숨 쉴 비율"이
급격히 줄어든다:

| 무장 (러닝 피크 ≥) | 견뎌낸 되돌림 / 러닝피크 — 중앙 | p90 | **max** |
|---|---|---|---|
| 2R | 0.39 | 0.85 | **1.12** |
| 4R | 0.30 | 0.59 | 0.81 |
| 6R | 0.22 | 0.37 | 0.55 |
| **8R** | 0.21 | 0.35 | **0.40** |

**E16이 왜 실패했는지가 여기 있다.** 1~2R에서 무장하면 트레이드가 러닝 피크의 **112%**
까지 되돌리고도 살아 돌아온다 — 그 구간에서 안전한 비율은 **존재하지 않는다**. 그러나
8R을 증명한 뒤에는 설계구간 어떤 러너도 **40%**보다 깊게 되돌린 적이 없다. **같은
객체가 무장 수준에 따라 완전히 다른 물건이 된다.**

## 10b. 설계 단계 기각 2건 (측정 불요 — 자기 진단으로 사망)

- **Z-B 구조 청산** (4H 종가가 EMA를 역방향 돌파 시 청산, 수익 중일 때만). Q3대로
  EMA20은 러너 **13/25를 피크 전에 절단**하고 경로 무시 반사실조차 **−46.7R**. EMA50은
  반사실 +5.1R이지만 **이득 전부가 peak<2R 버킷**(+19R, 청산 R 중앙 +0.06 — 소액 승리로
  긁는 것)이고 2R↑ 버킷은 전부 −14R. "수익을 살린다"의 정반대 프로파일.
- **Z-C 부분 익절** (+K R에서 절반 청산, 나머지 러닝). 이건 **산수로 죽는다**:
  Δ = 0.5·Σ(K − r). 설계구간에서 **모든 K에 대해 E[r | peak ≥ K] > K**
  (K=2→3.73, 4→6.00, 6→7.41, 8→9.30, 10→11.12) — 즉 **K에 도달했다는 사실 자체가
  K 이상으로 끝날 것을 예측한다**. 따라서 어떤 수준의 절반 익절도 음수(−52.8R ~ −6.2R),
  경로 효과를 넣기 전에 이미. **꼬리가 무거운 분포에서 부분 익절은 구조적으로 진다.**
- (참고) **슬롯별 비대칭** — "PBK_S만 조이자"도 데이터가 막는다: 러너 25건 중
  PBK_S가 **9건(36%)** 이다. 문제아 슬롯이 아니라 러너 생산 슬롯이다.

## 10c. Z-A — 높은 곳에서 무장하는 비율 giveback 바닥 · **측정 완료**

**설계.** peak_r ≥ `hold_giveback_arm` 를 증명한 뒤에만, 러닝 피크의
`hold_giveback` 비율을 반납하면 청산. §4 경계를 **비율**로 우회한다(고정 R이 아니라
트레이드 규모에 비례) + **높은 무장**으로 Q1 봉투 밖에 선다. 배관은 기존 giveback 레그
재사용 + 무장 수준만 `hold_conf_min_r`에서 분리(DEC-A 선례). `0`이면 상속 = 완전 off.

**1차 셀은 arm=8R / gb=0.50, 결과를 보기 전에 Q1에서 고정했다** — 8R 무장 시 관측된
최대 숨쉬기가 0.40이므로 0.50은 **25% 안전 여유**. 격자는 평탄성 확인용이며
**argmax 채택은 금지**라고 스크립트에 명시했다.

| 셀 | ret | MDD | avg_r | pf | n | H1 ret | H2 ret |
|---|---|---|---|---|---|---|---|
| **OFF (현행)** | 26.27 | −57.3% | 0.1926 | 1.958 | 310 | 3.70 | 4.28 |
| arm6/gb0.4 | 27.60 | −53.0% | 0.1916 | 1.748 | 324 | 4.07 | 4.13 |
| arm6/gb0.5 | 28.48 | −55.5% | 0.1976 | 1.919 | 317 | 3.89 | 4.48 |
| arm6/gb0.6 | 26.66 | −57.3% | 0.1974 | 1.943 | 311 | 3.70 | 4.36 |
| **arm8/gb0.4** | **33.76** | **−53.0%** | **0.2029** | **1.986** | 312 | **4.17** | **5.12** |
| **arm8/gb0.5 (1차 셀)** | 26.53 | −55.5% | 0.1882 | 1.942 | 310 | 3.89 | 4.12 |
| arm8/gb0.6 | 26.27 | −57.3% | 0.1926 | 1.958 | 310 | 3.70 | 4.28 |
| arm10/gb0.4 | 29.70 | −53.0% | 0.2007 | 1.980 | 310 | 4.17 | 4.40 |
| arm10/gb0.5 | 26.77 | −55.5% | 0.1888 | 1.946 | 310 | 3.89 | 4.16 |
| arm10/gb0.6 | 26.27 | −57.3% | 0.1926 | 1.958 | 310 | 3.70 | 4.28 |
| **E16 대조군** arm1/gb0.5 | **1.22** | −69.6% | 0.0425 | 1.22 | 443 | −0.49 | 4.31 |
| **E16 대조군** arm2/gb0.5 | **2.32** | −64.3% | 0.0407 | 1.37 | 387 | −0.01 | 2.05 |

**대조군이 Q1을 그대로 확증한다** — 동일 코드, 무장만 8R→1R로 내리면 26.5x가 **1.22x**로
붕괴한다. E16의 기각은 giveback이라는 객체가 아니라 **무장 수준**에 대한 기각이었다.

**격자 성질 두 가지 (셀 하나가 아니라 상자 전체의 성질이라 선택 편향이 없다):**
1. **상자 안 어떤 셀도 baseline 아래로 가지 않는다** (최저 = 26.27 = 무발화). 10개 변형
   중 **처음으로 "해롭지 않은" 축.** 8R 미만인 트레이드 306건을 아예 건드리지 않으니
   당연한 결과이고, n이 거의 안 변하는 것(310→310~324)이 그 증거다.
2. **MDD는 giveback에 대해 단조 개선** — gb0.4는 세 무장 전부 −53.0%, gb0.5는 −55.5%,
   gb0.6/off는 −57.3%. 메커니즘과 일치하는 **평탄한** 개선.

### 측정 판정: **G5 미완주**

- **1차 셀(arm8/gb0.5)은 G5 미통과.** ret +1.0%·MDD 개선이지만 **avg_r 0.1926→0.1882,
  pf 1.958→1.942, H2 4.28→4.12 악화** = 혼합. MC도 사실상 불변(P(>50%) 0.470→0.475).
  발화 **3건**. 요컨대 **baseline 근방의 노이즈**이며, 이것만으로는 라이브를 바꿀 근거가
  못 된다.
- **최고 셀(arm8/gb0.4)은 전 지표·양 반기·LOO 9/10 개선**이지만 순수 측정으로는
  채택 기준 미달이다: (a) **argmax**이고, (b) 발화가 **4건**이며, (c) 임계 0.40이
  **표본 최대 숨쉬기와 정확히 같은 값** — 즉 분포의 최댓값에 경계를 세운 것이라, 표본
  밖에서 45% 숨쉬는 러너 하나면 절단된다. 이건 E20에서 SEL-A가 실패한 형태 그대로다
  (표본 내 양 반기 개선 → 표본 밖 반전).
- **표본 외 증거는 0이다.** 소진된 홀드아웃(2026-02-01~07-10)에서 두 셀 모두
  **baseline과 바이트 동일** — 57건 중 8R에 닿은 트레이드가 **1건**(max peak_r 8.16)뿐이라
  Z-A가 한 번도 발화하지 않는다. 유리한 증거도 불리한 증거도 없다는 뜻이다.

**§11에서 최고 셀(arm8/gb0.4)이 오너 오버라이드로 whale에 실제 무장됐다** — 위 세
유보가 사라진 게 아니라, 소유자가 그 리스크를 알고 승인했다는 뜻이다. 상세·되돌림
규칙은 §11. base/whale 모두 무장 전후 HEAD 대비 바이트 대조 확인, pytest 69/69.

## 10d. 종합 — 청산 축에서 실제로 알게 된 것

12개 변형을 심판한 뒤 그림이 단순해졌다.

- **8R 미만 구간(전체의 92%, DOGE 포함)에는 살릴 수익이 없다.** §4가 수치로 닫았다 —
  이 구간의 반납을 잡는 임계는 러너 숨쉬기와 분리 불가능하고, 최선의 시도가 1.3R을
  구하려 56.2R을 건다. **DOGE 같은 2.83R 왕복은 이 전략의 고장이 아니라 가격표다.**
- **살릴 수익이 있는 곳은 오직 8R 이상 구간이고, 거기서는 분리가 가능하다** (Q1: 러너
  숨쉬기 max 40% vs 실제 반납 최대 11.89R). 이게 이번 캠페인의 진짜 발견이다.
- **그런데 그 구간은 설계구간 2년 8개월에 16건, 홀드아웃 5개월에 1건뿐이다.** 표본이
  없어서 못 채택하는 것이지, 메커니즘이 틀려서가 아니다.

**측정 시점의 다음 단계는 새 변형이 아니라 표본이라고 판단했다.** `peak_r`가 이제
전방 원장에 기록되므로(§6a), 전방 트레이드가 쌓이면 다음 두 숫자가 자동으로 판정을
내린다:

1. **러너 숨쉬기 봉투** — 전방에서 peak≥8R에 도달한 트레이드의 최대 생존 되돌림 비율이
   0.40을 넘는가? 넘지 않으면 gb=0.4가 표본 밖에서 확인된 것이고, 넘으면 완화가 옳다.
2. **8R 도달 빈도** — 이 축의 기대 발화율. 현재 추정 5% (설계 16/310, 홀드아웃 1/57).

**되돌림 규칙(사전 확정) — §11에서 무장된 뒤 이 규칙이 그 결정을 심판한다.** 전방에서
peak≥8R 트레이드가 **10건** 쌓였을 때, 그 안에서 관측된 최악 생존 되돌림 비율이
0.40 이하이면 현행(`gb=0.4`) 유지, 0.40~0.55이면 `gb=0.6`으로 완화, 0.55를 넘으면
`hold_giveback_arm=0`으로 **영구히 끈다**. **이 규칙은 지금 확정해 두는 것이며,
전방 데이터를 본 뒤 고치지 않는다.**

## 11. 오너 오버라이드로 채택 (2026-07-26)

**결정.** 소유자가 §7의 디레버리징 선택지(LEV-A)를 명시적으로 기각하고("코인
종류 상관없이 기회를 포착하면 올인하는 고래 전략 유지"), 청산 로직 개선에
집중할 것과 "최대한 수익을 살릴 수 있는" 개선을 요청했다. §10c가 제시한 유일한
후보(Z-A)에서 **최고 셀 `hold_giveback_arm=8.0, hold_giveback=0.4`를 whale
프로파일에 실제로 무장**했다 — G5를 완주해서가 아니라, EXIT_REDESIGN.md §60
(X-R)과 같은 계열의 오너 결정이다.

**이 결정이 G5 미완주라는 사실을 바꾸지 않는다.** §10c의 세 유보(1차 셀 노이즈·
최고 셀 argmax/표본최대 임계·홀드아웃 무증거)는 여전히 유효하다. 오버라이드가
정당화되는 근거는 다음 세 가지뿐이다:

1. **손실이 구조적으로 바운디드된다.** 이 레그는 peak_r<8R인 트레이드(설계 구간의
   92%, DOGE 포함)를 **전혀 건드리지 않는다** — 최악의 경우도 "8R+ 러너에게 40%
   초과 반납분을 남겨두지 않는다"이지, 정상 동작하던 트레이드를 새로 망가뜨리는
   경로가 없다.
2. **설계 구간 신호는 방향이 일관됐다.** argmax라는 사실과 별개로, 격자 전체
   (§10c 표)가 baseline 아래로 가지 않았고 MDD는 giveback 값에 대해 단조
   개선했다 — "우연히 좋아 보이는 셀 하나"가 아니라 "상자 전체가 해롭지 않고
   그중 하나가 특히 좋다"는 모양이다.
3. **표본 밖 반증이 없다.** 소진된 홀드아웃이 이 레그를 검증하지 못했다는 것은
   "안전이 확인됐다"가 아니라 "아직 아무것도 모른다"는 뜻이며, 그 불확실성을
   안고 가는 것은 정량 게이트가 아니라 **소유자가 감수하기로 한 리스크**다.

**이 결정이 반증되지 않는 한 유지되는 이유.** 위 세 근거 중 셋 모두 데이터가
바뀌면 무너진다 — 그래서 §10d의 되돌림 규칙을 **결정과 동시에** 못 박았다.
전방에서 peak≥8R 트레이드 10건이 쌓이는 시점이 이 오버라이드의 첫 심판이다.

**변경 파일:** `fabletradebot/config.py`(whale 프로파일: `hold_giveback=1.0→0.4`,
`hold_giveback_arm=0.0→8.0`, 근거 주석 동반), `tests/test_whale.py`(고정 배선
검증 갱신), `README.md`(§7·§9·패치이력에 V7 반영). 검증: whale 설계 구간
ret 26.2698x→**33.7579x** / avg_r 0.1926→**0.2029** / n 310→312 (§10c 표 재현),
base 프로파일 무변경 확인(ret 0.6735 / n 1072, 변경 전후 직접 대조), pytest 69/69.

## 10e. 재현

```bash
python3 scripts/exp_giveback_arm.py     # 10c 격자 + E16 대조군 + G6
python3 -m pytest tests/ -q
```
