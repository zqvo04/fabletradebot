# CONF_REDESIGN — conf / hold_conf 진단과 재설계 (V5 제안, 측정 전 사전 등록)

작성 기준: 설계 구간 2023-06-01~2026-01-31 재실행 (base Params, n=1381).
홀드아웃 미개봉. 아래 모든 수치는 이 세션에서 새로 측정한 값이며, 반쪽 교차
(H1 23-06~24-09 / H2 24-10~26-01)로 안정성을 함께 보고한다.

---

## 0. 전제 교정 (정직 우선)

"conf와 hold_conf가 현재 알고리즘의 엣지"라는 전제는 절반만 맞다.

- **hold_conf(청산 측)**: 측정된 기여가 있다. E16에서 conviction-collapse 다리
  단독의 가치가 확인됨 (W-B +5,434% vs W-C 트레일 단독 +3,462%). LossFade의
  모멘텀 포화 보조 조건도 E15에서 whale +256→+318% 개선. **개선 투자 가치 있음.**
- **entry conf(진입 측)**: 현재 **엣지가 아니다**. corr(conf, R) = **−0.010**
  (n=1381, 이번 재측정; E9b의 0.005와 일치). base 프로파일에서 conf는 사이징에
  쓰이지 않으므로 무해하지만, whale 프로파일에서는 좌석 tiebreak과 레버리지
  티어(2x~10x)를 결정하므로 **예측력 없는 점수가 레버리지를 고르는 부채**다.

측정상 실제 엣지의 원천: (1) BRK_L 구조+게이트, (2) 청산 구조(트레일8 +
conviction-collapse + LossFade, giveback 제거), (3) 생존 사이징. conf/hold_conf
완성도를 높이는 작업은 "엣지 극대화"가 아니라 (2)를 정밀화하고 (whale에서)
conf발 레버리지 오배분이라는 마이너스를 제거하는 작업으로 정의해야 한다.

---

## 1. entry conf 진단 (측정 사실)

conf = 0.55·c_base + 0.25·c_fit + 0.20·c_align + c_fund, clip[0,1].

### 1a. 성분별 예측력 (설계 구간, 전체 n=1381 / BRK_L n=212)

| 성분 | corr(·, R) 전체 | BRK_L | BRK_L H1 | BRK_L H2 | 판정 |
|---|---|---|---|---|---|
| conf 합성 | −0.010 | +0.068 | +0.001 | +0.151 | 예측력 없음 |
| c_base | +0.031 | **+0.128** | +0.028 | **+0.251** | 유일한 양의 성분, 부호 양쪽 안정 |
| c_fit | −0.067 | −0.018 | −0.017 | −0.024 | **부호 역전** (아래 1b) |
| c_align | −0.004 | −0.112 | −0.034 | −0.220 | 포화 + 음의 상관 (아래 1c) |
| c_fund | std ≈ 0 | — | — | — | **죽은 성분** (아래 1d) |

### 1b. c_fit은 부호가 뒤집혀 있다

BRK_L 진입 국면별 평균 R (양쪽 반기 동일 방향):

| 국면 | H1 avg R | H2 avg R | c_fit 부여값 |
|---|---|---|---|
| RANGE 발 돌파 | **+1.15** (n32) | **+0.83** (n41) | 0.4 |
| TREND_UP 중 돌파 | +0.93 (n70) | +0.62 (n69) | 1.0 |

E9b("RANGE 국면 돌파 +1.28R vs TREND 중 +0.38R")의 재확인. 신선한 추세 시작이
가장 좋은데 fit은 정확히 반대로 점수화한다. 이 역전은 conf_entry=0.55 문턱과
결합해 **가장 좋은 하위그룹(RANGE 돌파)을 가장 세게 걸러낸다**: fit=1·align=1이면
base≥0.18이면 통과(사실상 무의미), fit=0.4·align=1/3이면 base≥0.70 요구.
whale에서는 같은 역전이 RANGE 돌파에 낮은 레버리지 티어를 배정한다.

주의: E10a는 "RANGE→돌파 판별자"를 사이징 예측자로 기각했다(당시 글로벌 국면,
H2 부호 반전). 현 V3 자산별 국면 파이프라인에서는 양쪽 반기 모두 RANGE 우위로
재현되지만, **역방향 보정(RANGE>TREND 가점)은 재적합 위험이 있으므로 채택하지
않는다.** 결론은 가점 역전이 아니라 **성분 제거**다(§3).

### 1c. c_align은 게이트와 이중계상되어 포화 상태다

BRK/PBK mask가 이미 bias1d==d & bias4h==d를 하드 요구 → 진입 조건부로 align의
1D/4H 항은 상수 1이고 BTC 항만 변동한다. 실측: BRK_L의 c_align std=0.083,
mean=0.978(포화), corr은 양쪽 반기 음수. 즉 align이 conf에 넣는 유일한 정보는
"BTC 동행 여부"인데 그 정보의 부호가 음수다 — E15가 진입 게이트에서 btc_dir
하드 게이트를 제거한 것과 같은 결론이 소프트 성분에서도 반복되고 있다.

### 1d. c_fund는 설계 구간에서 검증 불가능한 채로 conf를 흔들 수 있다

펀딩 히스토리 부재(~3개월)로 설계 구간 전체에서 c_fund 분산 = 0 (한 번도 발화
안 함). 그러나 라이브에서는 ±0.05/−0.10이 conf에 가산된다 → **백테스트가 검증한
conf 분포와 라이브 conf 분포가 다른, 라이브-온리 괴리 원천.** whale에서는 이
미검증 가산이 레버리지 티어 경계(0.62/0.70/0.80)를 넘나들게 할 수 있다.

### 1e. 슬롯 간 비교 불가능성

c_base의 의미가 슬롯마다 다르다(BRK: margin+squeeze+body+vol / PBK: shallowness
+body+vol / OSC·BND: depth+body+vol). 서로 다른 자로 잰 점수를 whale 좌석 정렬의
2차 키와 레버리지 티어에 그대로 쓴다. risk_scale 1차 정렬(E15)이 검증/실험 간
오염은 막았지만, 실험 슬롯끼리(전부 0.20)의 좌석 경합은 여전히 비교 불가능한
conf가 결정한다.

### 1f. conf 5분위 (BRK_L): 단조가 아니라 혹(hump)

mid conf +1.66R, top 분위 +0.49R. conf≥0.80이 10x를 받는 whale 티어 구조는
측정과 정면 배치된다(최고 레버리지가 최고 기대값 구간에 배정되지 않음).

---

## 2. hold_conf 진단

hold_conf = 0.45·align + 0.30·fit + 0.25·mom. 청산 측 가치는 측정됐으나(§0)
구조적 약점 4가지:

1. **성분의 75%가 래깅·계단식이다.** fit은 2일 히스테리시스 일봉 상태의 계단
   맵(1.0/0.5/0.35/0.0), align은 1D/4H/BTC 이진 3표 평균 — 하나가 뒤집힐 때마다
   hold_conf가 0.15씩 점프한다. E15가 이미 "블렌드가 래깅 fit/align에 떠받쳐져
   V반전을 놓친다"고 진단하고 mom==0 포화 보조 조건으로 **우회**했다. 우회가
   아니라 성분 자체를 연속화하는 것이 원인 치료다.
2. **문턱 0.50이 분포의 계단 경계 위에 있다.** config 주석의 측정("물린 추세
   포지션의 conviction이 0.45~0.55에 군집")은 계단 성분의 산술적 결과다. 계단
   하나가 문턱 통과를 좌우하므로, 국면 라벨 분포가 바뀌면(예: HIGH_VOL 비중
   증가 → fit 0.35) 같은 0.50이 전혀 다른 빈도로 발화한다. 연속화하면 문턱
   주변에 질량이 퍼져 이 민감성이 구조적으로 줄어든다.
3. **streak 디바운스가 느린 성분에는 허상이다.** hold_conf_bars=2는 1H 2봉인데
   fit/align은 4H/1D 주기로만 갱신 → 느린 성분이 무너진 순간 2시간 뒤 자동
   발화(디바운스 효과는 mom에만 실재). 의도한 "확인" 의미를 가지려면 streak이
   최소 1개의 새 4H 마감을 포함해야 한다.
4. **rsi_ok의 역방향 기울기 소실.** clip(0,1)이라 RSI4H<50 전 구간이 0 —
   LossFade가 판별해야 할 역방향 영역에서 45와 30을 구분하지 못한다. E15의
   mom 포화 조건이 필요했던 이유의 절반이 이 데드존이다.

부수: align의 BTC 1/3은 E15에서 진입 게이트로는 이중계상으로 판정된 정보다.
알트 자체 추세 중 BTC 횡보가 hold_conf를 0.15 깎아 조기 청산(FN의 청산 버전)을
유발할 수 있다 — 단, 소프트 성분이므로 제거가 아니라 측정 대상(HV-C).

---

## 3. 재설계 (사전 등록 변형 — E15/E16 프로토콜과 동일)

원칙: 적합 가중치 금지, 선험적 단조 성분만, 신규 파라미터 최소(기존 지표 재사용),
변형은 하나씩 스택, 판정은 반쪽 교차 부호 안정 + G5(±20%) + G6(비용 2배) +
G7(MC 생존) + base 회귀 불변(비무장 경로) + 전방 페이퍼 8주.

### 3A. entry conf — "게이트 중복 성분 제거, 점수는 연속 증거만"

핵심 아이디어: mask가 이미 요구하는 조건(국면, 1D/4H 정렬)은 진입 조건부로
정보가 없거나(포화) 부호가 반대다(1b, 1c). conf에서 빼고 게이트로만 남긴다.
conf는 게이트 통과 후 남는 **연속 증거의 품질**만 잰다. 성분 역할이 진입
게이트/좌석 랭킹/레버리지 티어 3가지로 갈라져 있으므로 역할별로 분리 설계한다.

- **CV-A (성분 정리)**: conf = c_base + c_fund 게이트화.
  - c_fit, c_align을 conf 산식에서 제거 (mask 게이트는 전부 유지 — 진입 집합
    자체는 최대한 불변).
  - c_fund는 conf 가산 대신 **독립 거부권**으로 이동: fund_z·d > +1.5면 진입
    차단(과밀 방향 신규 진입 금지), 보너스 가산은 폐지. 설계 구간에서 검증
    불가능한 성분이 점수를 움직이는 경로를 차단하고, 전방 채점으로만 판정.
  - conf_entry 문턱은 base-단독 분포에 재앵커: 설계 구간 트레이드 수가 현행
    ±10% 이내가 되는 값을 **하나만** 사전 계산해 고정(격자 탐색 금지). 트레이드
    집합 변화량과 기대값을 양쪽 반기로 보고.
- **CV-B (비교 가능화, CV-A 스택)**: c_base의 각 원시 성분(margin, squeeze,
  body, vol_ratio, shallowness, depth …)을 자산별 **인과적 롤링 백분위 순위**
  (기존 bbw_lookback=240 재사용, 신규 윈도우 금지)로 [0,1] 균등화한 뒤 동일가중
  평균. 목적: 자산 간·슬롯 간 conf를 같은 자(percentile)로 만들어 whale 좌석
  경합(실험 슬롯끼리)의 비교 불가능성(1e)을 해소. 가중치는 계속 동일가중 —
  성분별 가중 적합은 금지.
- **CV-C (whale 레버리지 티어 분리, 독립 변형)**: conf→티어 매핑 폐지.
  레버리지 = min(국면 캡, 청산 캡, 자산 캡)의 floor_tier — 이미 존재하는 구조
  제약만으로 결정(스탑이 넓으면 자동으로 낮은 티어). conf는 좌석 tiebreak
  (risk_scale 다음 2차 키)로만 남긴다. 근거: 1f의 혹 모양 — conf 상위분위에
  10x를 주는 현행이 측정과 배치. **conf가 사이징 권한을 되찾는 조건**은 슬롯
  승급과 동일하게 명문화: 전방 n≥30에서 conf 분위-R 단조성이 양쪽 반기 재현.

기각 옵션(기록): fit을 RANGE>TREND로 역보정하는 안은 이번 재측정이 양쪽 반기
지지함에도 **채택하지 않는다** — E10a가 같은 계열 판별자의 반기 반전을 이미
관측했고, 같은 데이터에 대한 2차 적합이기 때문. 성분 제거(CV-A)는 이 정보를
"쓰지 않는" 방향이라 과적합 위험이 비대칭적으로 작다.

### 3B. hold_conf — "성분 연속화, 구조는 불변"

가중치(0.45/0.30/0.25)와 블렌드 구조, 문턱(0.50), min_r 등 **모든 파라미터
불변**. 성분만 계단→연속으로 교체(전부 기존 지표 재사용, 신규 파라미터 0개).

- **HV-A (연속 fit + 대칭 rsi)**:
  - fit_c = clip(d·(ema20_4h − ema50_4h)/atr4h, −1, 1)·0.5 + 0.5
    — 트레이드 자신의 4H 프레임에서 잰 연속 추세 강도. 일봉 히스테리시스
    래그(최대 수일)와 0.5/0.35 계단 제거. regime.py의 추세 정의(|EMA20−EMA50|
    > 0.5 ATR)와 같은 원료라 의미 보존.
  - rsi_ok = (d·(rsi4h − 50)/20).clip(−1, 1)·0.5 + 0.5 — px_ok와 동형 대칭.
    역방향 기울기 복원(§2-4). mom==0 포화 보조 조건(E15)은 그대로 두되 발화
    빈도 변화를 보고.
- **HV-B (streak 의미 복원, HV-A 스택)**: fade/loss streak 카운트가 최소 1개의
  새 4H 마감을 포함하도록 hold_conf_bars 2→4 (선험적 근거: 성분 갱신 주기의
  1배를 덮는 최소값. {2,4} 격자 비교 금지 — 4 하나만 측정, 열위면 2 유지).
- **HV-C (align의 BTC 항 측정, 독립 변형)**: align에서 BTC 표를 제거하고 1D/4H
  2표 평균으로. 채택 조건은 whale 반쪽 교차 양쪽 개선 — 아니면 현행 유지.
  (E15의 진입 측 결론을 청산 측에 무검증 이식하지 않는다.)
- 2단계 보류 목록(이번 사이클 측정 안 함, 기록만): 진입시점 대비 상대 감쇠
  문턱, BRK 전용 레벨-홀드 항 — 둘 다 신규 파라미터를 만들므로 1단계 결과가
  전방에서 확인된 후에만.

### 3C. 판정 게이트 (변형 공통, 사전 고정)

1. 설계 구간 full + 반쪽 교차: 기대값 부호 양쪽 유지, base와 whale 각각 보고.
2. G5 ±20% 민감도 전 설정 양수 / G6 비용 2배 양수 / G7 MC base 생존 게이트.
3. base 프로파일 비무장 경로(hold_conf_exit=0) 수치 완전 불변 회귀.
4. 전방 재현 구간(2026-06-20~) 비교 — 악화여도 정직 공개(E16 방식).
5. 채택은 변형당 독립 판정, 스택은 채택분만. 홀드아웃 미개봉 유지.

### 3D. 측정 인프라 (로직과 무관하게 즉시 가치)

- scoring.py 전방 리포트에 **conf 분위-R 단조성**(슬롯별)과 **hold_conf 청산
  직후 24/48h 가상 보유 R**(SignalFade/LossFade가 실제로 아낀/버린 R) 추가 —
  conf가 사이징 권한을 "얻는" 심사와 hold 문턱의 실효를 전방 데이터로 상시 측정.
- c_fund 발화 로그: 라이브에서 fund_z 게이트가 실제로 무엇을 막는지 기록
  (설계 구간 검증 불가의 보완).

---

## 4. 요약

| 대상 | 진단 | 처방 |
|---|---|---|
| c_fit | 부호 역전 (RANGE 돌파가 더 좋은데 감점) | conf에서 제거, 게이트 유지 (CV-A) |
| c_align | 게이트 이중계상으로 포화, 잔여 정보(BTC) 음의 상관 | conf에서 제거 (CV-A) |
| c_fund | 설계 구간 분산 0 — 미검증 라이브-온리 성분 | 가산 폐지, 과밀 거부권으로 (CV-A) |
| c_base | 유일한 양의 성분, 슬롯 간 비교 불가 | 백분위 균등화 + 동일가중 (CV-B) |
| conf→레버리지 티어 (whale) | 예측력 없는 점수가 2~10x 결정, 분위 혹 모양 | 구조 제약 기반 티어로 분리 (CV-C) |
| hold fit/align | 계단·래깅이 75%, 문턱이 계단 경계 위 | 연속화 (HV-A), 구조·문턱 불변 |
| hold rsi_ok | 역방향 데드존 | 대칭 클립 (HV-A) |
| hold streak | 느린 성분에 디바운스 허상 | 4H 마감 1개 포함 (HV-B) |
| hold align BTC 항 | 진입 측 이중계상 결론의 청산 측 미검증 | 독립 측정 (HV-C) |

이 문서는 설계 사전 등록이며, 구현·측정은 변형별로 별도 커밋에서 진행한다.

---

## 5. 측정 결과 (구현 후, 설계 구간 반쪽 교차 — E17)

각 변형을 플래그 뒤에 구현해 독립 A/B 측정. 기준선은 이 세션 재실행값
(base +0.1074R/+109%/MDD −14.7%, whale +0.1344R/+5,434%/MDD −47.9% — E15/E16
문서값과 정확히 일치, 하네스 신뢰 확인).

### 청산 측 (whale에서만 무장, base는 비무장 → 자동 불변)

| 변형 | whale exp | ret | MDD | H1 / H2 | 판정 |
|---|---|---|---|---|---|
| 기준선 | +0.1344R | 54.3x | −47.9% | +0.155/+0.119 | — |
| **HV-A** (연속 fit + 대칭 rsi) | **+0.1955R** | 53.1x | **−46.5%** | +0.254/+0.150 | **채택** |
| HV-A+HV-B (streak 4) | +0.1876R | 50.8x | −48.7% | +0.235/+0.150 | 기각(전 지표 열위) |
| HV-A+HV-C (BTC 표 제거) | +0.1752R | 47.7x | −48.7% | +0.235/+0.128 | 기각(양쪽 반기 열위) |

- **HV-A 채택**: base byte-for-byte 불변 확인. whale 기대값 +45%, MDD 개선,
  양쪽 반기 상승. 총수익 소폭 하락(53.1 vs 54.3x)은 복리 노이즈 내이며 MDD·기대값
  우위가 명확 → 위험조정 순개선.
- **HV-B 기각**: 연속화(HV-A) 후 4봉 디바운스는 청산만 지연. 사전 등록의 "열위면
  2 유지" 발동. HV-B 단독은 54x→20x로 급락.
- **HV-C 기각**: 진입 게이트의 BTC 이중계상 결론(E15)은 청산 측에 전이되지 않음.
  보유 포지션에겐 systemic BTC 방향이 정당한 홀드 정보 → BTC 표 유지.

### 진입 측 (base/turbo/max는 conf를 단일티어 게이트로, whale은 conf→레버리지)

| 변형 | base exp/ret/pf | base MC p95 | whale exp/ret | 판정 |
|---|---|---|---|---|
| 기준선 | +0.1074/+109%/1.57 | −20.7% | +0.134/54.3x | — |
| **CV-A** (c_base 단독 + 펀딩 거부권) | **+0.1101/+114%/1.63** | **−18.6%** | +0.100/13.4x | **base 채택 / whale 제외** |
| CV-C (whale 구조 레버리지) | — | — | +0.112/16.8x, MDD−65% | 기각 |
| HV-A+CV-A+CV-C (풀스택) | — | — | +0.106/7.8x, MDD−69% | 기각 |

- **CV-A base 채택**: conf_entry=0.55 **불변**으로 n=1267(사전 설정 ±10% 밴드
  1243~1519 내) → 문턱 재적합 없음. 전 게이트 통과·개선: G5 17/17 양수
  (min +0.026R), G6 비용2배 pf 1.42→1.46, **G7 MC p95 −20.7%→−18.6%,
  P(>30%) 0.002→0, final_p5 1.16→1.23**. 부호 반전 c_fit·포화 c_align·미검증
  c_fund 가산을 점수에서 제거 → 로직 정합성·수치 동시 개선.
- **whale는 CV-A 제외**: whale은 conf가 레버리지 티어(0.55/0.62/0.70/0.80 →
  2/3/5/10x)를 고르므로, CV-A의 낮은 c_base-단독 conf 분포가 티어를 붕괴시킴
  (54x→13x, avg_lev 4.21→3.47, MDD −48%→−60%). conf가 R을 랭킹하진 못해도
  (E9b) 티어가 넓은 스탑/저conf 트레이드에 낮은 레버리지를 배정하는 부수효과가
  복리 베이스를 보호 → **load-bearing**. profile("whale")에서 conf_clean=False.
- **CV-C 기각**: whale 레버리지를 최대 구조 캡으로 분리하면 avg_lev 4.21→4.88,
  MDD −48%→−65%로 악화. 위 load-bearing 진단의 대우.
- **CV-B 미구현(연기)**: 유일한 근거였던 whale 좌석 비교가능성이, whale이 legacy
  conf를 유지(CV-A 제외)하면서 소멸. base는 conf를 이진 게이트로만 써 백분위
  정규화의 사전 근거가 없음 → CLAUDE.md 단순성·과적합 경계 원칙에 따라 구현하지
  않고 기록만. 1단계 결과가 전방에서 확인되면 재검토.
- turbo/max: CV-A+HV-A가 양쪽 다 개선(turbo −0.072→−0.034R, max −0.101→−0.086R,
  pf·ret 상승). 음수 기대값은 pre-existing한 꼬리 프로파일 특성(E13).

### 최종 채택 (config 기본값)

`hold_cont=True`(HV-A), `conf_clean=True`(CV-A) — base/turbo/max 적용. whale은
`conf_clean=False`로 legacy conf 유지 + HV-A 상속. 기각 4변형(HV-B/HV-C/CV-B/CV-C)은
근거와 함께 문서화. pytest 54/54(+2: CV-A 점수=c_base·펀딩 거부권, whale 배선).
홀드아웃 미개봉 유지. 전방 페이퍼 트랙이 최종 심판.

---

# 6. V7 재진단 — conf는 "예측자"가 아니라 "레버리지 조정기"다 (2026-07-26)

계기: 소유자가 "현재 conf 계산을 개선할 필요가 느껴진다"고 판단. §1의 진단(conf⊥R)은
2026-07 시점 값이므로, 현행 파이프라인(9자산 · trail10 · Z-A 무장, n=312)에서 전면
재측정하고 **§1이 묻지 않았던 축들**을 새로 열었다.

## 6a. conf는 6개 축 **전부**에서 무정보다 (측정 완결)

§1은 corr(conf, R) 하나만 봤다. 그러나 whale에서 conf의 실제 임무는 **레버리지 선택**
이므로, "R을 못 맞춘다"만으로는 부족하다 — 레버리지가 곱해주는 대상(꼬리)이나
레버리지가 감당해야 할 위험(하방)을 conf가 아는지가 진짜 질문이다. 전부 측정했다.

| 축 | 측정 | 값 | 판정 |
|---|---|---|---|
| 실현 R | corr(conf, R) | **+0.024** | 무 |
| 꼬리 (R 단위) | corr(conf, peak_r) | **+0.003** | 무 |
| 꼬리 (스탑-프리) | corr(conf, peak_%) | **+0.012** | 무 |
| 변동폭 | corr(conf, \|price %\|) | +0.046 (H1 +0.015 / H2 +0.089) | 무·불안정 |
| 손절 확률 | P(SL) 4분위 | 0.538 / 0.558 / 0.500 / 0.513 | 평평 |
| 손실 크기 | 평균 손실 R 4분위 | −0.92 / −0.92 / −0.96 / −0.87 | 평평 |

4분위 평균 R은 반기마다 **모양 자체가 달라진다**(H1 +0.04/−0.39/+0.82/+0.10,
H2 +0.45/−0.11/−0.09/+0.94) — 단조도 아니고 재현도 안 된다. **conf는 방향·크기·
꼬리·하방·분산 어디에도 정보가 없다.** 이로써 "conf를 더 잘 계산한다"는 방향의
개선은 정의상 노이즈 적합이다.

## 6b. 10x 티어가 좋아 보이는 것은 **스탑 폭 착시**다

레버리지 티어별 성과를 보면 10x가 압도적으로 보인다(avg peak_r 2.835 vs 2x의 0.969).
그러나 **peak_r은 R 단위이고 1R = 스탑 거리**이므로, 스탑이 좁은 트레이드는 같은 가격
움직임에도 peak_r이 기계적으로 커진다. 스탑-프리 단위(원시 가격 %)로 다시 재면:

| 레버리지 | n | avg peak_r (R) | **avg peak %** | **median peak %** | avg stop % |
|---|---|---|---|---|---|
| 2x | 25 | 0.969 | 4.012 | **2.777** | 5.878 |
| 3x | 117 | 1.882 | **5.866** | 1.637 | 3.730 |
| 5x | 142 | 1.482 | 4.043 | 1.068 | 2.891 |
| 10x | 29 | **2.835** | 4.695 | **0.854** | 1.672 |

**10x 티어는 스탑-프리로 재면 중앙값이 전 티어 중 꼴찌다**(0.854% vs 2x의 2.777%).
R 단위로 본 우월성은 전부 스탑 폭이 만든 것이다. conf가 레버리지를 잘 배분한다는
마지막 정황 근거가 여기서 사라진다.

## 6c. 구조적 결함 — **받침대(pedestal)** 와 위험 역상관

whale conf = 0.55·c_base + 0.25·c_fit + 0.20·c_align. 성분 실측:

| 성분 | 가중 | 평균 | 표준편차 | 점수 기여(평균) | 점수 기여(표준편차) |
|---|---|---|---|---|---|
| c_base | 0.55 | 0.671 | 0.127 | 0.369 | **0.070** |
| c_fit | 0.25 | 0.925 | 0.198 | 0.231 | 0.050 |
| c_align | 0.20 | 0.863 | 0.177 | 0.173 | 0.035 |

1. **fit+align이 거의 상수인 +0.404 받침대다.** 티어 경계(0.62/0.70/0.80)는 그 받침대
   위에 서 있고, 실제로 변별하는 항은 유효 표준편차 **0.070**짜리 c_base 하나뿐이다.
   경계 간격(0.08~0.10)이 그 표준편차와 같은 크기라 **티어 배정이 c_base의 미세한
   움직임에 과민**하다.
2. 그 받침대의 재료가 **측정으로 부실한 것들**이다 — c_fit은 현행 유니버스에서도
   여전히 부호 역전(corr R −0.043, peak_r −0.076)이고 c_align은 포화(평균 0.863).
   즉 **whale conf 점수의 45%가 노이즈이거나 반대 부호**이며, 그 45%가 전원을 상위
   티어로 밀어 올린다.
3. **conf는 위험과 역상관이다.** conf 티어 10x를 받는 그룹의 평균 스탑이 3.628%로 전
   티어 중 **가장 넓다**(3x 그룹 2.759%). 즉 티어 맵은 가장 위험한 트레이드에 가장 큰
   레버리지를 주려 하고, `lev_liq_cap`이 사후에 눌러서 겨우 구제된다. 실제로
   `corr(conf, stop_frac) = +0.174` — conf와 유동성 안전 캡이 서로 반대로 당긴다.
4. **진입 게이트는 whale에서 사실상 사문이다.** fit=align=1이면 conf≥0.55는
   c_base≥0.18을 뜻하는데(평균 0.671, 표준편차 0.127 → 약 −3.9σ) 트렌드 슬롯에서는
   결코 걸리지 않는다. 게이트로 실제 기능하는 구간은 align이 0.5로 고정된 OSC/BND뿐.

## 6d. **CV-A의 whale 기각은 이유가 틀렸다** (선행 판정 정정)

§5는 CV-A(conf=c_base 단독)를 whale에서 54x→13x 붕괴를 근거로 기각하고 "conf가
load-bearing"이라 해석했다. 그러나 **+0.404 받침대를 걷어내면서 티어 경계를 그대로
두면 책 전체가 2x/3x로 쏟아지는 것이 기계적으로 당연하다** — 이것은 정보 손실이 아니라
**캘리브레이션 사고**다.

검증: 티어 **모집단을 고정**한 채(레버리지 분포 동일, 어떤 트레이드가 어떤 티어를
받는지만 교체) 점수만 바꿔 1차 근사로 복리:

| 점수 (모집단 고정) | terminal | avg_lev | MDD(근사) |
|---|---|---|---|
| 현행 합성 conf | 35.96 | 4.48 | −40.9% |
| **c_base 단독 (CV-A)** | **26.42** | 4.36 | −37.2% |
| **c_base_pct (CV-B)** | **51.06** | 4.23 | −40.6% |
| 무작위 점수 (n=400) | p5 1.19 / p50 7.09 / p95 40.60 | — | — |

**CV-A는 26.4x이지 13x가 아니다** — 붕괴분의 대부분이 경계 미조정에서 왔음이 확인됐다.
§5의 "load-bearing" 해석은 이 문서에서 **정정**한다: conf가 실어 나르는 것은 정보가
아니라 **레버리지 수준**이다.

## 6e. 설계 단계 기각 — LEV-R (위험 정규화 레버리지)

6c-3의 정의적 수정: `lev = target / stop_frac` 로 트레이드당 계좌 위험을 균등화(포트폴리오
모드 `size_position`이 이미 쓰는 교과서 규칙 — whale만 예외다). target은 **현행 평균
계좌 위험을 재현**하도록 사전 고정(0.1226).

| 변형 | terminal | avg_lev | MDD(근사) | 계좌위험/트레이드 |
|---|---|---|---|---|
| 현행 | 35.96 | 4.48 | −40.9% | 4.21% |
| **LEV-R (a-priori)** | **1.55** | 3.48 | **−57.4%** | 3.00% |
| LEV-R target 0.15 | 2.72 | 4.02 | −68.0% | 3.58% |

**기각.** 위험 균등화는 **꼬리 전략에 정확히 반대**다 — 좁은 스탑(E5가 "노이즈에
쓸린다"고 측정한 그 구간)에 레버리지를 몰아주고, 살아남아 달리는 넓은 스탑을 눌러
버린다. 계좌 위험을 낮췄는데 MDD가 **악화**하는 것이 그 증거다.

## 6f. 사전 등록 후보 CF-A — 사이징 점수 분리 · **미채택**

**설계.** 진입 게이트/좌석 랭킹은 합성 conf 그대로 두고, **레버리지 티어만** 자기 점수
(`c_base_pct` — 슬롯 간 비교 가능한 유일한 성분이자 R·peak_r 양쪽에 유일하게 양의
상관을 가진 성분: +0.120 / +0.134)를 자기 사다리에서 읽는다. DEC-A가 진입/청산 문턱을
분리한 것과 같은 조치를 **사이징 역할**에 적용하는 것.

**1차 셀은 모집단 정합 사다리 0.63/0.81/0.95** — 현행 티어 모집단(6/16/34/44%)을
재현하는 c_base_pct 분위값. **결과가 아니라 분포에 맞춘 값**이며, 레버리지 믹스를
고정한 채 배정만 바꿔 "점수 내용"과 "레버리지 수준"을 분리하기 위한 것이다.

| 셀 | ret | MDD | avg_r | pf | n | avg_lev | H1 r/ret | H2 r/ret |
|---|---|---|---|---|---|---|---|---|
| **OFF (현행)** | 33.76 | **−53.0%** | 0.2029 | 1.986 | 312 | 4.47 | 0.027/4.17 | 0.239/5.12 |
| **matched (1차 셀)** | **40.07** | −54.5% | 0.2081 | 2.099 | 309 | 4.27 | 0.034/4.46 | 0.242/5.91 |
| shift −.02 | 42.15 | −54.9% | 0.2039 | 2.093 | 310 | 4.35 | 0.034/4.57 | 0.234/6.12 |
| shift +.02 | 43.29 | −54.5% | 0.2041 | 2.180 | 310 | 4.09 | 0.034/4.61 | 0.234/6.26 |
| quartile .25/.50/.75 | **31.60** | −57.6% | 0.1865 | 1.937 | 308 | 5.03 | 0.027/3.32 | 0.208/5.92 |

G6 비용 2배: OFF 16.44 → matched **15.45** (avg_r 0.0973→0.0825).

**판정: 미채택.** 긍정 신호는 분명하다 — **양 반기 모두 ret·avg_r 개선**, 터미널 부
+19%, pf 개선, 그것도 **평균 레버리지를 낮추면서**(4.47→4.27). 경계 ±0.02 이동에도
40~43x로 평탄하다. 그러나 채택 기준을 넘지 못한다:

1. **MDD 악화** (−53.0% → −54.5%) — G5는 전 지표 비악화를 요구한다.
2. **G6 비용 2배에서 열위** (16.44 → 15.45) — 비용 민감도가 baseline보다 나쁘다.
3. **파라미터-프리 대조군이 baseline보다 나쁘다** (quartile 31.60, avg_lev 5.03).
   즉 이득은 c_base_pct라는 *점수*에서 오는 게 아니라 **모집단 정합이라는 캘리브레이션**
   에서 온다 — 점수의 정보력 주장은 이 격자가 뒷받침하지 못한다.
4. **c_base_pct는 SEL-A와 같은 피처다.** SEL-A는 설계구간 양 반기 개선(+0.045R) 후
   홀드아웃에서 **부호 반전**(−0.024R)하고 꼬리까지 압축(f_p95 359→133)했다
   (TAIL_REDESIGN §3-홀드아웃). 이 피처에 대한 **문서화된 표본 외 반증 전력**이 있고,
   홀드아웃은 이미 소진돼 재확인할 방법이 없다.

Z-A(§GIVEBACK_REDESIGN §11)와 대비하면 근거의 질이 명확히 낮다: Z-A는 상자 전체가
비파괴적이고 MDD가 단조 개선했으며 대조 실험으로 메커니즘이 증명됐다. CF-A는 두 게이트를
실패하고, 대조군이 반대로 가며, 피처에 표본 외 반증 전력이 있다. **오버라이드를 권하지
않는다.**

배관(`lev_tiers`, 기본 `()` = off)은 코드에 남긴다 — 사이징 점수를 진입 점수에서
분리하는 것 자체는 DEC-A와 같은 계열의 옳은 관심사 분리이고, 전방 증거가 쌓이면
재심할 사전 등록 후보이기 때문. base/whale 모두 HEAD 대비 바이트 불변
(base ret 0.6735/n 1072, whale ret 33.7579/n 312), pytest 69/69.

## 6g. 결론 — 무엇을 개선할 수 있고 무엇은 없나

- **"conf 점수를 더 잘 계산한다"는 개선은 불가능하다.** 6개 축 전부 무정보이므로
  가중치 조정·성분 추가는 정의상 노이즈 적합이다. §1의 결론이 훨씬 강한 형태로 재확인됐다.
- **개선 가능한 것은 점수가 아니라 캘리브레이션이다** — 받침대(6c-1), 부호 역전 성분
  (6c-2), 위험 역상관(6c-3), 사문 게이트(6c-4)는 전부 예측력과 무관한 구조 결함이고
  실제로 고치면 설계구간 양 반기가 개선된다(6f). 다만 현재 측정으로는 MDD·비용
  민감도를 대가로 지불하므로 채택선을 넘지 못한다.
- **다음 단계는 전방 증거다.** `conf_monotonicity`(scoring.py, §3D에서 이미 구현됨)가
  전방에서 conf 분위-R 단조성을 상시 측정한다. **되돌림/승격 규칙(사전 확정):** 전방
  n≥30에서 (a) corr(c_base_pct, R)이 양수이고 (b) conf 분위-R이 양 반기 단조이면
  CF-A 1차 셀을 재측정해 채택을 재심한다. 둘 중 하나라도 실패하면 CF-A를 **영구 기각**
  하고, whale 레버리지는 현행 합성 conf 티어로 고정한다.
- **하지 말아야 할 것 (측정으로 닫힘):** 위험 정규화(LEV-R, 6e) · 구조 캡 단독
  (CV-C, §5) · 평탄 캡(6d 무작위/상수 대조) · c_fit 역보정(§3 기각 유지).
