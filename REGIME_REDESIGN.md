# REGIME_REDESIGN — 국면 분류 / 게이트 층 재설계

측정 기준: 설계 구간 2023-06-01~2026-01-31, 9자산.
판정 통계: **좌석 없는 기록** + **주 단위 블록 부트스트랩 CI**(중첩 보정).
사전 등록 채택 기준: `CI가 0 배제` AND `반기 부호 동일` AND `n >= 150`.

이 문서는 성과 개선을 주장하지 않는다. 대부분의 항목은 *정합성*과 *측정 가능성*
근거로만 정당화된다. 성과 근거가 있는 항목은 그 CI를 명시하고, 다중검정 부담도
같이 적는다.

---

## 0. 왜 이 문서가 필요한가 — 측정 기반의 붕괴

whale 백테스트는 설정 비교의 근거가 될 수 없다.

```
설계구간 whale 트레이드 451건을 블록 부트스트랩으로 순서만 재배열 (5000 경로)
  실현  16.30x
  p5     1.81x    p25   6.49x    p50  16.87x    p75  43.86x    p95 206.84x
```

단일 좌석 풀마진 복리는 최종 자산이 **큰 위너가 자본이 클 때 왔는가**로 결정된다.
설정을 조금 바꾸면 좌석에 다른 트레이드가 앉고 경로 전체가 달라진다. 따라서
`total_return`으로 인용된 모든 채택 근거(예: `trail_atr` 8→10의 "10.5x→26.3x",
Z-A의 "26.27x→33.76x")는 p25~p75 노이즈 밴드 안이다.

기대값도 whale 표본으로는 해소되지 않는다.

```
설계구간 whale  n=451  mean R +0.1220  SD 1.8427
  95% CI [-0.0416, +0.2945]   P(exp<=0) 7.6%
  이 크기 엣지를 80% 검정력으로 해소: ~1,826건 (월 12건 기준 12년)
```

**결론**: 판정은 좌석 없는 기록에서, 통계는 주 블록 CI로. whale은 배포 축이지
판정 축이 아니다.

---

## 1. 확립된 사실

### 1a. 국면 분포 (바 단위, 9자산, n=185,003)

```
TREND_DOWN  24.3%
RANGE       24.1%
TREND_UP    22.0%
CRISIS      19.6%   <- 진입 완전 차단
HIGH_VOL    10.0%
```

### 1b. `bias1d == d`는 TREND 상태 안에서 중복

```
state x bias1d (행 정규화)
b1           -1.0    0.0    1.0
TREND_DOWN  0.983  0.017  0.000
TREND_UP    0.000  0.004  0.996
RANGE       0.335  0.249  0.416
```

`regime.raw_regime_1d`의 `up = strength & (c > e100) & (e20 > e50)`와
`signals.build_features`의 `bias1d[(g20 > g50) & (close > g100)] = 1`은 같은
일봉 EMA 조건이다. 차이는 `strength`(|EMA20-EMA50| > 0.5 ATR)와 히스테리시스뿐.

- **PBK / RCL** (`state == trend` 요구): 1D 게이트가 추가로 거르는 것 **0.4~1.7%**.
  죽은 조건.
- **BRK** (`state.isin([trend, "RANGE"])`): RANGE 바에서 롱 기준 58% 제거.
  실질 게이트.

### 1c. 게이트 대조 — 상위집합 내부 (n=6535)

`state`/`bias1d`/`bias4h` 게이트를 전부 무력화한 상위집합을 생성하고, 진입 시점의
**진짜** 값으로 분할해 대조했다. 표본 크기 비교가 아니라 동일 표본 내 대조다.

```
=== state 게이트 ===
HIGH_VOL       n=1002  -0.1137  CI[-0.190,-0.028] *  SAME
RANGE          n=1867  +0.0184  CI[-0.062,+0.108]    SAME
trend-against  n=1697  +0.0120  CI[-0.067,+0.097]    FLIP
trend-with     n=1969  -0.0342  CI[-0.105,+0.044]    SAME

=== 1D 게이트 ===
bias1d ==d     n=3092  -0.0294  FLIP
bias1d ==0     n= 615  +0.0269  SAME
bias1d ==-d    n=2828  -0.0184  FLIP

=== 4H 게이트 ===
bias4h ==d     n=3276  +0.0348  CI[-0.056,+0.131]    FLIP
bias4h ==-d    n=3259  -0.0737  CI[-0.092,-0.056] *  SAME
```

**사전 등록 기준을 통과한 것은 둘뿐이며, 둘 다 부정형이다.**

1. **4H 역행은 나쁘다** — n=3259, 양 반기 -0.076 / -0.072
2. **HIGH_VOL 진입은 나쁘다** — n=1002, 양 반기 -0.112 / -0.115

증거가 있는 게이트는 4H와 HIGH_VOL 둘뿐이다. 시스템이 1차 축으로 삼는
**1D 바이어스와 TREND 상태에는 증거가 없다.** `trend-with`(-0.034)가
`trend-against`(+0.012)보다 낮아 부호가 설계 전제와 반대다.

### 1d. BRK 단독 — 유일하게 확립된 슬롯

```
좌석 없는 기록, 주 블록 CI
BRK_L  n=300  +0.3831  CI[+0.091,+0.682] *  SAME   <- 유일한 확립 엣지
PBK_L  n=302  -0.1682  CI[-0.324,-0.0035]*  SAME   <- 상단 폭 0.0035, 가설 수준
PBK_S  n=333  -0.0599  CI[-0.313,+0.223]    FLIP
BRK_S  n=148  -0.0304  CI[-0.382,+0.419]    FLIP
RCL_L  n=133  -0.0559  CI[-0.148,+0.043]    FLIP
RCL_S  n= 98  +0.0650  CI[-0.139,+0.286]    FLIP
```

상위집합에서 BRK을 국면·바이어스별로 쪼개면:

```
BRK bias1d ==d     n=544  +0.1653  SAME    <- 게이트 통과분이 최저
BRK bias1d ==0     n=124  +0.4197  SAME  * (n<150, 기준 미달)
BRK bias1d ==-d    n=420  +0.2698  SAME

BRK RANGE          n=345  +0.2834  SAME
BRK trend-against  n=280  +0.3488  SAME
BRK trend-with     n=314  +0.2039  SAME  *   <- 역시 최저
BRK HIGH_VOL       n=149  -0.0278  FLIP
```

BRK은 HIGH_VOL을 빼면 모든 국면·모든 1D 바이어스에서 양수이고, 게이트가 요구하는
조건이 매번 가장 낮다. **BRK의 엣지는 추세 상태에 조건부가 아니다.**

### 1e. CRISIS — 단위 오류와 4중 자물쇠

```
자체 CRISIS 비율 (설계구간)
  BTC 3.7%  ETH 13.3%  XRP 12.7%  ADA 19.4%  SOL 21.5%
  DOGE 25.2%  SUI 30.5%  HYPE 31.4%  ONDO 32.0%
BTC 전파 기여: 0.0~1.2%p (무시 가능)
히스테리시스 증폭: raw 1.3% -> 3.7% (BTC), 2.8배
CRISIS 에피소드: 7회, 중앙 5일, 최대 8일
```

```python
crash  = (r5 < -0.12) | (r1 < -0.07)          # 절대 퍼센트
crisis = crash | ((vol_pct > 90) & (r5 < -0.08))   # vol_pct는 자산 자체 백분위
```

`crash` 항만 절대값이라 **같은 함수 안에서 단위가 섞여 있다.** BTC에게 하루 -7%는
크래시, ONDO에겐 평범한 변동이다. 결과적으로 고변동 알트가 시간의 1/3을 차단당한다.
이는 백테스트 성과와 무관하게 사전적으로 지적 가능한 결함이다.

자물쇠는 4개다.

```python
# 1. signals._playbook   mask가 state==trend / [trend,RANGE]만 허용 (후보 미생성)
# 2. config.regime_lev_cap["CRISIS"] = 0.0
# 3. engine.py:324       if ... or state == "CRISIS": continue     (체결 시점 차단)
# 4. engine.py:507       if state == "CRISIS": finalize(pos, "Regime")  (강제 청산)
```

**정정 (초판의 오류)**: 초판은 3번을 "도달 불가 코드"로 적었다. 틀렸다.
`engine.py:290`의 `state = state_at[pend.sym].iloc[i]`는 **체결 바의 심볼별**
상태다. 후보는 t바에서 생성되고(1번이 CRISIS를 배제) 체결은 t+1바이므로, 그
사이에 CRISIS가 걸리면 3번이 발화한다. 실측: 설계구간 비CRISIS 바 148,751개 중
278개(0.19%)가 다음 바 CRISIS다. **도달 가능한 경쟁 상태 가드이며 삭제하면 안 된다.**

2번은 3번이 먼저 `continue`하므로 실제로 도달하지 않지만, 비용이 없는 방어 코드라
그대로 둔다.

**4번은 비용이 아니라 수익원이다.** whale 기록에서 `Regime` 청산은
n=29 / 승률 89.7% / 평균 +3.02R로 Trail 다음가는 R 기여원이다. 급락 중 숏이
크게 이익인 상태에서 강제 청산되므로 사실상 "크래시 바닥 익절"로 작동한다.

따라서 CRISIS 임계값 정규화는 **양방향**이다. 진입 차단이 줄어드는 만큼 이 수익성
있는 강제 청산도 줄어든다. 순효과는 측정된 바 없다.

---

## 2. 층 1 — 정합성 수정 (성과 주장 없음)

성과 데이터 없이 정당화되며, 채택 여부가 측정 결과에 의존하지 않는다.

| # | 항목 | 근거 | 위험 |
|---|---|---|---|
| L1-1 | PBK/RCL에서 `bias1d == d` 제거 | `state == trend`가 이미 함의(99.6%/98.3%). 죽은 조건 | 없음 — 실측 확인 |
| ~~L1-2~~ | ~~CRISIS 분기 정리~~ | **철회.** 가드가 도달 가능(§1e 정정) | — |
| L1-3 | `crash` 항을 자산 변동성 단위로 | 같은 함수 내 `vol_pct`는 정규화. 단위 혼용 | **큼. §4 참조** |

**L1-1 적용 결과 (2026-08-06)**

죽은 조건이라는 진단이 바 수준을 넘어 후보 수준에서 실증됐다. `state == trend`이면서
`bias1d != d`인 바 중 나머지 진입 조건까지 성립하는 것은 설계 구간 9자산 전체에서
PBK **0개**, RCL **4개**다.

```
좌석 없는 기록: n=1361, 슬롯별 평균·CI 전부 소수점 4자리까지 불변
whale 설계구간: n=450, exp +0.0999, pf 1.359, ret 17.77x, MDD -0.4733 — 불변
```

동작이 바뀌지 않았다는 것이 이 수정의 성공 조건이었다. 얻은 것은 성과가 아니라,
1D 타임프레임이 PBK/RCL의 방향 판단에 기여한다는 **잘못된 인상의 제거**다.

**L1-2는 철회한다.** §1e 정정 참조. **L1-3은 §4의 독립 과제로 분리한다** —
단위 오류라는 진단은 확실하지만 수정의 손익이 양방향이기 때문.

---

## 3. 층 2 — 게이트 재설계

### 제안

```
유지    bias4h == d          증거: 역행 n=3259, CI[-0.092,-0.056], 양 반기 동일
유지    HIGH_VOL 진입 차단    증거: n=1002, CI[-0.190,-0.028], 양 반기 동일
제거    bias1d == d          증거 없음. 점추정은 오히려 통과분이 최저
제거    state == trend       증거 없음. trend-with < trend-against
```

### 조합 확인 (사후 선택 — 근거가 아니라 확인)

```
superset (게이트 없음)         n=6535  -0.0193  FLIP
현행 4H+1D+trend-state        n=1088  -0.0053  FLIP
제안 4H + not HIGH_VOL        n=2784  +0.0667  SAME  h1+0.014 h2+0.120
4H만                          n=3276  +0.0348  FLIP
4H + 1D (state 제거)           n=1754  +0.0060  FLIP
4H + trend-state (1D 제거)     n=1088  -0.0053  FLIP

BRK 현행                       n= 289  +0.2295  CI[+0.021,+0.443] *  SAME
BRK 제안                       n= 754  +0.3382  CI[+0.072,+0.610] *  SAME
```

제안 조합은 거래 수 2.6배, 기대값 -0.005 → +0.067, 반기 부호 동일. 그러나
**CI는 여전히 0을 걸친다**([-0.031, +0.173]). 전체 시스템 수준에서 이 제안은
"측정상 더 낫다"가 아니라 "측정상 나쁘지 않고 표본을 2.6배로 늘린다"이다.

BRK 수준에서는 n 289→754, 기대값 +0.23→+0.34로 둘 다 유의. 다만 이 조합은
데이터를 본 뒤 고른 것이므로 별표가 사전 등록된 발견은 아니다.

### 다중검정 부담

대조 15개를 수행했다. 5% 기준 우연히 0.75개가 유의하게 나온다. 별 4개 중:

- `bias4h == -d`, `HIGH_VOL` — n이 크고 CI가 0에서 멀며 반기 동일 → **신뢰**
- `BRK trend-with` — CI 하단 +0.013, 경계
- `BRK bias1d == 0` — n=124로 하한 150 미달 → **기각**

### 왜 이것이 argmax 사냥과 다른가

이 세션에서 앞서 제기된 개선 후보(PBK_S 비활성화, 좌석 재배분, hold 재설계,
점유율 메우기)는 전부 검증에서 죽었다. 전부 노이즈 안의 argmax였기 때문이다.
층 2가 다른 점:

- **부정형 발견에 기반한다.** "4H 역행은 나쁘다", "HIGH_VOL은 나쁘다" — 둘 다
  CI가 0에서 멀고 양 반기 동일. 부정형은 긍정형보다 견고하다.
- **조건을 추가하지 않고 제거한다.** 제거는 표본을 늘려 후속 측정을 쉽게 만든다.
  추가는 표본을 줄여 자기 검증을 불가능하게 만든다.
- **제거 대상에 증거가 없다는 것이 근거이지, 제거하면 좋아진다는 것이 근거가 아니다.**

### 3a. 적용 결과 (2026-08-06)

채택 기준은 §0의 비대칭 원칙(게이트 유지가 귀무가설)에서 기계적으로 도출해
**결과를 보기 전에** 고정했다: ① BRK_L의 EDGE+ 생존, ② 전체 점추정과 CI 하단이
내려가지 않을 것, ③ 어떤 슬롯도 새로 EDGE-가 되지 않을 것, ④ 거래 수 증가.

```
                    S0 (state==trend)          적용 후 [trend, RANGE]
ALL       n=1361 +0.0260 CI[-0.0936,+0.1455]   n=1539 +0.0364 CI[-0.0778,+0.1529]
BRK_L     n= 300 +0.3831 CI[+0.0874,+0.6780]   n= 299 +0.3844 CI[+0.0898,+0.6791]  EDGE+
PBK_L     n= 302 -0.1682 CI[-0.3254,-0.0039]   n= 397 -0.1235 CI[-0.2753,+0.0417]  EDGE- -> -
BRK_S     n= 148 -0.0304                       n= 129 -0.0807
```

4개 기준 전부 통과. BRK_L은 사실상 무변(300 -> 299, 평균 +0.3831 -> +0.3844).

**실험이 효과를 과대평가했다 — 기록해 둔다.** 사전 실험은 `_playbook`에 가짜
state를 넘겨 게이트를 무력화했는데, 그 가짜 state가 mask뿐 아니라
`fit = (state == trend)`까지 1.0으로 만들었다. `conf`에 `w_regime * fit`이 들어가므로
RANGE 바의 conf가 부풀려져 `conf_entry`를 더 많이 통과했고, n을 1732로 예측했다.
실제 구현은 `fit`이 진짜 state를 읽으므로 n=1539(+13%)다. 실험이 예고한
**반기 FLIP -> SAME 개선도 실현되지 않았다**(적용 후에도 FLIP: h1 -0.012, h2 +0.085).
그 주장은 철회한다.

**역추세 변형(S2)은 채택하지 않았다.** S2(HIGH_VOL/CRISIS 외 전부 허용)도 같은
4개 기준을 통과했고 수치도 더 높았으나(+0.0463 대 +0.0349, 실험 기준), 두 CI가
거의 완전히 겹쳐 측정이 둘을 구별하지 못한다. 측정이 구별하지 못할 때는 [A] 원칙이
결정한다: 증거 없이 시스템 전제를 바꾸지 않는다. S2는 추세추종 시스템에 역추세
진입을 도입하는 전략 변경이고, 대가로 얻는 것이 노이즈 안의 차이다.

**BRK 잠식은 확립 슬롯에 대해서는 기우였다.** 게이트 완화로 PBK/RCL 후보가 늘면
`scan`의 바당 1개 중복 제거(승자는 conf 최고, 그리고 corr(conf,R) = -0.026)에서
BRK을 밀어낼 수 있다. 실측: BRK_L은 300건 중 295건 유지(소멸 5, 신규 5), BRK_S는
148 -> 117로 33건 소멸. 잠식은 BRK_S에 집중되는데 BRK_S는 적용 전후 모두 판정
불가라 확립된 엣지를 잃은 것이 아니다.

다만 `scan`의 타이브레이크가 무정보 변수라는 것 자체는 남는 문제다. [A] 클래스로
"확립 슬롯 우선"으로 고정하는 것이 §5e의 좌석 정렬과 같은 원칙이다. 이번 변경의
전제 조건은 아니었으나(BRK_L 무손상), 별도 항목으로 남긴다.

**미해결 불일치**: `fit`은 PBK/RCL에서 RANGE에 0.0을 주지만 BRK은 0.4를 준다. RANGE를
유효 상태로 인정하면서 적합도를 0으로 두는 것은 앞뒤가 안 맞고, RANGE 진입의 conf를
낮춰 조용한 추가 게이트로 작동한다. 손대지 않았다 — conf는 [A] 클래스이고 R을
랭킹하지 못하므로, 거래 수를 늘리려 조정하는 것은 성과를 근거로 [A] 노브를 튜닝하는
셈이 된다.

### 남는 위험

- 전부 설계 구간 in-sample이다. 게이트 임계값이 이미 이 구간에서 정해졌다.
- BRK의 방향 게이트를 건드리는 것은 **유일하게 확립된 엣지**를 건드리는 것이다.
  `bias1d` 제거 시 BRK은 RANGE에서 방향 게이트를 4H에만 의존하게 된다.
- 상위집합 전체 평균은 -0.019로 음수다. "게이트를 다 없애라"는 결론이 아니다.

---

## 4. CRISIS 재설계 — 독립 과제

### 진단 (확실)

`crash` 항의 단위 오류. BTC 3.7% vs ONDO 32.0%는 시장 상태 차이가 아니라
변동성 차이다.

### 수정 방향 (A 클래스 — 원칙으로 고정, 성과로 튜닝 금지)

```python
# 현재
crash = (r5 < p.crash_5d) | (r1 < p.crash_1d)

# 제안: 자산 자체 변동성 단위
sigma = realized_vol(c, 20)                      # vol_pct 계산에 이미 존재
crash = (r5 < -k5 * sigma * sqrt(5)) | (r1 < -k1 * sigma)
```

`k5`, `k1`은 정규 꼬리 배수로 **사전 고정**(예: 3σ). 백테스트로 고르는 순간
A 클래스 규칙 위반이다.

### 왜 지금 채택하지 않는가

수정의 손익이 양방향이고 순효과가 미측정이다.

- 진입 차단 완화 → 알트 좌석 공백 회수 (이득 가능)
- 강제 청산(`Regime`, +3.02R × 29건) 감소 (손실 가능)

### 사전 등록 실험

1. 정규화 전/후로 좌석 없는 기록 생성
2. **진입 축과 청산 축을 분리 측정** — 진입 차단 완화분의 R, 강제 청산 감소분의 R
3. 채택 조건: 두 축 합산이 중립 이상 AND 반기 부호 동일

이 실험 없이 L1-3을 적용하면 안 된다.

### 4b. 실험 결과 (2026-08-06) — **기각**

`k`는 BTC의 현재 발화율을 보존하도록 역산했다(k1=2.62, k5=2.40). 바뀌는 것은
자산 간 분포뿐이며, 그것이 진단된 결함이다. 현상 유지에 맞추는 것은 성과 적합이 아니다.

```
crash 발화율     절대      정규화
  BTC            1.2%       1.4%      <- 보존됨
  ETH            5.6%       2.0%
  DOGE          11.8%       1.1%
  SUI           15.1%       1.6%
  HYPE          17.2%       0.6%
  ONDO          15.1%       0.6%
```

```
                    A (절대, 현행)              B (정규화)
ALL          n=1539 +0.0364 [-0.078,+0.153]  n=1717 +0.0314 [-0.074,+0.143]
BRK_L        n= 299 +0.3844 [+0.090,+0.679]  n= 308 +0.4123 [+0.127,+0.702]
Regime 강제청산 n=76 +2.7584 합 +209.64        n= 38 +4.3195 합 +164.14
총 R 합계            +55.97                        +53.88

진입 축: 신규 257건 mean -0.0978 합 -25.15
청산 축: Regime 76 -> 38, R합 +209.64 -> +164.14 (-45.50)
순 변화: -2.10R
```

**채택 조건 "두 축 합산 중립 이상"을 통과하지 못했다. 기각한다.**

세 가지를 함께 기록한다.

1. **-2.10R은 0과 구별되지 않는다.** 총 R 합계의 표준오차는 1.75*sqrt(1539) ~ 69R이고
   -2.10은 0.03 SE다. **채택 조건 자체가 잘못 설계됐다** — SE가 거대한 양에 점추정
   임계를 걸었다. 허용오차를 명시했어야 한다. 다음 사전 등록에서 고칠 점이다.
2. **측정 가능한 유일한 엣지는 개선된다.** BRK_L이 모든 축에서 나아진다
   (+0.3844 -> +0.4123, CI 하단 +0.090 -> +0.127, n 299 -> 308).
3. **§4의 가설이 직접 반증됐다.** 새로 열린 257건의 평균이 -0.0978이다. 절대
   임계값이 막던 바는 실제로 평균 이하였다 — 단위가 틀린 채로 **우연히 유용한 일**을
   하고 있었고, 고변동 알트에 대한 두 번째 변동성 필터로 작동했다. "단위만 고치면
   순수 개선"이라는 §4의 전제는 틀렸다.

기준이 잘못 설계됐다는 이유로 사후에 통과시키는 것은 이 문서가 비판하는 argmax
사냥과 같은 행위다. 사전 등록은 자기에게 불리할 때 지켜야 의미가 있다.

**보상 조정은 하지 않는다.** 정규화를 채택하면서 진입 축 손실을 상쇄하려
`vol_pct_highvol`을 함께 조이는 것은 정확히 노이즈에 맞추는 행위다.

단위 혼용은 코드 정합성 문제로 `config.py` 주석에 남아 있다. 고치려면 진입 축의
반대 증거를 뒤집을 새로운 근거가 필요하다.

### 4a. [M] 노브 스윕 결과 (2026-08-06) — 둘 다 변경 없음

`hysteresis_bars`와 `vol_pct_highvol`을 좌석 없는 기준에서 스윕했다. 응답곡선
전체를 출력했다 — 진짜 효과는 넓고 과적합은 뾰족하므로, argmax만 보면 노이즈를
고르게 된다.

```
hysteresis_bars (현재 2)          전체        BRK_L
  0    n=1720  +0.0276 SAME     +0.3337  EDGE+
  1    n=1623  +0.0379 FLIP     +0.3368  EDGE+
  2    n=1539  +0.0364 FLIP     +0.3844  EDGE+
  3    n=1446  +0.0585 SAME     +0.4321  EDGE+
  4    n=1382  +0.0531 SAME     +0.4220  EDGE+
```

BRK_L이 0->3에서 단조 상승 후 평탄. 넓은 형태지만 hyst=2의 CI[+0.090,+0.679]와
hyst=3의 [+0.129,+0.738]이 거의 완전히 겹쳐 **셀을 구별하지 못한다.** 현재값 유지.

**가설 반증**: "1D 국면의 2일 지연이 4H 주축 시스템에 과도하다"고 추정했으나 방향이
반대다. 지연이 길수록 BRK_L이 낫다. 근거 없이 세운 추정이었다.

```
vol_pct_highvol (현재 80)         전체        BRK_L
  20   n= 816  +0.0022          +0.2575  CI[-0.077,+0.580]  EDGE+ 상실
  30   n= 941  +0.0064          +0.2753  CI[-0.068,+0.632]  EDGE+ 상실
  40   n=1057  +0.0434          +0.2989  CI[-0.016,+0.613]  EDGE+ 상실
  50   n=1203  +0.0680          +0.3685  CI[+0.042,+0.696]
  60   n=1322  +0.0840          +0.3475  CI[+0.050,+0.641]
  80   n=1539  +0.0364          +0.3844  CI[+0.090,+0.679]
  101  n=1767  +0.0091          +0.3571  CI[+0.053,+0.681]   (HIGH_VOL 차단 끔)

corr(n, 전체) = +0.163    corr(n, BRK_L) = +0.836
```

**HIGH_VOL 차단은 교차검증을 통과한다.** 자기반증 셀로 넣은 101(차단 끔)이 전체
최악(+0.0091)이고, 이는 상위집합의 HIGH_VOL 진입 -0.114와 독립적으로 일치한다.

**임계값은 80을 유지한다.** 40 이하는 BRK_L의 EDGE+를 깨뜨려 사전 등록 규칙에
따라 즉시 기각된다. 남은 유효 셀(50/60/80/90/101) 중 BRK_L이 가장 강한 곳이 80이다.
전체 기대값은 60에서 최고지만 CI가 완전히 겹치고 BRK_L은 거기서 더 나쁘다.

**인공물 가설도 반증됐다.** 곡선이 단조라면 "거래를 줄이면 건당 기대값이 오르는"
이 시스템의 일반 성질을 재생산하는 것뿐이라고 의심했으나, 실제 곡선은 60에서 꺾이는
역U자이고 corr(n, 전체) = +0.163으로 거의 0이다. 단순 표본축소 효과가 아니다.

**도구에 결론을 미리 박아두면 안 된다는 사례**: 이 스윕 스크립트는 마지막 줄에
"strong negative on overall + near-zero on BRK_L == the artifact"라는 결론을
하드코딩해 두었는데, 실제 값(+0.163 / +0.836)이 정반대였다. 실행 전에 써둔 추측이
출력에 섞여 나왔다.

### 부수 항목

- 히스테리시스가 CRISIS를 2.8배 증폭한다(raw 1.3% → 3.7%). 즉시 진입 + 3일 해제 +
  `r5`가 급락일 이후 5일 유지. 한 번 터지면 최소 8일. 스윕 결과(§4a) 히스테리시스
  자체는 현재값이 최선 구간이므로, 증폭은 `crash` 단위를 고치면 자연히 줄어든다.
- `crisis`의 두 번째 항 `(vol_pct > 90) & (r5 < -0.08)`도 절대값 성분을 남긴다.
  `vol_pct`는 정규화되어 있으나 `r5 < -0.08`은 아니다. 이번 수정 범위에서 제외했다 —
  진단이 명확한 `crash` 항만 먼저 고치고, 잔여 항은 별도로 다룬다.
- `effective_state`의 BTC 전파는 기여가 0.0~1.2%p로 무시 가능하다. 손대지 않는다.

---

## 5. 파이프라인 통합 설계

### 5a. 노브 3분류 (모든 파라미터가 정확히 하나에 속한다)

| 클래스 | 정하는 주체 | 판정 통계 | 대상 |
|---|---|---|---|
| **M (측정)** | 좌석 없는 기록 | 주 블록 CI + 반기 부호 + n>=150 | 진입 트리거, 청산 파라미터, 스톱 배치, 국면 임계값, 히스테리시스 |
| **A (원칙)** | 사람이 이유를 적고 고정 | 없음. 성과로 못 바꿈 | 좌석 정렬, 유니버스, 쿨다운, CRISIS 단위, conf 게이트 |
| **D (배포)** | MC 생존 게이트 | 파산확률만. 최적화 금지 | 레버리지, per-stop 손실 목표, 마진 비율 |

규칙:
- **A 클래스는 성과 수치를 근거로 인용 금지.** 이유만 적는다.
- **D 클래스는 통과/실패만.** "더 좋은 값"을 찾지 않는다.
- **M 클래스만 데이터가 정한다.** whale 백테스트가 아니라 좌석 없는 기록에서.

### 5b. 2단 판정 구조

```
1단 판정   좌석 없는 포트폴리오, 9자산, n≈1361/2.7년 (게이트 완화 시 n≈2784)
           주 단위 블록 부트스트랩 CI (중첩 60% 보정)
           채택: CI 0 배제 AND 반기 부호 동일 AND n>=150

2단 생존   whale 풀마진, MC 파산확률 게이트
           통과/실패만. 수익률·샤프로 순위 매기지 않는다
```

**1단 하네스가 현재 없다.** `shadow.py`는 라이브 증분 전용이고 백테스트 진입점이
없다. 그리고 `shadow.params()`가 `whale_mode=True`를 유지해 풀마진 9포지션 동시가
되므로, 백테스트로 돌리면 계좌가 터진다(실측: n=1361이 n=79로 붕괴). 1단 하네스는
`whale_mode=False` + 포트폴리오 사이징이어야 한다.

### 5c. 좌석 접근권 (risk_scale과 별개 축)

현재 `risk_scale=0.20`은 마진만 줄이고 **좌석 점유는 그대로**다. 미검증 슬롯이
계좌 전체를 최대 10배로 잡고 앉아 BRK_L을 막는다. `promotion.py`의
`LADDER=(0.20,0.50,1.0)`에 off 단이 없어 영원히 그렇다(`return max(base, earned)`).

```
Tier P   1단 채택 조건 통과   라이브 좌석 진입 가능, 풀마진, 최대 10배
Tier E   그 외 전부          shadow 북 전용. 라이브 좌석 접근 불가
```

현재 Tier P는 **BRK_L 하나**다. 이는 라이브가 롱 전용이 되고 TREND_DOWN에서
무거래가 됨을 뜻한다. **이 설계에서 가장 비싼 결정이며 별도 승인이 필요하다.**

### 5d. 레버리지 역전 (D 클래스)

```
per-stop 계좌 손실 = leverage * stop_frac   (whale 설계구간 n=451)
  p5 5.0%  p50 12.7%  p95 26.8%  max 31.4%   최대/최소 14.3배
  corr(conf, R) = -0.026     corr(leverage, R) = -0.001
```

베팅 크기가 14배 범위로 흔들리는데 그것을 정하는 변수가 R과 무상관이다.

```
현재:  conf -> 티어(2/3/5/10x) -> 캡 -> per-stop 손실은 결과적으로 2.2~31.4%
제안:  per-stop 손실 목표 L 고정 (사전 리스크 결정)
       lev = L / stop_frac
       캡: lev_liq_cap(stop_frac), asset_cap, regime_lev_cap, 10x 하드
       floor_tier()
```

효과: 모든 트레이드가 스톱 시 동일 비율 손실. 스톱이 좁을수록 자동으로 10배가
나오므로 공격성이 체계화된다(현재 10x는 45/451 = 10%뿐). conf가 레버리지 경로에서
사라진다. R은 레버리지 독립이므로 1단 판정에 영향이 없다.

**생존 제약**

```
연속 손실: 최대 11회 관측 (p90=5, p99=9). 손실 중 81%가 full SL.

per-stop L      k=4     k=6     k=8    k=11
      10%     65.6%   53.1%   43.0%   31.4%
      15%     52.2%   37.7%   27.2%   16.7%
      25%     31.6%   17.8%   10.0%    4.2%
      35%     17.9%    7.5%    3.2%    0.9%
```

현재 실효 중앙값은 12.7%다. L=0.25는 꼬리 리스크를 약 2배로 만든다. 11연패는
이미 관측된 사건이고 그때 계좌가 4.2% 남는다.

**L은 파산 임계를 먼저 정하고 MC를 만족하는 최대값으로 뽑는다.** 수익률로 고르면
D 클래스 규칙 위반이다.

### 5e. 좌석 정렬 (A 클래스)

좌석 축은 좌석 없는 기록에 존재하지 않으므로 영원히 측정 불가다. 성과로 정하지
않고 원칙으로 고정한다.

```
1순위  Tier P만 후보       이유: 미검증에 계좌 전체를 걸지 않는다
2순위  stop_frac 최소      이유: 같은 손실 한도에서 최대 레버리지를 쓸 수 있고
                                liq-before-stop 여유가 가장 크다
```

`conf` 정렬과 `seat_rank_cbase`를 제거한다. 둘 다 무정보 변수 기반이다.

---

## 6. 구현 순서

```
0. 1단 하네스  [완료]    run_seatfree.py + shadow.params_backtest()
                         validation.week_block_ci()
   검증 통과: n=1361, BRK_L +0.3831 CI[+0.0874,+0.6780] SAME -> EDGE+
              PBK_L -0.1682 CI[-0.3254,-0.0039] SAME -> EDGE-
              나머지 6슬롯 전부 "판정 불가"

1. 층 1 정합성 [완료]    L1-1만 적용. L1-2 철회
   검증 통과: 좌석 없는 기록·whale 기록 모두 불변

2. 노브 3분류표 [완료]   config.py에 M/A/D 태그
   검증: 그룹 헤더 태그 완료. 개별 파라미터 태깅은 해당 축을 손댈 때 진행

3. 층 2 게이트 [완료]    PBK/RCL의 state 허용을 [trend, RANGE]로 (BRK과 동일)
                         4H 게이트 유지, HIGH_VOL/CRISIS 배제 유지
   사전 등록 기준 4개 전부 통과 (§3a)

4. 레버리지 역전         risk.py: conf_tier 대신 L/stop_frac
   검증: per-stop 손실 분포가 단일값으로 수렴, MC 파산확률 사전 임계 통과

5. 좌석 접근권 티어      seat_eligible 신설. promotion.py가 1단 결과를 읽음
   ** 별도 승인 필요 — 라이브가 롱 전용이 된다 **

6. CRISIS 정규화         §4 사전 등록 실험 통과 시에만
```

0~2는 나머지의 전제다. 3이 가장 큰 성과 영향, 5가 가장 비싼 결정,
4가 공격성을 체계화하는 부분이다.

---

## 7. 이 설계가 못 하는 것

- **수익률 개선을 약속하지 못한다.** 좌석 없는 기록 전체 기대값 CI는
  [-0.092, +0.147]이다. 이 설계는 측정 가능성과 베팅 크기 일관성을 개선하지
  엣지를 만들지 않는다.
- **전부 설계 구간 in-sample이다.** 국면 임계값(`crash_5d`, `crash_1d`,
  `vol_pct_*`, `strength > 0.5 ATR`, `hysteresis_bars`)이 이미 이 구간에서
  정해졌다. BRK_L의 CI도 그 위에 있다. G3 홀드아웃은 소진 완료다
  (TAIL_REDESIGN §3, FAIL).
- **좌석 로직은 영원히 검증 불가하다.** A 클래스 고정은 "포기"의 형식화다.
- **CRISIS 수정의 순효과를 모른다.** 진입 축과 청산 축이 반대로 움직인다.
- **미오염 구간은 2026-07-10 이후 4주뿐이고 forward 12건이 전부다.**

---

## 8. 국면 라벨 자체의 검정 (2026-08-06) — TREND_DOWN은 무정보

지금까지 국면은 **트레이드 결과를 통해서만** 평가됐다. 그것은 게이트를 통과한
표본 안에서만 보는 것이라(collider 조건부) 라벨 자체를 검정하지 못한다. 라벨의
근본 주장 — "TREND_DOWN이면 가격이 내려간다" — 을 바 단위로 직접 쟀다. 트레이드
선택 편향이 없고 표본이 18만 바다.

```
전방 수익률 %, 바 단위, 설계구간 9자산 n=185,080, 주 블록 CI

state          n      wk |   +4h     +24h     +72h     +168h
CRISIS      36,329   105 |  +0.06    +0.33    +0.73    +0.89
HIGH_VOL    18,528    61 |  +0.05    +0.25    +0.57    +0.79
RANGE       44,655   114 |  +0.02    +0.12    +0.35    +0.68
TREND_DOWN  44,864    95 |  -0.01    -0.04    -0.01    +0.62
TREND_UP    40,704    90 |  +0.03    +0.20    +0.77    +2.24 [+0.48,+4.12] *
```

**TREND_UP은 작동한다** — 0을 배제하는 유일한 셀이고 지평선에 따라 단조 증가한다.
**TREND_DOWN은 하락 정보를 전혀 담지 않는다** — 전 지평선에서 0이고 168시간에서는
양수다.

### 8a. 사전 등록 부분집합 — 둘 다 실패

```
TREND_DOWN 전체        +72h -0.01   +168h +0.62
  BTC도 TREND_DOWN     +72h +0.14   +168h +0.67     가설1 실패
  BTC는 아님            +72h -0.15   +168h +0.58
  strength 하위 50%     +72h +0.12   +168h +1.11
  strength 상위 25%     +72h +0.09   +168h +0.34     가설2 실패
  strength 상위 10%     +72h +0.13   +168h +0.15

대조 TREND_UP          +72h +0.77   +168h +2.24 *
  BTC도 TREND_UP       +72h +0.90   +168h +2.67 *   대조군은 강화
```

어느 부분집합도 음수가 되지 않는다. 대조군이 강화되는 것은 방법론이 신호를 찾을 수
있음을 보이므로, TREND_DOWN에는 찾을 신호가 없다는 뜻이다.

`strength` 결과가 특히 시사적이다: 하락 추세가 강할수록 168시간 수익률이 낮아지지만
(+1.11 -> +0.34 -> +0.15) 끝내 음수가 되지 않는다. **강한 하락 추세는 "더 내려간다"가
아니라 "덜 반등한다"를 의미한다.**

### 8b. 재해석 — 분류기는 방향 예측기가 아니라 드리프트 랭커다

168시간 기준 TREND_UP(+2.24%)만 분리되고 나머지 넷은 +0.62~0.89%로 한 덩어리다.
5상태 분류기지만 거래 관점에서는 실질 2상태 — "드리프트가 강한 구간"과 "나머지"다.

이 관점에서 현재 설계의 오류가 드러난다. **TREND_DOWN을 숏 트리거로 쓰는 것은 라벨이
측정하지 못하는 것을 요구하는 것이다.** 라벨이 아는 것은 "여기는 드리프트가 약하다"
이고, 그것은 숏 진입 근거가 아니라 롱 회피 근거다.

이것은 이미 관측된 여러 사실을 하나로 설명한다.

- E6의 "숏 전 계열 기각(12/12 자산이 숏 진입 방향과 반대로 이동)" — 슬롯이 아니라
  슬롯이 키로 삼는 상태가 무정보였다.
- BRK_L이 유일한 확립 엣지인 이유 — 작동하는 유일한 상태에 걸린 유일한 슬롯이다.
- forward 로그의 13/13 SHORT과 shadow 5/7의 `peak_r = 0.00`(진입 즉시 역행).
- forward 구간 ADA: 409바 중 383바가 TREND_DOWN인데 구간 수익률 +15.72%.

### 8c. 근본 원인 — 대칭 설계 대 비대칭 시장

전 국면의 168시간 수익률이 양수다(+0.62 ~ +2.24). 자산군에 상방 드리프트가 있다.
드리프트가 있는 자산군에서 하락 추세는 지속이 아니라 감속이다.

그런데 플레이북 행렬은 완전 대칭이다. 모든 롱 슬롯에 숏 미러가 있고 같은
`risk_scale`, 같은 `conf_entry`, 같은 청산 구조를 쓴다. **시장이 비대칭인데 설계가
대칭이다.** 숏 슬롯은 좌석 없는 기록의 43%를 차지한다(PBK_S 415, BRK_S 134, RCL_S 98).

### 8d. 선택지와 비용 — 미결, 소유자 결정 필요

**A. 대칭 폐기 — 숏을 라이브 좌석에서 배제**
근거 3중: 무정보 상태 위에 서 있음(바 단위 44,864 / 95 주 블록), 자체 측정이
판정 불가~음수, E6에서 이미 기각. 비용: TREND_DOWN 구간 무거래, forward 표본
축적 둔화.

**B. 숏의 근거 교체 — 권하지 않음**
시계열 하락 추세로 불가능함이 확인됐다. 상대강도 등 대안은 측정된 근거가 없다.
지금 설계하면 개선이 아니라 발명이다.

**C. 비대칭 파라미터 — 숏이 남되 롱과 다르게**
최대 보유를 제한한다. TREND_DOWN의 전방 수익률이 168시간에서 확실히 양수가 되므로,
숏을 그 지평선까지 들고 가는 것은 기대수익이 양수인 구간에 숏을 유지하는 것이다.
이 논거는 바 단위 측정(인과적)에서 나온다.
**보유시간별 R 표로 검정할 수 없음에 주의** — 보유 시간은 설정이 아니라 결과다.
지는 트레이드는 스톱에 빨리 닿고 이기는 트레이드는 트레일을 달고 길어지므로,
그 표의 기울기는 기계적이지 발견이 아니다.
비용: `trail_atr=10`의 러너 포착을 숏에서 포기(숏 꼬리 평균 +1.94R).

**D. TREND_DOWN을 롱 필터로만 사용**
숏 트리거에서 빼고 "이 상태에서는 롱을 하지 않는다"로만 쓴다. 라벨을 그것이 실제로
아는 용도로 재정의한다. 실질은 A와 같다.

**권고: A + D.** 같은 결론에 이르는 두 경로다. C는 숏이 살아남아야 한다는 전제를
필요로 하는데 그 전제를 뒷받침하는 측정이 없다. 다만 이는 시스템 철학을 바꾸는
결정이므로 §5c(좌석 접근권)와 같이 별도 승인 항목으로 남긴다.
