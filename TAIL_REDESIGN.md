# TAIL_REDESIGN — 목표 정렬 재설계 (V6 / E20, 측정 완료)

작성 기준: 설계 구간 2023-06-01~2026-01-31, **9자산 유니버스**(2026-07-21 재편성
후 첫 전수 재측정 — XRP/ADA 히스토리 신규 페치). 반쪽 교차(H1 23-06~24-09 /
H2 24-10~26-01) + MC 3000경로. 홀드아웃 미개봉 유지. 모든 변형은 이 세션에서
플래그 뒤 A/B로 측정 후, 기각분은 하우스 규칙대로 코드에서 제거하고 판정만
기록했다 (SR-B/SR-C 방식).

---

## 0. 목표의 정직한 산술 (소유자 방향: 월 +100% tail 추구)

소유자의 목표는 월 +100% 이상, "더 좋은 기회에 더 큰 베팅"이다. 측정 사실로
번역하면:

- **기대값으로서의 +100%/월은 수학적으로 없다** (profile 주석의 기존 결론 유지:
  월 Sharpe ~3+ 필요, 현재 ~1). 추구 가능한 것은 **tail 월의 빈도와 크기**다.
- baseline whale (9자산, 31개월): **+100% 월 1회, +50% 월 5회**, 최고
  +105/+98/+96/+90%. tail은 이미 존재한다.
- tail의 최대 적은 디레버리징이 아니라 **파산 경로다**: MC P(MDD>50%) = 73.3%,
  95%p MDD −80%, 하위 5% 경로는 2.6년 뒤 원금 미만(final_p5 0.83). −80% 구덩이
  에서는 +100% 월이 와도 복리 베이스가 없다. tail 극대화 = 좌석 정확도 ×
  복리 베이스 보존.

## 1. 측정 캠페인 전결과 (whale, 9자산)

| 변형 | 내용 | ret | MDD | exp | H1/H2 exp | MC f_p95 | P(>50%) | 판정 |
|---|---|---|---|---|---|---|---|---|
| BASELINE | conf 티어 2/3/5/10x | 10.91x | −60% | +0.124 | −0.117/+0.232 | 226 | 0.733 | — |
| LEV-A(naive) | c_base 재앵커 티어 | — | — | — | 티어→R 반기 **전부 부호반전** | — | — | **폐기** (설계 단계) |
| LEV-A flat2x | 티어 폐지, 플랫 2x | 5.16x | −40% | +0.180 | +0.044/+0.207 | — | 0.035 | **보류** (생존 최적이나 소유자 방향과 불일치 — 기록 유지) |
| SEL-A | 좌석 tiebreak conf→c_base 백분위 | 7.91x | −60% | **+0.169** | **+0.004**/+0.217 | 132 | 0.723 | **채택 권고** |
| HLD-A | 손실측 mom-주도 hold 점수 | 7자산 유해(MDD−69%, Trail 54→49) / 9자산 중립 | | | | | | **기각** (강건성 없음) |
| W-PYR | 증거금 70% 진입 + 예비 30%를 +2R 스텝 피라미딩 | 6.87x | −63% | **−0.128** | −0.288/−0.039 | **96** | 0.675 | **기각** (전 지표 악화) |
| W-RNG | RANGE 레버캡 5→10x | 10.87x | −60% | +0.124 | 불변 | 224 | 0.734 | **no-op** (아래 1a) |
| W-T5 | 10x 티어만 제거 (2/3/5x) | 7.17x | −60% | +0.110 | −0.128/+0.214 | 124 | 0.712 | **기각** (tail↓, 파산 불변) |
| W-MINR | conviction-collapse에 min_r 계약 집행 | 설계구간 **byte-identical** | | | | | | **채택 권고** (비용 0 계약 정직화) |
| B1 | whale 펀딩 가산→거부권 | 설계구간 **byte-identical** | | | | | | **채택 권고** (라이브-온리 리스크 제거) |
| SORT-FIX | scan dedup 안정 정렬 | conf 동률 승자 비결정론 제거 | | | | | | **채택** (정합성 — 무조건) |

### 1a. 핵심 발견: "더 큰 베팅"의 사이징 신호는 존재하지 않음이 측정됐다

"더 좋은 기회에 더 큰 베팅"의 후보 트리거를 전부 측정했다:

1. **진입 예측(conf/c_base)** — 티어별 R이 반기마다 부호반전. conf 합성도
   c_base도 사이징 신호가 아니다 (E9/E17 재확인, 이번엔 티어 단위로).
2. **트레이드 자신의 증명(+2R 피라미딩)** — whale 스케일에서 전 지표 악화
   (W-PYR). +2R 증액은 R 분포의 꼭대기를 재매수하고 8ATR 트레일의 giveback을
   배가한다. E10의 포트폴리오 피라미딩(1% 리스크 단위)은 15% 증거금 단위로
   이식되지 않는다.
3. **국면(RANGE발 돌파 = 최고 R 하위그룹)** — 레버캡을 풀어도 no-op (W-RNG):
   바인딩 제약은 regime_lev_cap이 아니라 conf 티어다. c_fit=0.4가 RANGE 돌파의
   composite conf를 낮은 티어로 끌어내리는 **이중 역배분**이 확인됐지만, fit
   역보정은 CONF_REDESIGN §3의 기각(같은 데이터 2차 적합)을 유지한다.

**유일하게 강건한 신호는 좌석 선택(SEL-A)뿐이다**: 어느 코인이 한 발을 받을지
(순서)는 c_base가 개선하지만, 얼마나 크게 쏠지(크기)를 아는 신호는 현재 없다.
사이징 권한의 회복 조건은 CONF_REDESIGN §3C와 동일: 전방 n≥30에서 분위-R
단조성이 양쪽 반기 재현될 때.

### 1b. SEL-A의 정직한 트레이드오프

exp +0.124→+0.169, **H1 기대값 부호 회복**(−0.117→+0.004), MDD 불변. 대가:
복권 꼬리 압축 (f_p50 11.5→8.8, f_p95 226→132). baseline의 226x 꼬리는 H1에서
16개월 음수 기대값으로 피 흘리며 H2 행운 시퀀스에 올라탄 수치다 — H1 음수
전략은 실전에서 H2가 오기 전에 소유자가 포기한다. 강건성이 tail의 전제조건
이라 판단해 채택을 권고하되, 소유자가 raw tail을 선호하면 플래그를 끈다.

## 2. 진단 → 해소 매핑

| 세션 진단 | 해소 |
|---|---|
| 핵심: conf의 3역할 겸직 | ① 게이트: base는 CV-A 기채택, whale legacy 유지(측정) ② 좌석: **SEL-A로 c_base 백분위 분리** ③ 레버리지: 티어 유지(load-bearing, E17) — 천장은 소유자 다이얼 (§3) |
| 파이프라인 1: 무엣지 키가 한 발 배분 | SEL-A |
| 파이프라인 2: min_r 사문화 | W-MINR (계약 집행, 설계구간 불변) |
| 파이프라인 3: c_fund 라이브-온리 티어 점프 | B1 (가산→거부권, 설계구간 불변) |
| hold 1: 승자/패자 동일 문턱 | HLD-A로 비대칭화를 측정 → **기각**. 현행 단일 척도가 측정상 옳다 (E9/E16 계열 재확인: 손실 조기 절단은 미성숙 추세 승자를 자른다) |
| hold 2: streak 디바운스 허상 | HV-B 기각(E17) 유지; HLD-A 기각으로 손실측 재가중도 배제 — 남은 길은 3D 전방 인프라(청산 직후 24/48h 가상 보유 R)로 문턱 실효를 상시 측정 |
| (신규 발견) conf 동률 비결정론 | SORT-FIX: dedup 안정 정렬 — conf 동률(PBK_L/RCL_L 비트 동일 0.881469, 2025-06-13 HYPE 실측)의 승자가 무관한 배열 내용에 따라 뒤바뀌던 라이브-백테스트 재현성 결함 제거 |

## 3. 최종 구성 권고 (whale V6)

채택 권고 (전부 플래그, 기본 off — 무장은 소유자 결정):

```python
profile("whale") + replace(
    seat_rank_cbase=True,    # SEL-A: 한 발을 유일한 +corr 신호로 배분
    hold_minr_strict=True,   # W-MINR: 문서화된 min_r 계약 집행 (과거 불변)
    conf_fund_veto=True,     # B1: 미검증 펀딩이 레버 티어를 못 흔들게 (과거 불변)
)
# SORT-FIX는 플래그 없음 (무조건 정합성 수정, 이미 적용)
```

9자산 설계구간: ret 7.91x, exp +0.169, H1/H2 +0.004/+0.217, MDD −60%,
MC P(>50%DD) 0.723, f_p5 0.74 / f_p50 8.8 / f_p95 132.7.

**남는 단 하나의 다이얼 — 레버리지 천장 — 은 위험선호 결정이지 측정 문제가
아니다.** 측정된 프런티어 (V6 스택 기준 아님, 단독 측정치):

| 천장 | ret | MDD | P(>50%DD) | f_p5 |
|---|---|---|---|---|
| 현행 2~10x 티어 | 10.9x | −60% | 73% | 0.83 |
| 플랫 2x (참고) | 5.2x | −40% | 3.5% | 1.17 |

소유자는 tail 우선으로 현행 티어를 유지한다. 이 선택의 뜻을 기록한다:
**MC 경로의 73%가 절반 이상을 잃는 구간을 지나며, 5%는 2.6년 뒤 원금 미만이다.**
이 위험은 알고리즘 결함이 아니라 선택된 프로파일의 성질이다 (profile 주석의
기존 고지와 일치).

## 4. 잔여 게이트 (채택 전)

1. G5 ±20% 민감도 / 워크포워드 (SEL-A는 트레이드셋 거의 불변이라 저위험,
   W-MINR/B1은 설계구간 불변이라 자동 통과).
2. **전방 페이퍼 8주가 최종 심판** (E16 방식) — 특히 hold_entry/seat_report/
   conf 분위-R의 3D 인프라 지표.
3. 홀드아웃 미개봉 유지.
