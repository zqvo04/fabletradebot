# fabletradebot V4 — 고래 모드: 단일 집중 · 국면 인식 · 이벤트+연속 진입

**설계 철학**: 과거 데이터 최적화가 아니라 **선험적 완전성** — 모든 시장 국면에
대응하는 교과서-표준 진입 세트를 설계하고, 페이퍼 전방 트랙이 각 플레이북을
심판한다. TF 위계는 고래 관점: **4H가 기본 판단 축**(셋업 포착), **1D는 참고**
(국면·편향), **1H는 정밀 타점과 채점 전용**. 진입은 두 종류: 이벤트 확인형
(크로싱 순간)과 **연속 차트-상태형**(크로싱을 놓쳐도 현재 차트가 매집 구조면 진입).

이 시스템은 **고래 모드(V4)**로 운영된다: 포트폴리오 분산을 버리고, 매 봉 전
유니버스 후보 중 **가장 확신도 높은 단 하나**의 코인에 **전체 자산**을 확신도별
레버리지로 몰아넣는다. 진입한 자리는 청산까지 홀드하며(다른 코인 신호가 직접
밀어내지 않음), 매시 재채점되는 실시간 확신도(hold_confidence)가 승자는 모멘텀
페이드로 익절, 패자는 국면/모멘텀이 명확히 돌아섰을 때 구조적 손절보다 앞서
손실을 줄인다.

> **정직한 리스크 선언 (반드시 읽을 것)**: 고정 리스크로 손실을 상수화하는
> 포트폴리오 방식과 달리, 고래 모드의 손절 1회는 **레버리지×손절폭**만큼 계좌를
> 깎는다(예: 10x·손절 2% = −20%). 청산-거리 캡(`L_liq`) 때문에 이 값은 구조적으로
> **최대 ~35%**로 상한이 걸리지만, 단일 포지션 집중이라는 설계 자체가 이 저장소가
> 다뤄본 어떤 방식보다도 **높은 꼬리 수익·높은 파산 위험**을 동시에 가진다. 이
> 숫자를 숨기지 않는 것이 이 시스템의 핵심 가치다(§7).

문서: [BLUEPRINT.md](BLUEPRINT.md)(설계·수식) · [EXPERIMENTS.md](EXPERIMENTS.md)(반증
로그 — 기각 포함 전부, 최신 E18) · [CONF_REDESIGN.md](CONF_REDESIGN.md)(V5 conf/
hold_conf 재설계) · [WHALE_WORKFLOW.md](WHALE_WORKFLOW.md)(고래 좌석 유지/회전
워크플로우 진단 + V5.1 개선) · [VALIDATION.md](VALIDATION.md)(게이트 결과) ·
[SECRETS.md](SECRETS.md) · [CRON_SETUP.md](CRON_SETUP.md)

---

## 트레이딩 로직 — 구체 설명

### 0. 파이프라인 한눈에

```
매 1H 마감(+3분, cron-job.org → GitHub Actions, run_live.py)
 │
 ├─ [수집] OKX 1H 캔들+8h 펀딩 증분 수집 (data_okx.update_cache, CSV 캐시, 마감봉만)
 │
 ├─ [국면] 자산별 1D 국면 분류 (TREND_UP/DOWN·RANGE·HIGH_VOL·CRISIS + 히스테리시스)
 │    + BTC CRISIS 전역 상속 (regime.effective_state)                    [1D=참고]
 │
 ├─ [스캔] 코인별 독립 스캔 (signals.scan):
 │    마감 4H봉의 크로싱/연속-상태 셋업으로 플레이북 슬롯 무장           [4H=기본]
 │    → 1H 확인봉이 격발 → conf(entry confidence, c_base 단독=CV-A) 계산
 │    → 손절폭·변동성 하한 통과분만 후보                                [1H=정밀 타점]
 │
 ├─ [WF-A 정합 게이트] (고래 전용, engine.py §3) 결정봉의 hold_confidence를
 │    후보의 진입 방향으로 미리 재계산 — hold_conf_exit(0.50) 미만이면
 │    "즉시 SignalFade당할 후보"로 판단해 좌석 진입 자체를 스킵            [I1, WF-A]
 │
 ├─ [좌석 선택] 교차-코인 선택 (engine.py §1): 살아남은 후보를
 │    (검증상태 risk_scale, conf) 내림차순 정렬 → 최고 하나만 유일한
 │    자리를 차지 (자리가 차 있으면 대기 — Upgrade 규칙 예외만 밀어냄)
 │
 ├─ [리스크 게이트] 국면CRISIS·DD거버너·24h서킷 통과 시 다음 1H 시가에 체결
 │    (풀마진, conf→레버리지 티어, 청산안전 캡 L_liq, DD/상관 시 margin_frac 반감)
 │
 ├─ [보유 관리] 매 봉(engine.py §2):
 │    · 8×ATR1H 샹들리에 트레일 / 4H bias 2연속 플립 청산 / CRISIS 즉시청산
 │    · hold_confidence 재채점(HV-A: 연속 4H EMA20-EMA50 갭 fit + 대칭 rsi)
 │      → 승자 모멘텀 페이드(SignalFade) 익절 / 패자 붕괴(LossFade) 조기손절 [§7·§10]
 │
 ├─ [청산 시] 좌석이 비면 다음 실행에서 §스캔~좌석선택이 다음 최고 후보를 채움
 │    (같은 실행 내 교차-코인 회전 없음 — "밀어내지 않는다" 설계)
 │
 └─ [기록] Telegram 알림 + Notion 저널(오픈/청산/시간별 재채점) + journal/v1_state.json
      커밋 (롤링 앵커 = 새 봉만 증분 처리, §11) → scoring.score_report로 setup·
      국면·티어·코인·청산사유별 + Phase 0 진단(좌석시간/R, conf 단조성, hold_entry) 분해
```

**V5.1 정합 규율 요약**: 진입(conf)·유지(hold_confidence)·청산(SignalFade/
LossFade)·좌석선택이 서로 다른 잣대를 쓰면 "홀드 로직이 즉시 거부할 포지션이
좌석을 차지"하는 정합 누수가 생긴다(I1). WF-A는 이 누수를 진입 문턱에서
직접 봉합하도록 측정·채택된 유일한 개선이며(+26% 복리, EXPERIMENTS E18), 같은
원칙을 청산(WF-B)·좌석 tiebreak(WF-C)에 확장하는 시도는 측정으로 **기각**됐다
— 이진 BiasFlip과 legacy conf tiebreak가 우연이 아니라 복리 경로를 보호하고
있었다. 상세는 WHALE_WORKFLOW.md §6, EXPERIMENTS.md E18.

### 1. 데이터와 무(無)룩어헤드 규약

- OKX 무기한 선물 1H OHLCV + 8h 펀딩. 4H/1D는 1H에서 리샘플(부분 봉 제외).
- 판단 시각 t에는 **t에 이미 마감된 봉만** 사용. 상위 TF 피처는 "마감 시각 ≤ t"인
  봉만 투영(`closed_asof_1h`). pytest가 데이터 절단 불변성·돌파선 사전성으로 증명.

### 2. 국면 분류 — 자산별 방향 명시형 5국면 (V3)

**왜 자산별인가 (FN 제거)**: BTC 광역 국면 하나로 전 유니버스를 게이트하면 BTC가
횡보일 때 홀로 추세인 알트의 추세 셋업이 통째로 막히는 구조적 **False Negative**.
각 코인을 **자기 일봉으로 독립 분류**하고, 시스템 리스크인 **CRISIS만 BTC에서
전역 상속**한다(위기는 언제나 전역이므로).

각 자산의 1D봉에서 (우선순위 순):

| 상태 | 판정 | 진입 정책 | L 상한 |
|---|---|---|---|
| **CRISIS** | 5D<−12% 또는 1D<−7% 또는 (변동성>90p & 5D<−8%), 또는 BTC가 CRISIS | 신규 금지 + 보유 즉시 청산 | 0 |
| **HIGH_VOL** | 실현변동성(20D) 1년 백분위 > 80 | 이벤트 확인형(OSC)만, 절반 리스크 | 3x |
| **TREND_UP** | \|EMA20−EMA50\|>0.5ATR & 종가>EMA100 & EMA20>EMA50 | 롱 추세 플레이북(BRK_L/PBK_L/RCL_L) | 10x |
| **TREND_DOWN** | 〃 반대(종가<EMA100 & EMA20<EMA50) | 숏 추세 플레이북(BRK_S/PBK_S/RCL_S) | 10x |
| **RANGE** | 그 외 | 회귀 플레이북(OSC/BND) 양방향 | 5x |

방향을 상태에 새겨(**TREND_UP ≠ TREND_DOWN**) 롱/숏 플레이북이 정확한 국면에서만
무장된다 — 상승추세에서 숏 셋업이 뜨는 False Positive를 원천 차단. 전환은 새 상태
2연속 1D봉 확인(CRISIS 진입 즉시, 해제 3봉) — 채터링 방지.

#### 2-1. 국면 → 플레이북 매핑

```
TREND_UP    → BRK_L(돌파) · PBK_L(연속 눌림매집) · RCL_L(EMA20 재탈환)     [롱만]
TREND_DOWN  → BRK_S · PBK_S · RCL_S                                        [숏만]
RANGE       → OSC_L/S(RSI 30/70 재돌파) · BND_L/S(밴드 재진입) · BRK(돌파) [양방향]
HIGH_VOL    → OSC_L/S만 (변동성 국면은 이벤트 확인 없이는 진입 안 함)
CRISIS      → 없음 (관망 + 청산)
```

### 3. 진입 전략 — 완전 커버리지 플레이북 매트릭스

**설계 원리 4개 (전부 선험, 교과서 표준값, 최적화 없음):**

1. **두 종류의 진입** — (a) **이벤트 확인형**: 레벨이 아니라 *크로싱*. 과매도라서
   사는 게 아니라 과매도에서 **빠져나오는 순간**(RSI 30 상향 재돌파) 산다. (b)
   **연속 차트-상태형(PBK)**: 크로싱을 놓쳐도 매 봉 현재 차트를 읽어, 추세 중
   가격이 가치 구간(4H EMA20~EMA50)으로 눌리고 모멘텀이 리셋됐을 때 1H 확인봉에
   진입. **고래가 눌림에서 조용히 매집하듯** 이벤트 없이도 타점을 잡는다.
2. **무장(arm) → 격발(trigger) 2단 구조** — 마감된 **4H 봉**에서 셋업(크로싱 또는
   가치 구간 진입)이 성립하면 슬롯이 무장되고, **1H 확인봉**(방향 실체봉이 직전
   1H 고/저 돌파)이 격발한다. 4H가 판단, 1H가 방아쇠.
3. **국면-방향 정합** — 각 슬롯은 자기 국면·방향에서만 무장(§2-1). TREND_UP에서
   숏 슬롯은 아예 안 뜬다(False Positive 원천 차단).
4. **청산은 스타일 종속** — 추세형(BRK/PBK/RCL)은 트레일8+바이어스 플립, 회귀형
   (OSC/BND)은 고정 TP 1.5R + 시간청산 48h.

| 슬롯 | 국면 | 셋업 (무장 조건) | 트리거 종류 | 스타일 | 스케일 |
|---|---|---|---|---|---|
| **BRK_L/S** | TREND·RANGE | 7일 신고/신저가 돌파 (1D·4H·BTC 정렬) | 이벤트 | 스윙 트레일 | L 1.0 / S 0.2 |
| **PBK_L/S** | TREND | 가치구간(EMA20~EMA50) 눌림 + 1H RSI 40~60 리셋 | **연속 상태** | 스윙 트레일 | 0.2 |
| **RCL_L/S** | TREND | 4H 종가가 EMA20 **재탈환/재상실** (1D 동의) | 이벤트 | 스윙 트레일 | 0.2 |
| **OSC_L/S** | RANGE·HIGH_VOL | RSI(14,4H) **30 상향 / 70 하향 재돌파** | 이벤트 | 데이 TP+시간 | 0.2 |
| **BND_L/S** | RANGE | 4H 종가 2σ 밴드 밖→**안 복귀** | 이벤트 | 데이 TP+시간 | 0.2 |

- 회귀형(OSC/BND)은 확정된 1D 추세와 **역행 금지**.
- 손절: 셋업 4H봉/스윙 극단 ∓ 0.5×ATR4H(추세형은 8봉 스윙 ∓0.6×ATR1H), 전역
  2×ATR1H 하한.
- CRISIS: 전 슬롯 무장 해제 + 보유 청산.
- 리스크 스케일(위 표 마지막 열)은 **슬롯이 신호를 낼 확신도의 사전 가중치**일
  뿐 — 고래 모드의 실제 포지션 사이즈는 §5의 풀마진 사이징이 전적으로 결정한다
  (스케일은 여러 후보가 동시에 뜰 때의 교차-코인 확신도 비교에 반영되지 않는다).
- **Upgrade 지배 규칙**: 검증 슬롯(BRK_L) 신호는 같은 코인의 실험 포지션을
  청산·승격한다(역방향 선점 불가) — 실험이 코어를 잠식 못 하게 하는 기계적 방화벽.

#### 3-0. FN/FP 관점의 필터 설계 (E14 — 신호 정밀도)

진입 신호는 "다음 24h가 비용 허들(0.4%) 이상 유리하게 움직이나?"의 이진 분류기다.
각 필터가 **False Positive**(진입했는데 틀림)와 **False Negative**(막았는데
승자였음)를 어떻게 맞바꾸는지 `scripts/fn_fp.py`로 측정한다. BTC 7일 돌파(raw)에
필터를 하나씩 얹은 결과:

| 필터 | 통과분 승률(=1−FP) | 거부분 승률(FN 비용) | 판정 |
|---|---|---|---|
| raw 돌파(무필터) | 0.55 | — | 기준 |
| + 1D 추세 정렬 | 0.56 | 0.54 | 값 있음 |
| + 4H 추세 정렬 | **0.57** | **0.37** | 강함 (거부분이 훨씬 못 이김) |
| + 거래량 ≥1.2× | 0.56 | 0.33 | 강함 |

각 필터가 FP를 낮추면서 거부한 진입(FN)은 실제로 덜 이긴다 → 필터가 값을 한다.
반대로 숏 슬롯(PBK_S FP 0.56, OSC_S FP 0.63)은 FP가 높아 실험 스케일 유지 근거.

#### 3-1. BRK_L 상세 (백테스트 검증이 있는 유일한 슬롯)

다음 **전부** 충족 시 후보 (1H 봉 t 마감 기준):

```
① close(t) > max(high[t-168 .. t-1])          # 직전 7일 신고가 돌파
② 1D bias = up   (EMA20>EMA50 & close>EMA100) # 자산 자체 일봉 추세
③ 4H bias = up   (EMA20>EMA50)                # 중간 TF 정렬
④ BTC 1D 방향 = up                            # 광역 순풍
⑤ volume(t) ≥ 1.2 × median(volume, 48)        # 참여 확인
⑥ 국면 ∈ {TREND, RANGE}
```

**롱 전용인 이유(측정)**: 최근 16개월, 신저가 돌파 숏은 12/12 자산에서 진입 방향과
반대로 +3.1%/72h(숏스퀴즈 지배). 약세장은 관망이 포지션이다.

#### 3-1a. 확신도(conf) 계산 — V5 CV-A 재설계 (E17)

기본(base/turbo/max) 프로파일은 **CV-A(clean confidence)**를 쓴다:

```
conf = clip(c_base, 0, 1)                     # 연속 성분만 — 국면적합·TF정렬 제외
```

과거 버전은 `conf = 0.55×c_base + 0.25×국면적합(c_fit) + 0.20×TF정렬(c_align)
+ 펀딩수정자`였으나, 설계 구간 측정(E17)이 두 가지를 드러냈다: **c_fit 부호
역전**(RANGE 돌파가 TREND 돌파보다 R이 더 좋은데 국면적합 점수는 더 낮게 매김)과
**c_align 마스크 이중계상 포화**(진입 마스크가 이미 정렬을 강제해 c_align이
거의 항상 0.978 근처로 굳음, 분산 0.083). 둘 다 점수에서 **제거**하되, 그
게이트(마스크 조건)는 그대로 유지 — 대신 `c_base`(돌파마진·사전압축·돌파봉
실체·거래량의 평균) **단독**이 conf가 된다. 펀딩수정자도 가산에서 **거부권**으로
바뀐다: 자기 방향으로 크라우딩된 펀딩(`fund_z×dir > funding_z_ext`)이면 가산·
감산이 아니라 **진입 자체를 차단**한다.

`conf ≥ 0.55`만 진입 자격. **주의: conf는 진입 자격 필터 + 코인 선택 랭킹(§4)에
쓰일 뿐, 포지션 사이즈는 결정하지 않는다** — 설계 측정에서 conf가 R을 랭킹하지
못했음(E9 corr 0.005, V5.1 E18 재확인 corr −0.011)을 그대로 반영한 결정.

**고래(whale) 프로파일만 예외**: CV-A를 켜지 않는다(`conf_clean=False`). 이유는
conf가 §5에서 **레버리지 티어**(2/3/5/10x)로 직접 사상되는데, CV-A의 c_base 단독
분포가 이 사상을 무너뜨린다는 측정 결과(CV-A 적용 시 ret 54x→13x, MDD −48%→−60%
— 티어의 손절폭/국면 상관 de-risking이 load-bearing, E17) 때문이다. 고래는 legacy
가중합 conf를 유지하되 `hold_confidence`의 HV-A는 상속한다(아래 §9). 펀딩 가산
제거·거부권화(WF-D)는 whale에서도 시도했으나 사전 등록한 "설계구간 바이트 불변"
게이트를 1건 위반해 **기각**됐다(EXPERIMENTS E18) — whale conf는 지금도 펀딩
가산을 포함하며, 이 라이브 전용 성분은 미해결 누수로 정직 공개돼 있다(EXPERIMENTS.md
"미검증 항목").

### 4. 손절 배치 (E5 측정 반영)

```
SL = min( 돌파선 − 0.6×ATR1H,  close − 2.0×ATR1H )   # 더 먼 쪽
```
1H 노이즈 밴드(≈2×ATR) 안의 손절은 스윕당한다는 측정 결과로 변동성 하한 강제.
진입 후 손절은 타이트닝만 허용(절대 안 넓힘).

### 5. 코인 선택과 확신도 → 레버리지 · 풀마진 사이징

**단 하나의 자리.** 같은 봉에 여러 코인 후보가 뜨면 먼저 **검증상태**
(`risk_scale`: 검증 슬롯 1.0 > 실험 슬롯 0.20, E15)로 정렬하고, 동률이면
**확신도(conf)** 내림차순으로 최고 후보가 자리를 가져간다(교차-코인 선택,
`(risk_scale, conf)` 정렬 키). 이미 자리가 차 있으면 검증 슬롯(BRK_L)만 실험
포지션을 밀어낼 수 있고(Upgrade, §3), 그 외엔 대기한다 — 진입한 자리는 청산까지
홀드된다.

> **tiebreak를 결정봉 hold_confidence로 바꾸는 실험(WF-C)은 측정 후 기각됐다**
> (EXPERIMENTS E18): ret −73%, MDD +7.2pp. conf도 hold_confidence도 R을
> 순위매기지 못하므로(§3-1a) 어느 쪽을 2차 키로 써도 이론상 우열이 없어야
> 하지만, 실측에서 legacy conf tiebreak가 우연히 복리에 유리한 좌석을 골라온
> 것으로 나타났다 — "더 정합적인 척도"가 항상 "더 나은 결과"는 아니라는
> 사례로 남겨둔다(WHALE_WORKFLOW.md §6).

**진입 전 정합 게이트(WF-A, I1)**: 고래(hold_conf_exit>0)에서는 좌석을 배정하기
전에, 그 후보가 지금 열리면 held-side conviction(hold_confidence, 진입 방향
기준)이 이미 `hold_conf_exit`(0.50) 미만인지 미리 확인한다. 미만이면 "몇 봉
안에 SignalFade당할 게 뻔한 진입"이므로 좌석 자체를 스킵한다 — 진입·유지·청산이
하나의 hold_confidence 척도를 공유하게 하는 장치이며, 측정으로 ret +26%가
확인된 유일한 채택 변형이다(EXPERIMENTS E18). 비무장 프로파일(base/turbo/max,
hold_conf_exit=0)에서는 이 게이트가 전혀 발화하지 않는다(바이트 단위 불변,
회귀 테스트로 증명).

확신도는 목표 레버리지 티어로 사상된다:

```
conf ≥ 0.80 → 10x      conf ≥ 0.70 → 5x
conf ≥ 0.62 →  3x      conf ≥ 0.55 → 2x   (0.55 미만은 진입 자격 없음)
```

포트폴리오 방식이 손절폭에서 역산해 고정 리스크를 정하는 것과 달리, 고래 모드는
**전체 자산을 증거금**으로 넣는다:

```
확신도 티어 레버리지 L_conf  (위 표)
최종 레버리지 L = floor_tier( min( L_conf, 국면 캡, 자산 캡, L_liq ) )
   L_liq = 1 / (3×stop_frac + 1.5%)          # 청산 거리 ≥ 3×손절 거리 (불변식)
   floor_tier: 2/3/5/10 표준 계단으로 내림

명목(notional) = equity × L                  # 전체 자산 × 레버리지
증거금(margin) = equity                       # = 명목 / L  (계좌 전부가 증거금)
손절 시 손실   = 명목 × stop_frac = equity × L × stop_frac
```

**핵심 — 청산 안전 불변식은 절대적이다.** L이 `L_liq`로 캡되므로 **손절폭이
넓으면 레버리지가 자동으로 낮은 티어로 내려간다**: 예컨대 확신도가 10x를 원해도
손절폭이 8%면 `L_liq ≈ 3.9` → **3x로 강등**돼 청산가가 손절보다 항상 3배 멀리
앉는다. 엔진은 `size_position`에서 매 진입 시 "손절이 청산보다 먼저"를 assert한다.

**정직한 리스크 경고.** 손절 한 번이 `L×stop_frac`만큼 계좌를 깎는다 — 10x·손절
2%면 **−20%**, 5x·3%면 −15%. `L_liq` 캡 때문에 `L×stop_frac ≤ 1/3`이 구조적
상한이라 단일 손절 최대 손실은 계좌의 ~35% 부근이다. 이 프로파일은 저장소가
다뤄본 방식 중 **최고 파산 위험**이고 페이퍼 전용이다(§11).

### 6. 리스크 가드 (분산 없이 단일 포지션을 지키는 장치)

동시 포지션 상한·상관 경보는 자리가 하나뿐이라 의미가 없어 열어두는 대신, 아래
장치가 계좌를 지킨다:

- **DD 거버너**: 드로다운 −10%부터 **증거금**을 절반으로(`margin_frac`, 레버리지·
  청산 거리는 불변) — 상관 경보 시에도 동일 반감. −20%부터 신규 진입 정지,
  −15%에서 해제.
- **24h 서킷 브레이커**: 24h 실현손실 ≥ 4% equity → 24h 신규 정지.
- **CRISIS 국면**: 신규 금지 + 보유 즉시 청산.
- **쿨다운**: 청산 후 해당 코인 12봉 재진입 금지(RSI<20 지속 구간의 칼날 재포착
  방지).

> **완결성 수정(감사 로그)**: 초기 구현에서 `dd_half`(드로다운 시 사이즈 반감)와
> 상관경보 반감이 **풀마진 사이징에서 무시**되고 있었다(`risk_frac`을 거치는 두
> 컨트롤이 full-margin 경로에서 미적용). 실제 캐시 백테스트에서 진입 8건 중 3건이
> 이 반감을 발동시켰으나 전액 사이징돼 있었다. 수정: 두 컨트롤이 이제 **증거금
> 비율**(`margin_frac`)로 반영돼 드로다운·상관 국면에 노출을 줄인다(레버리지·
> 청산 거리 불변). 결과 MDD −35%→−27%.

### 7. 청산 (측정으로 선택 — 부분익절·시간손절은 승자를 잘라 기각)

1. **초기 SL** (§4)
2. **8×ATR1H 샹들리에 트레일**: 진입 직후부터, `max(최고 종가 − 8×ATR, 현재 SL)`
3. **4H bias 플립**: EMA20<EMA50 2연속 4H봉 → 종가 청산 (추세 종료 판정)
4. **CRISIS 국면** → 즉시 청산
5. **SignalFade**: 매시 재채점에서 **승자**가 모멘텀을 잃거나 확신도가 붕괴하면
   익절 — 상세 §10. `hold_conf_exit=0.50`.
6. **LossFade**: 매시 재채점에서 **패자**의 확신도가 붕괴 상태로 2봉 연속 머물면
   구조적 손절보다 앞서 손실을 줄인다 — 상세 §10. `hold_loss_exit=0.50`.
- 같은 봉에서 SL·상방이 겹치면 **항상 SL 먼저**(보수적 체결 가정).
- 청산 우선순위: CRISIS > SignalFade > LossFade > bias플립 > (시간손절, 이 슬롯
  매트릭스에서는 대부분 비활성).

> **4H bias 플립을 hold_confidence에 종속시키는 실험(WF-B)은 측정 후
> 기각됐다**(EXPERIMENTS E18): 거래당 기대값(exp)은 0.231→0.268로 오르지만
> 총수익 ret이 71.5x→30.6x(−57%)로 붕괴하고 MDD가 +5.8pp 악화됐다. 진단
> 자체는 정당했다 — BiasFlip 45건 중 44건이 hold_confidence≥0.50("논지 아직
> 살아있음")에서 발화하고, 컷 이후 48h 순방향 수익률이 +2.07%로 나쁘지
> 않았다. 하지만 이 이진 청산을 연속 conviction의 확인 신호로 약화시키자
> 오히려 복리를 지키던 조기 청산 다수가 사라져 결과가 나빠졌다 — "이진
> 신호가 노이즈"라는 직관이 실측에서는 반증된 사례. bias 플립은 현행(독립
> 이진 청산)을 유지한다.

### 8. 비용 모델

테이커 0.05%/side + 슬리피지 0.02~0.08%/side(자산군별, 체결가에 반영) + 실측 펀딩
(히스토리 없는 과거 구간은 8h당 0.01% 보수 드래그). 비용 2배 스트레스에서도
설계 구간 총수익 유지가 검증 게이트(G6)다.

### 9. 채점(scoring) 워크플로우 — 두 층위

"채점"은 두 가지를 뜻하며 둘 다 매 실행마다 돈다.

**층위 A — 시간별 포지션 재채점 (관리로 직결).** 매시 리플레이가 끝나면 오픈
포지션을 최신 봉으로 마크투마켓(미실현 R·가격손익%·레버리지손익%·보유시간)하고,
**추가로 실시간 확신도(hold_confidence)를 재계산**한다. 이는 "지금 이 포지션의
근거가 아직 살아있나?"의 연속 점수 [0,1], **방향 대칭**이라 승자·패자 모두에게
같은 잣대를 적용한다:

```
hold_confidence(d) = 0.45×MTF정렬 + 0.30×국면적합(HV-A 연속) + 0.25×4H모멘텀(대칭)
  MTF정렬  : 1D·4H·BTC가 포지션 방향과 몇 개나 일치하나 (0, ⅓, ⅔, 1)
  국면적합 : HV-A(E17 채택) — 계단형(추세=1.0/RANGE=0.5/HIGH_VOL=0.35) 대신
             부호있는 4H EMA20-EMA50 갭을 ATR4H로 정규화한 연속값 [0,1].
             HIGH_VOL·CRISIS는 하드 0 (추세 상태가 아니므로).
             과거 계단형은 국면 라벨이 바뀔 때마다 0.15씩 점프해 히스테리시스
             창 만큼 지연됐다 — 연속값은 regime.py와 같은 추세 정의
             (|EMA20-EMA50|>0.5ATR)를 매끄러운 척도로 재사용해 지연을 없앤다.
  4H모멘텀 : 종가가 4H EMA20에서 우호 방향으로 얼마나(ATR 단위 연속값) +
             RSI(4H)가 우호 방향으로 얼마나(HV-A: 20pt 밖은 0으로 대칭 포화 —
             과거는 비우호 구간 전체가 0으로 뭉개져 RSI 45와 30을 구분 못 했다)
```

두 연속화(HV-A)는 hold_confidence가 계단·래깅이던 구간(전체 성분의 75%)을
매끄럽게 만들어, whale 설계 구간 exp +0.134→**+0.196R**, MDD −47.9%→**−46.5%**,
반쪽 교차 양쪽 개선을 냈다(E17). base는 hold 시스템이 비무장(hold_conf_exit=0)
이라 HV-A가 있어도 영향이 없다 — hold_cont=True가 기본값이지만 base 경로에서는
발화하지 않는다.

이 값은 시간별 채점 리포트(`open_report`, 예: `... hold_conf 0.66`)와 Notion 오픈
행의 Note에 매시 갱신돼 **"현재 확신도 평가"가 눈에 보인다**. 엔진(백테스트=라이브
동일 코드)은 이 재채점으로 **두 가지** 청산을 판단한다:

```
SignalFade (승자만, unreal_r > 0)  — hold_conf_min_r(=1R) 이상 달린 뒤
  ① 모멘텀 상실 : 최고 도달 R에서 hold_giveback(=50%)를 반납  (고점 러너가 꺾임)
  ② 새로운 상황 : hold_confidence < hold_conf_exit(=0.50)가 2봉 연속 (레짐/정렬 반전)

LossFade (패자만, unreal_r < 0) — 구조적 SL이 담당하던 영역의 보완
  국면/모멘텀 명확 붕괴 : hold_confidence < hold_loss_exit(=0.50)가 2봉 연속
  → 풀 손절(최대 ~35%, §5)까지 끌려가지 않고 더 작은 손실로 청산
```

지연 신호인 4H bias 플립보다 **먼저** 작동해 되돌림/추가 하락 전에 결과를 확정하고,
청산 후 자리가 비면 §5의 선택 로직이 다음 최고 확신도 코인으로 넘어간다.

**설계 근거(측정, G5 민감도 포함)**: SignalFade는 캐시 백테스트에서 2회 발동, 평균
+0.80R·승률 100% — 승자를 확정하는 방향으로 작동함을 확인(표본 작아 전방 트랙이
최종 심판). LossFade는 설계 구간에서 `hold_loss_exit` ∈ {0.30, 0.35, 0.40, 0.45,
0.50, 0.55, 0.60}로 민감도를 돌린 결과, **0.40 미만은 바인딩하지 않고(비활성),
0.45~0.60에서는 -0.2~-0.35R로 손실을 조기 확정하면서도 기대값 부호가 전 구간에서
유지**됨을 확인 — 엣지를 파괴하지 않는 무해한 컷으로 판단해 0.50을 채택. 단, 고래
모드 자체에서는 표본이 너무 작아(0회 발동, 설계 구간) 이 메커니즘의 최종 검증은
페이퍼 전방 트랙(층위 B)에 있다.

**층위 B — 전방 트랙 채점 (슬롯·프로파일 심사).** `score_report`가 청산된
트레이드를 setup별·국면별·확신도 티어별·코인별·**청산사유별**로 분해해 기대값(R)을
측정한다. SignalFade/LossFade는 각각 자체 사유 버킷으로 잡혀 "이 청산이 실제로
값을 하나"가 전방 성적으로 심판된다(§과적합 규율).

**Phase 0 진단 (V5.1, I5 전방 심판 인프라).** `score_report`는 위 분해에 이어
세 진단을 더 붙인다(`scoring.py`, 로직에 관여하지 않는 순수 계측):

```
seat_report        : 슬롯별 좌석시간 점유율 vs R 기여율 — 고래는 단일 좌석이라
                      "많은 시간을 차지하면서 R을 못 버는 슬롯"이 다른 슬롯의
                      기회를 잠식하는 §2a 좌석 왜곡의 전방판. 설계 구간 관측:
                      BRK_L 35%좌석·94%R(일꾼) vs PBK_L 17%좌석·−7.9%R(기생).
conf_monotonicity   : conf 분위(quantile)별 평균 R + 상관계수 — conf가 실제로
                      R을 순위매기는지 매 전방 구간마다 재확인(E9/E18 corr≈0
                      재확인이 계속되면 conf 기반 로직 전체를 재검토해야 함).
hold_entry_report   : 진입 시점 hold_confidence 분포 + corr(hold_entry, R) —
                      WF-A 게이트(§5)가 실전에서도 바닥 역할을 하는지, 순위
                      역할까지 넘보지 않는지 확인하는 전방 심판.
```

이 셋은 **판단하지 않는다** — 8주 이상 전방 데이터가 쌓이면 E12 프로토콜
(전방 기대값 ≤0 → 강등/차단, n≥30 & >0 → 승급 심사)을 좌석시간 가중으로
확장할지 별도로 사전 등록해 판단한다. 설계 구간 수치만으로 슬롯을 끄는 것은
사후 선택(과적합)이므로 금지 — 지금은 계측만 존재한다(WHALE_WORKFLOW.md §6
Phase 4, EXPERIMENTS E18).

---

## 검증 상태 (정직 요약 — 상세 VALIDATION.md)

고래 모드는 **단일-포지션·풀마진** 구조라 포트폴리오 방식의 1000경로 몬테카를로
생존 게이트를 아직 통과시키지 않았다(설계 구간 캐시 백테스트, 표본이 작아 신뢰
구간 없음 — 7거래·MDD −27%·최종자산 상승, §6 완결성 수정 반영 후). **그럼에도
고래 모드는 저장소가 다뤄본 방식 중 최고 꼬리·최고 파산 위험**(단일 손절이 계좌의
최대 ~35%, §5)이며 **라이브 금지, 페이퍼 전용**이다. 판정은 백테스트가 아니라
페이퍼 전방 트랙(§9 층위 B)이 setup·청산사유별로 기대값을 쌓아 내린다.

### 운영 (Operations) — 어떻게 도는가

```
스케줄: cron-job.org가 매시 3분 GitHub Actions workflow_dispatch 호출 (CRON_SETUP.md)
        └ 내장 cron은 지연/누락 → 외부 스케줄러 사용
매 실행 (run_live.py, 증분 리플레이 — 롤링 앵커):
  1. OKX 1H 캔들 + 8h 펀딩 증분 수집 (마감봉만, live_data/ CSV)
  2. 상태의 앵커부터 **새 봉만** 리플레이 (직전 실행의 carry=오픈포지션·에쿼티·
     쿨다운·펜딩·서킷/DD를 주입해 이어감) → 오픈/청산 산출
  3. 신규 진입 → Telegram 즉시 알림 + Notion Open 행 생성
  4. 청산 → Telegram 손익 알림 + Notion 행 갱신(승률·R·레버리지 손익·보유시간)
  4b. 오픈 포지션 시간별 재채점: 마크투마켓 + hold_confidence 재계산 → Notion/
      리포트 갱신, 이 재채점으로 SignalFade/LossFade 청산 판단 (§9)
  5. journal/v1_state.json 저장(앵커를 최신봉 다음으로 전진 + carry 직렬화) + git 커밋
멱등성: 앵커가 단조 전진하므로 앵커 이전 봉은 재유도·재알림되지 않는다 → **지운
  과거는 되살아나지 않는다**. 상태 유실/구스키마는 깨끗한 리셋(최신 실시간 봉부터
  새 출발, 과거 재유도 없음). LIVE_RESET=1로 수동 리셋(현재 시점부터 플랫 재시작),
  LIVE_ANCHOR로 특정 봉부터 재시작 지정 가능.
  청크 리플레이가 단일 패스와 동일함은 tests/test_infra.py가 증명(규칙 불변, 저장만 변경).
상태 파일(journal/v1_state.json)이 담는 것: schema, anchor(다음 처리 시작봉),
  equity(마크투마켓), carry(오픈포지션·현금·peak·DD동결·서킷·쿨다운·펜딩 — 전부
  엔진이 직렬화), pages(Notion 페이지ID 맵, 실패 시 다음 실행에서 재시도),
  closed_keys(최근 500건 중복방지 가드).
라이브 전환: TRADE_MODE=live + OKX 3키 + LIVE_CONFIRM + OKX_DEMO 검증 (4중 잠금),
        단 V1 okx_exec는 스켈레톤 — 실주문은 페이퍼 전방 게이트(G8) 통과 후.
채점: 전방 트랙이 setup별·청산사유별 기대값 + Phase 0 진단(좌석시간/R, conf 단조성,
      hold_entry)을 분해 측정 → 실험 슬롯 승급/강등 심사(§9).
```

### 과적합 방지 규율 (이 리포의 헌법)

- 모든 탐색은 설계 구간(2023-06~2026-01)에서만, 반쪽 교차(H1/H2 부호 안정) +
  base 비무장 경로 불변 게이트. 홀드아웃 미개봉, 재실행 시 문서 공개.
- 신규 파라미터는 **사전 등록 후 측정** — 채택/기각 모두 근거와 함께 기록한다.
  실패 실험 전부 기록(EXPERIMENTS.md E1~E18): 신호 4계열·숏 브레이크다운·확신도
  사이징·페이드/레인지·V5.1 워크플로우 정합 4변형 중 3개 — 전부 측정 기각.
  "논리적으로 더 정합적"이라는 이유만으로 채택하지 않는다(WF-B/WF-C가 그 사례).
- 사전 등록한 게이트(예: "설계구간 바이트 불변")를 실측이 어기면, 그 개선의
  다른 장점과 무관하게 **엄격히 기각**한다(WF-D 사례, E18) — 규율이 결과를
  이긴다.
- 새 메커니즘은 "사전 표준값 + 사후 ±20% 섭동 확인"만 허용, 격자 최적화 금지.
- 성패 판정은 백테스트가 아니라 **페이퍼 전방 트랙**(§9 층위 B + Phase 0 진단).

## 실행

```bash
pip install pandas numpy pytest
python3 -m pytest tests/ -q                       # 메커니즘 게이트 (61개, 전부 통과해야 함)
python3 fetch_data.py data                        # OKX 히스토리 백필
python3 run_backtest.py 2023-06-01 2026-01-31     # 백테스트 (base 프로파일 고정, CLI가 profile() 미노출)
python3 scripts/fn_fp.py                          # 필터 FN/FP 분석
TRADE_MODE=paper python3 run_live.py              # 1회 페이퍼 스텝 (PROFILE 기본 whale, run_live.py만 지원)
```

whale/turbo/max로 백테스트를 돌리려면 `run_backtest.py` CLI가 아직 profile을
받지 않으므로, `fabletradebot.backtest.run_backtest(data_dir, profile("whale"),
start=..., end=...)`를 직접 호출해야 한다(§9 층위 A/B 분석 스크립트도 같은 방식).

구조는 `fabletradebot/` 참조: config(전 파라미터·프로파일 단일 출처, base/turbo/
max/whale) · data_okx(OKX 수집·리샘플) · indicators · regime(자산별 5국면) ·
signals(플레이북 매트릭스·conf 계산, CV-A/legacy 분기) · risk(레버리지 티어·
풀마진 사이징) · engine(백테스트=페이퍼 동일 코드, WF-A 게이트·hold_confidence
재채점·청산 우선순위) · backtest(오케스트레이션·지표) · validation(G4~G7 게이트) ·
scoring(층위A/B 리포트 + Phase 0 진단) · notify(Telegram) · journal_notion(Notion
동기화) · okx_exec(4중 잠금 스켈레톤, 실주문 전용) · synthetic(테스트용 합성 데이터).
