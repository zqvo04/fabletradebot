# fabletradebot V3 — 자산별 국면 인식 · 이벤트+연속 진입 · 레버리지 프로파일

**설계 철학**: 과거 데이터 최적화가 아니라 **선험적 완전성** — 모든 시장 국면에
대응하는 교과서-표준 진입 세트를 설계하고, 페이퍼 전방 트랙이 각 플레이북을
심판한다. TF 위계는 고래 관점: **4H가 기본 판단 축**(셋업 포착), **1D는 참고**
(국면·편향), **1H는 정밀 타점과 채점 전용**. 진입은 두 종류: 이벤트 확인형
(크로싱 순간)과 **연속 차트-상태형**(크로싱을 놓쳐도 현재 차트가 매집 구조면 진입).

> **레버리지 목표에 대한 정직한 선언 (반드시 읽을 것)**: 목표는 레버리지를 적극
> 활용한 월 100%다. 진실은 — **월 100%를 "기대값"으로 만들면서 파산 확률을 0
> 근처로 두는 파라미터는 수학적으로 존재하지 않는다**(월샤프 »3 필요, 본 시스템은
> ~1). 존재하는 것은 생존↔꼬리수익을 맞바꾸는 **프로파일 스펙트럼**이고, 각각의
> 파산 확률을 몬테카를로로 명시해 눈 뜨고 고르게 한다(§10). `base`는 생존 게이트
> 통과(최고의 달 +41%), `turbo/max`는 +100%대 달이 나오지만 P(파산성 −50% DD)가
> 58~86%다. 이 숫자를 숨기지 않는 것이 이 시스템의 핵심 가치다.

고확신 셋업을 1H 마감마다 코인별 독립 스캔해 discrete 진입하고, 트레이드가
**스스로 작동을 증명하면**(BTC 한정) 피라미딩+리스크 부스트로 공격적으로 키우며,
손절이 청산보다 항상 먼저 맞도록 파산을 구조적으로 봉쇄하는 시스템.

문서: [BLUEPRINT.md](BLUEPRINT.md)(설계·수식) · [EXPERIMENTS.md](EXPERIMENTS.md)(반증
로그 — 기각 포함 전부) · [VALIDATION.md](VALIDATION.md)(게이트 결과) ·
[SECRETS.md](SECRETS.md) · [CRON_SETUP.md](CRON_SETUP.md)

---

## 트레이딩 로직 — 구체 설명

### 0. 파이프라인 한눈에

```
매 1H 마감(+3분, cron-job.org → GitHub Actions)
 ├─ OKX 1H 캔들+8h 펀딩 증분 수집 (CSV 캐시, 마감봉만)
 ├─ BTC 1D → 광역 국면 분류 (TREND/RANGE/HIGH_VOL/CRISIS + 히스테리시스) [1D=참고]
 ├─ 코인별 독립 스캔: 마감 4H봉의 크로싱 이벤트로 슬롯 무장 [4H=기본]
 │    → 이후 4시간 내 1H 확인봉이 격발 → 확신도(0~1) → 후보 [1H=정밀 타점]
 ├─ 리스크 게이트 (국면·상관·DD거버너·서킷·동시노출) 통과 시
 │    다음 1H 시가에 진입 (리스크 0.55%/유닛, 청산안전 레버리지)
 ├─ 보유 중: 8×ATR 샹들리에 트레일 / 4H bias 플립 청산 / CRISIS 청산
 │    (BTC만) +2R·+4R 도달 시 피라미딩 유닛 추가
 │    매 봉 오픈 포지션 재채점(hold_confidence) → 승자 모멘텀 페이드 청산 [§12]
 └─ Telegram 알림 + Notion 저널 + 상태 커밋 (롤링 앵커 = 새 봉만 증분 처리, §운영)
```

> **운영 모드 두 가지**: 위는 **포트폴리오 모드**(기본 로직, 동시 최대 4포지션 분산).
> `PROFILE=whale`은 **고래 모드(V4)** — 분산 대신 전 유니버스에서 가장 확신도 높은
> **단 하나**의 코인에 **전체 자산**을 확신도별 레버리지(10/5/3/2x)로 몰아넣는다.
> 사이징·청산·채점 관리가 달라지며 상세는 **§11(청산/손절) · §12(채점)**.

### 1. 데이터와 무(無)룩어헤드 규약

- OKX 무기한 선물 1H OHLCV + 8h 펀딩. 4H/1D는 1H에서 리샘플(부분 봉 제외).
- 판단 시각 t에는 **t에 이미 마감된 봉만** 사용. 상위 TF 피처는 "마감 시각 ≤ t"인
  봉만 투영(`closed_asof_1h`). pytest가 데이터 절단 불변성·돌파선 사전성으로 증명.

### 2. 국면 분류 — 자산별 방향 명시형 5국면 (V3)

**왜 자산별인가 (FN 제거)**: V2까지는 BTC 광역 국면 하나로 13개 코인을 몽땅
게이트했다 — BTC가 횡보면 홀로 추세인 알트의 추세 셋업이 통째로 막히는 구조적
**False Negative**. V3는 각 코인을 **자기 일봉으로 독립 분류**하고, 시스템
리스크인 **CRISIS만 BTC에서 전역 상속**한다(위기는 언제나 전역이므로).

각 자산의 1D봉에서 (우선순위 순):

| 상태 | 판정 | 진입 정책 | L 상한 |
|---|---|---|---|
| **CRISIS** | 5D<−12% 또는 1D<−7% 또는 (변동성>90p & 5D<−8%), 또는 BTC가 CRISIS | 신규 금지 + 보유 즉시 청산 | 0 |
| **HIGH_VOL** | 실현변동성(20D) 1년 백분위 > 80 | 이벤트 확인형(OSC)만, 절반 리스크 | 3x |
| **TREND_UP** | \|EMA20−EMA50\|>0.5ATR & 종가>EMA100 & EMA20>EMA50 | 롱 추세 플레이북(BRK_L/PBK_L/RCL_L) | 10x |
| **TREND_DOWN** | 〃 반대(종가<EMA100 & EMA20<EMA50) | 숏 추세 플레이북(BRK_S/PBK_S/RCL_S) | 10x |
| **RANGE** | 그 외 | 회귀 플레이북(OSC/BND) 양방향 | 5x |

방향을 상태에 새겨(**TREND_UP ≠ TREND_DOWN**) 롱/숏 플레이북이 정확한 국면에서만
무장된다 — 상승추세에서 숏 셋업이 뜨는 False Positive를 원천 차단. 전환은 새 상태
2연속 1D봉 확인(CRISIS 진입 즉시, 해제 3봉) — 채터링 방지.

### 2-1. 국면 → 플레이북 매핑 (어느 국면에 무엇을 켜는가)

```
TREND_UP    → BRK_L(돌파) · PBK_L(연속 눌림매집) · RCL_L(EMA20 재탈환)     [롱만]
TREND_DOWN  → BRK_S · PBK_S · RCL_S                                        [숏만]
RANGE       → OSC_L/S(RSI 30/70 재돌파) · BND_L/S(밴드 재진입) · BRK(돌파) [양방향]
HIGH_VOL    → OSC_L/S만 (변동성 국면은 이벤트 확인 없이는 진입 안 함)
CRISIS      → 없음 (관망 + 청산)
```

### 3. 진입 전략 — V3 완전 커버리지 플레이북 매트릭스

**설계 원리 4개 (전부 선험, 교과서 표준값, 최적화 없음):**

1. **두 종류의 진입** — (a) **이벤트 확인형**: 레벨이 아니라 *크로싱*. 과매도라서
   사는 게 아니라 과매도에서 **빠져나오는 순간**(RSI 30 상향 재돌파) 산다. (b)
   **연속 차트-상태형(PBK)**: 크로싱을 놓쳐도 매 봉 현재 차트를 읽어, 추세 중
   가격이 가치 구간(4H EMA20~EMA50)으로 눌리고 모멘텀이 리셋됐을 때 1H 확인봉에
   진입. **고래가 눌림에서 조용히 매집하듯** 이벤트 없이도 타점을 잡는다.
2. **무장(arm) → 격발(trigger) 2단 구조** — 마감된 **4H 봉**에서 셋업(크로싱 또는
   가치 구간 진입)이 성립하면 슬롯이 무장되고, **1H 확인봉**(방향 실체봉이 직전
   1H 고/저 돌파)이 격발한다. 4H가 판단, 1H가 방아쇠.
3. **국면-방향 정합** — 각 슬롯은 자기 국면·방향에서만 무장(§2-1). TREND_UP에서
   숏 슬롯은 아예 안 뜬다(False Positive 원천 차단).
4. **청산은 스타일 종속** — 추세형(BRK/PBK/RCL)은 트레일8+바이어스 플립, 회귀형
   (OSC/BND)은 고정 TP 1.5R + 시간청산 48h.

| 슬롯 | 국면 | 셋업 (무장 조건) | 트리거 종류 | 스타일 | 스케일 |
|---|---|---|---|---|---|
| **BRK_L/S** | TREND·RANGE | 7일 신고/신저가 돌파 (1D·4H·BTC 정렬) | 이벤트 | 스윙 트레일 | L 1.0 / S 0.2 |
| **PBK_L/S** | TREND | 가치구간(EMA20~EMA50) 눌림 + 1H RSI 40~60 리셋 | **연속 상태** | 스윙 트레일 | 0.2 |
| **RCL_L/S** | TREND | 4H 종가가 EMA20 **재탈환/재상실** (1D 동의) | 이벤트 | 스윙 트레일 | 0.2 |
| **OSC_L/S** | RANGE·HIGH_VOL | RSI(14,4H) **30 상향 / 70 하향 재돌파** | 이벤트 | 데이 TP+시간 | 0.2 |
| **BND_L/S** | RANGE | 4H 종가 2σ 밴드 밖→**안 복귀** | 이벤트 | 데이 TP+시간 | 0.2 |

- 회귀형(OSC/BND)은 확정된 1D 추세와 **역행 금지**.
- 손절: 셋업 4H봉/스윙 극단 ∓ 0.5×ATR4H(추세형은 8봉 스윙 ∓0.6×ATR1H), 전역
  2×ATR1H 하한.
- CRISIS: 전 슬롯 무장 해제 + 보유 청산.

**리스크 스케일과 지배 규칙 (실험과 코어의 방화벽):**
- 백테스트 검증 슬롯(BRK_L)만 스케일 1.0. 나머지는 **0.2로 시작해 전방 트랙
  성적으로 사이즈를 번다.** 0.2는 V3 전 슬롯 가동 시 유니버스 MC 생존 게이트
  (95%p MDD ≤30%)를 지키는 최대 스케일(E13).
- **Upgrade 지배 규칙**: 검증 슬롯 신호는 같은 코인의 실험 포지션을 청산·승격
  (역방향 선점 불가). 실험이 코어를 잠식 못 하게 하는 기계적 방화벽.

### 3-0. FN/FP 관점의 필터 설계 (E14 — 신호 정밀도)

진입 신호는 "다음 24h가 비용 허들(0.4%) 이상 유리하게 움직이나?"의 이진 분류기다.
각 필터가 **False Positive**(진입했는데 틀림)와 **False Negative**(막았는데
승자였음)를 어떻게 맞바꾸는지 `scripts/fn_fp.py`로 측정한다. BTC 7일 돌파(raw)에
필터를 하나씩 얹은 결과:

| 필터 | 통과분 승률(=1−FP) | 거부분 승률(FN 비용) | 판정 |
|---|---|---|---|
| raw 돌파(무필터) | 0.55 | — | 기준 |
| + 1D 추세 정렬 | 0.56 | 0.54 | 값 있음 |
| + 4H 추세 정렬 | **0.57** | **0.37** | 강함 (거부분이 훨씬 못 이김) |
| + 거래량 ≥1.2× | 0.56 | 0.33 | 강함 |

각 필터가 FP를 낮추면서 거부한 진입(FN)은 실제로 덜 이긴다 → 필터가 값을 한다.
반대로 숏 슬롯(PBK_S FP 0.56, OSC_S FP 0.63)은 FP가 높아 실험 스케일 유지 근거.

### 3-1. BRK_L 상세 (백테스트 검증이 있는 유일한 슬롯)

다음 **전부** 충족 시 후보 (1H 봉 t 마감 기준):

```
① close(t) > max(high[t-168 .. t-1])          # 직전 7일 신고가 돌파
② 1D bias = up   (EMA20>EMA50 & close>EMA100) # 자산 자체 일봉 추세
③ 4H bias = up   (EMA20>EMA50)                # 중간 TF 정렬
④ BTC 1D 방향 = up                            # 광역 순풍
⑤ volume(t) ≥ 1.2 × median(volume, 48)        # 참여 확인
⑥ 국면 ∈ {TREND, RANGE}
```

**롱 전용인 이유(측정)**: 최근 16개월, 신저가 돌파 숏은 12/12 자산에서 진입 방향과
반대로 +3.1%/72h(숏스퀴즈 지배). 약세장은 관망이 포지션이다.

확신도: `conf = 0.55×base + 0.25×국면적합 + 0.20×TF정렬 + 펀딩수정자`
(base = 돌파마진·사전압축(BB폭 백분위)·돌파봉 실체·거래량의 평균,
펀딩수정자 = 숏 크라우딩(z<−1.5) +0.05 / 롱 크라우딩(z>+1.5) −0.10).
`conf ≥ 0.55`만 진입. **주의: conf는 진입 자격 필터일 뿐 사이징에 쓰지 않는다**
— 설계 측정에서 conf가 R을 랭킹하지 못했음(corr 0.005)을 그대로 반영한 결정.

### 4. 손절 배치 (E5 측정 반영)

```
SL = min( 돌파선 − 0.6×ATR1H,  close − 2.0×ATR1H )   # 더 먼 쪽
```
1H 노이즈 밴드(≈2×ATR) 안의 손절은 스윕당한다는 측정 결과로 변동성 하한 강제.
진입 후 손절은 타이트닝만 허용(절대 안 넓힘).

### 5. 사이징과 청산 봉쇄 (파산 확률 ≈ 0의 핵심)

```
stop_frac = (진입가 − SL) / 진입가
명목      = min( equity × 0.55% / stop_frac ,  equity × L )
증거금    = 명목 / L
L         = min( 티어 5x, 국면 캡, 자산 캡, L_liq )
L_liq     = 1 / (3×stop_frac + 1.5%)      # 청산 거리 ≥ 3×손절 거리 강제
```
- 손절 시 손실은 **항상 equity의 0.55%×(부스트 배수)** 로 상수 — 레버리지는 손익을
  키우는 게 아니라 증거금 효율·청산 거리를 정하는 수단(수학적 사실, BLUEPRINT §1).
- 청산가는 구조적으로 손절보다 3배 멀리. 엔진이 매 봉 이 불변식을 assert.
- r=0.55%는 몬테카를로 생존 게이트(1000경로, 95%p MDD ≤ 30%, P(MDD>50%)=0)를
  공격 레이어 포함 전 유니버스 트레이드로 통과하는 최대치.

### 6. 공격 레이어 — "좋은 기회에 크게" (V1.1, BTC 한정)

**핵심 원칙: 예측이 아니라 증거에 공격한다.** 어떤 트레이드가 대박일지 사전에
고르는 예측자(확신도 티어, 국면 origin 등)는 전부 측정에서 기각됐다(E9b·E10a).
살아남은 공격 방식은 두 가지 기계적 정책:

**① 피라미딩 (승자 증축)** — 포지션이 초기 손절폭 기준 **+2R, +4R**에 도달할
때마다 신규 리스크 유닛(0.55%)을 추가. 각 유닛은 현재 트레일 손절까지의 거리로
사이징되고 자체 청산안전 캡을 통과해야 한다. 최대 2회. 추세가 스스로 증명한
만큼만 노출이 커지고, 전체 포지션은 공유 트레일로 함께 청산된다.

**② 안티마틴게일 부스트 (승기 압박)** — 에쿼티가 고점 대비 2% 이내면 신규 진입
리스크 ×1.5 (0.55%→0.83%). 드로다운 −10%부터는 반감, −20%에서 신규 정지(기존
거버너). 시스템이 잘 맞는 국면에서 자동으로 크게, 어긋나는 국면에서 자동으로 작게.

**BTC 한정인 이유(정직)**: 전 유니버스에 켜면 MC 생존 게이트 붕괴(95%p MDD −53%,
수익도 악화) — 알트 챕에서 피라미딩은 고점 추격이 된다. BTC 단독 백테스트에서만
검증됐으므로(총수익 +19.5%→+33.9%, MDD −16.2%, 트리거·배율 ±20% 섭동 전부 양수
유지) BTC부터 단계 적용. 다른 자산 확대는 전방 트랙 증거가 쌓이면.

### 7. 청산 (측정으로 선택 — 부분익절·시간손절은 승자를 잘라 기각)

1. **초기 SL** (§4)
2. **8×ATR1H 샹들리에 트레일**: 진입 직후부터, `max(최고 종가 − 8×ATR, 현재 SL)`
3. **4H bias 플립**: EMA20<EMA50 2연속 4H봉 → 종가 청산 (추세 종료 판정)
4. **CRISIS 국면** → 즉시 청산
5. **SignalFade** (고래 모드 전용, V4): 매시 재채점에서 **승자**가 모멘텀을 잃거나
   확신도가 붕괴하면 익절 — 상세 §12. `hold_conf_exit=0`(기본)이면 비활성.
- 같은 봉에서 SL·상방이 겹치면 **항상 SL 먼저**(보수적 체결 가정).

### 8. 포트폴리오 가드

동시 ≤4 포지션(상관 경보 시 ≤2) · 코인당 1개 · 총 오픈리스크 ≤1.8% · 총 증거금
≤60% · 24h 실현손실 ≥4% → 24h 서킷 · 청산 후 12봉 쿨다운 · 유니버스 평균 상관
> 0.8 → 신규 반감.

**고래 모드(V4)는 이 분산 가드를 대체한다** — 동시 1포지션이 곧 전 계좌이므로
오픈리스크·증거금 상한은 의미가 없어 열어두고, 대신 §11의 확신도-레버리지·풀마진·
청산 봉쇄와 §12의 모멘텀 페이드가 리스크를 관리한다. DD 거버너·24h 서킷·CRISIS
정지·쿨다운은 그대로 유지(단일 포지션에도 유효한 안전 바닥).

### 9. 비용 모델

테이커 0.05%/side + 슬리피지 0.02~0.08%/side(자산군별, 체결가에 반영) + 실측 펀딩
(히스토리 없는 과거 구간은 8h당 0.01% 보수 드래그). 비용 2배 스트레스에서도
설계 구간 총수익 +74% 유지(G6).

### 10. 레버리지 프로파일 — 월 100% 목표의 정직한 스펙트럼

`PROFILE` 환경변수로 선택(기본 base). **프로파일은 사이징/공격 노브만 바꾸고
청산-안전 불변식(손절이 항상 청산보다 먼저)은 어느 프로파일에서도 불변.**

> **버그 발견·수정 공개 (정직 원칙)**: 이번 whale 작업 점검 중, `peak`(에쿼티
> 고점) 변수가 초기화 후 **단 한 번도 갱신되지 않는** 사전 존재 버그를 발견해
> 수정했다(`fabletradebot/engine.py`, 회귀 테스트
> `test_equity_peak_tracks_true_high_not_initial_capital` 고정). 영향: 계좌가
> 한 번이라도 초기자본을 넘으면 드로다운이 항상 "음수"로 계산돼 **DD 거버너
> (`dd_stop`/`dd_half`)와 안티마틴게일 부스트(`eq_boost_dd`)가 전 프로파일에서
> 사실상 무력화**돼 있었다 — 진짜 고점 대비 폭락도 걸러내지 못했다. 이는 §5가
> "파산 확률 ≈0의 핵심"이라 부르는 바로 그 메커니즘이라 **아래 base/turbo/max
> 수치(MC 1000경로 기반)는 이 버그가 있던 상태로 측정된 것 — 재검증이 필요
> 하다**(`python3 -m pytest` 및 `validation.py` MC 게이트 재실행 권장). whale
> 수치는 이 세션에서 직접 만든 것이라 **수정 후 값으로 즉시 갱신**했다(아래 각주).

| 프로파일 | 리스크/트레이드 | 공격 범위 | 월 기하(설계) | **최고의 달** | 95%p MDD | **P(−50% DD)** |
|---|---|---|---|---|---|---|
| **base** (기본) | 0.5% | BTC만 | +2.6%‡ | +41%‡ | −28%‡ | **0%**‡ ✅ |
| turbo | 1.0% | 전 자산 | +2.8%‡ | **+113%**‡ | −76%‡ | **58%**‡ ⚠️ |
| max | 1.5% | 전 자산, 피라미드 3 | +4.7%‡ | **+128%**‡ | −87%‡ | **86%**‡ ☠️ |
| **whale** (V4) | 가변(풀마진) | 단일 집중 | 미측정† | 미측정† | −35%† | 미측정† ☠️ |

‡ **peak 버그 수정 전 측정치 — 재검증 전까지 참고용.** 같은 버그 수정 후 캐시
유니버스 스팟체크로는 base가 오히려 결과가 크게 개선됐다(거버너가 실제로 작동하기
시작해서) — 정식 MC 게이트 재실행 전에는 이 표의 숫자를 최종으로 신뢰하지 말 것.

† whale은 단일-포지션·풀마진이라 1000경로 MC 생존 게이트를 아직 통과시키지 않았다
(캐시 유니버스 1회 백테스트, peak 버그 수정 후: 7거래·MDD −35%·최종자산
+170% — 표본이 작아 신뢰 구간 없음). 버그 수정 전에는 거버너가 무력화된 채
MDD −81~90%가 나왔던 것도 이 버그 때문. **그럼에도 whale은 여전히 모든 프로파일
중 최고 꼬리·최고 파산 위험**(단일 손절이 계좌의 최대 ~35%, §11)이며 페이퍼 전용.
구조·산식은 **§11**, 관리(모멘텀 페이드)는 **§12**.

**해석**: turbo/max는 강추세 달에 **실제로 월 +100%를 넘긴다** — 그게 레버리지의
힘이다. 그러나 같은 레버리지가 파산성 드로다운(−50%+) 확률을 58~86%로 만든다.
즉 월 100%는 "가끔 일어나는 사건"으로는 가능하지만 "매달의 기대값"으로는 계좌를
반드시 0으로 보낸다. **base로 살아남으며 복리하고, 강추세라는 확신이 설 때만
수동으로 turbo를 켜는 것**이 이 스펙트럼의 정직한 사용법이다(라이브는 여전히 G8
게이트 뒤에 잠겨 있음). max는 페이퍼 전용 가드레일.

```bash
PROFILE=base  python3 run_live.py    # 생존 (기본)
PROFILE=turbo python3 run_live.py    # 공격 — 파산 확률 58% 인지하고
PROFILE=whale python3 run_live.py    # 고래 — 단일 집중·풀마진 (§11·§12, 최고 위험)
```

### 11. 고래 모드 (V4) — 단일 포지션 전액 집중 + 확신도 레버리지

`PROFILE=whale`은 포트폴리오 **분산을 버리고** 크립토 고래처럼 **집중**한다: 매 봉
전 유니버스 후보 중 **가장 확신도 높은 하나**가 유일한 자리를 차지하고, **전체
자산**을 그 확신도에 맞는 레버리지로 몰아넣는다. 진입한 자리는 **청산까지 홀드**
(다른 코인 신호가 직접 밀어내지 않음) — 대신 §12의 모멘텀 페이드로 익절하면 자리가
비고 다음 최고 확신도 코인으로 넘어간다.

#### 11-1. 코인 선택과 확신도 → 레버리지 티어

한 자리뿐이므로 같은 봉에 여러 코인 후보가 뜨면 **확신도 내림차순**으로 정렬해
최고 확신도 후보가 먼저 자리를 가져간다(교차-코인 선택). 확신도는 목표 레버리지
티어로 사상된다:

```
conf ≥ 0.80 → 10x      conf ≥ 0.70 → 5x
conf ≥ 0.62 →  3x      conf ≥ 0.55 → 2x   (0.55 미만은 진입 자격 없음)
```

#### 11-2. 풀마진 사이징과 청산 봉쇄 (새 청산/손절 로직)

base 모드가 손절폭에서 역산해 **리스크 0.55%**를 고정하는 것과 달리, 고래 모드는
**전체 자산을 증거금**으로 넣는다:

```
확신도 티어 레버리지 L_conf  (위 표)
최종 레버리지 L = floor_tier( min( L_conf, 국면 캡, 자산 캡, L_liq ) )
   L_liq = 1 / (3×stop_frac + 1.5%)          # 청산 거리 ≥ 3×손절 거리 (불변식)
   floor_tier: 2/3/5/10 표준 계단으로 내림

명목(notional) = equity × L                  # 전체 자산 × 레버리지
증거금(margin) = equity                       # = 명목 / L  (계좌 전부가 증거금)
손절 시 손실   = 명목 × stop_frac = equity × L × stop_frac
```

**핵심 — 청산 안전 불변식은 그대로다.** L이 `L_liq`로 캡되므로 **손절폭이 넓으면
레버리지가 자동으로 낮은 티어로 내려간다**: 예컨대 확신도가 10x를 원해도 손절폭이
8%면 `L_liq ≈ 3.9` → **3x로 강등**돼 청산가가 손절보다 항상 3배 멀리 앉는다. 엔진은
`size_position`에서 매 진입 시 "손절이 청산보다 먼저"를 assert한다(base와 동일 코드).

**정직한 리스크 경고.** 고정 0.55%가 아니라 **손절 한 번이 `L×stop_frac`**만큼
계좌를 깎는다 — 10x·손절 2%면 **−20%**, 5x·3%면 −15%. `L_liq` 캡 때문에
`L×stop_frac ≤ 1/3`이 구조적 상한이라 단일 손절 최대 손실은 계좌의 ~35% 부근이다.
그래서 이 프로파일은 **모든 프로파일 중 최고 파산 위험**(§10 표)이고 페이퍼 전용이다.

| | base 모드 | 고래 모드(V4) |
|---|---|---|
| 동시 포지션 | ≤4 분산 | **1 (전액 집중)** |
| 레버리지 | 단일 5x 티어 | **확신도별 10/5/3/2x** |
| 사이징 | 리스크 0.55% 고정 | **풀마진 (증거금=전 자산)** |
| 손절 시 손실 | 계좌 0.55%×부스트 | **L×손절폭 (가변, 최대 ~35%)** |
| 청산 봉쇄 | L_liq 캡 (불변) | **L_liq 캡 (불변, 동일)** |
| DD 거버너 | −10% DD 리스크 반감 | **−10% DD 증거금 반감**(전 계좌×0.5), 상관경보도 반감 |
| 관리 청산 | 트레일·bias플립 | **+ SignalFade 모멘텀 페이드(§12)** |

> **완결성 수정(감사 로그)**: 초기 whale 구현에서 `dd_half`(드로다운 시 사이즈
> 반감)와 상관경보 반감이 **풀마진 사이징에서 무시**되고 있었다(`risk_frac`을
> 거치는 두 컨트롤이 full-margin 경로에서 미적용). 실제 캐시 백테스트에서 진입
> 8건 중 3건이 이 반감을 발동시켰으나 전액 사이징돼 있었다. 수정: 두 컨트롤이
> 이제 **증거금 비율**(`margin_frac`)로 반영돼 whale에서도 드로다운·상관 국면에
> 노출을 줄인다(레버리지·청산 거리 불변). 결과 whale MDD −35%→−27%.

### 12. 채점(scoring) 워크플로우 — 두 층위

"채점"은 두 가지를 뜻하며 둘 다 매 실행마다 돈다.

**층위 A — 시간별 포지션 재채점 (관리로 직결).** 매시 리플레이가 끝나면 오픈
포지션을 최신 봉으로 마크투마켓(미실현 R·가격손익%·레버리지손익%·보유시간)하고,
**추가로 실시간 확신도(hold_confidence)를 재계산**한다. 이는 "지금 이 포지션의
근거가 아직 살아있나?"의 연속 점수 [0,1]:

```
hold_confidence(d) = 0.45×MTF정렬 + 0.30×국면적합 + 0.25×4H모멘텀
  MTF정렬  : 1D·4H·BTC가 포지션 방향과 몇 개나 일치하나 (0, ⅓, ⅔, 1)
  국면적합 : 추세(방향일치)=1.0, RANGE=0.5, HIGH_VOL=0.35, 반대추세/CRISIS=0
  4H모멘텀 : 종가가 4H EMA20의 우호 방향 + RSI(4H)가 우호 방향
```

이 값은 시간별 채점 리포트(`open_report`, 예: `... hold_conf 0.66`)와 Notion 오픈
행의 Note에 매시 갱신돼 **"현재 확신도 평가"가 눈에 보인다**. 엔진(백테스트=라이브
동일 코드)은 이 재채점으로 **SignalFade** 청산을 판단한다 — **승자만 익절**한다
(손실 포지션은 손절이 담당). 포지션이 `hold_conf_min_r`(=1R) 이상 달린 뒤 다음
**둘 중 하나**면 종가 익절:

```
① 모멘텀 상실 : 최고 도달 R에서 hold_giveback(=50%)를 반납  (고점 러너가 꺾임)
② 새로운 상황 : hold_confidence < hold_conf_exit(=0.50)가 2봉 연속 (레짐/정렬 반전)
```

지연 신호인 4H bias 플립보다 **먼저** 작동해 되돌림 전에 이익을 확정하고, 청산 후
자리가 비면 §11의 선택 로직이 다음 최고 확신도 코인을 포착/전환한다. 왜 이 설계인가
(측정): 확신도-정렬 기반 건강도는 수익 중일 때 높게 유지돼 "붕괴만"으로는 캐시
백테스트에서 **0회** 발동했다 — 그래서 "수익 중 모멘텀 상실"을 실제로 잡는
**peak-R 반납**을 핵심으로 삼았다. 파라미터는 표본 과최적화 없이 선험값(+1R 후
절반 반납)으로 고정. `hold_conf_exit=0`(base 기본)이면 전 기능 비활성.

검증(캐시 유니버스, §10 상단의 peak 버그 수정 후 재측정): DD 거버너가 정상 작동한
뒤로는 전체 거래 수 자체가 크게 줄었다(거버너가 실제로 신규 진입을 막기 시작해서,
35거래→7거래). 그 안에서 SignalFade는 2회 발동, 평균 **+0.80R·승률 100%** —
여전히 승자를 확정하는 방향으로 작동함을 확인. 표본이 매우 작아(n=2) 이 수치를
전방 검증 없이 기본값 근거로 삼지 않는다 — §과적합 방지 규율대로 전방 트랙
(층위 B)이 최종 심판.

**층위 B — 전방 트랙 채점 (슬롯·프로파일 심사).** `score_report`가 청산된
트레이드를 setup별·국면별·확신도 티어별·코인별·**청산사유별**로 분해해 기대값(R)을
측정한다. SignalFade는 이제 자체 사유 버킷으로 잡혀 "모멘텀 페이드가 실제로 값을
하나"가 전방 성적으로 심판된다(§검증·과적합 규율).

---

## 검증 상태 (정직 요약 — 상세 VALIDATION.md)

| 항목 (base 프로파일) | 결과 |
|---|---|
| **V3 전 슬롯, 설계 구간 유니버스** | +125%, 월 기하 +2.6%, MDD −22%, 샤프 0.95 |
| V3 전 슬롯, BTC 단독 | +22% (실험 슬롯 드래그 포함) |
| MC 생존 — V3 전 슬롯, 유니버스 (1000경로) | 95%p MDD −28%, P(MDD>50%)=0 ✅ |
| 비용 2배 (유니버스) | +74% 유지 ✅ |
| 워크포워드 (유니버스, 2개월 슬라이스) | **9/15 양수** (V2 코어 5/15에서 개선) ⚠️ |
| **홀드아웃(26-02~07, 유니버스)** | **−0.14R (n=255) — 기대값 미증명** ❌, 생존 확인(−0.7%, MDD −8%) |
| 홀드아웃 활동량 | n=255 (V1 n=17 대비) — 자산별 국면으로 FN 대폭 감소 ✅ |
| **판정** | **라이브 금지. 페이퍼 전방 트랙이 슬롯·프로파일별로 심판** |

> 홀드아웃 재개봉 공개: V3는 신호군을 실질 변경(자산별 국면·PBK·프로파일)했으므로
> 홀드아웃을 재실행했다. 결과는 위와 같이 정직 기록. 이는 VALIDATION.md의 "재조회
> 시 공개" 규율에 따른 것이며, 파라미터를 홀드아웃에 맞춰 조정하지 않았다.

### 운영 (Operations) — 어떻게 도는가

```
스케줄: cron-job.org가 매시 3분 GitHub Actions workflow_dispatch 호출 (CRON_SETUP.md)
        └ 내장 cron은 지연/누락 → 외부 스케줄러 사용
매 실행 (run_live.py, 증분 리플레이 — 롤링 앵커):
  1. OKX 1H 캔들 + 8h 펀딩 증분 수집 (마감봉만, live_data/ CSV)
  2. 상태의 앵커부터 **새 봉만** 리플레이 (직전 실행의 carry=오픈포지션·에쿼티·
     쿨다운·펜딩·서킷/DD를 주입해 이어감) → 오픈/청산 산출
  3. 신규 진입 → Telegram 즉시 알림 + Notion Open 행 생성
  4. 청산 → Telegram 손익 알림 + Notion 행 갱신(승률·R·레버리지 손익·보유시간)
  4b. 오픈 포지션 시간별 재채점: 마크투마켓 + hold_confidence 재계산 → Notion/
      리포트 갱신, 고래 모드는 이 재채점으로 SignalFade 익절 판단 (§12)
  5. journal/v1_state.json 저장(앵커를 최신봉 다음으로 전진 + carry 직렬화) + git 커밋
멱등성: 앵커가 단조 전진하므로 앵커 이전 봉은 재유도·재알림되지 않는다 → **지운
  과거는 되살아나지 않는다**. 상태 유실/구스키마는 깨끗한 리셋(최신 실시간 봉부터
  새 출발, 과거 재유도 없음). LIVE_RESET=1로 수동 리셋(현재 시점부터 플랫 재시작).
  청크 리플레이가 단일 패스와 동일함은 tests/test_infra.py가 증명(규칙 불변, 저장만 변경).
프로파일: PROFILE=base|turbo|max|whale (run_live 기본 whale). §10·§11 참조.
라이브 전환: TRADE_MODE=live + OKX 3키 + LIVE_CONFIRM + OKX_DEMO 검증 (4중 잠금),
        단 V1 okx_exec는 스켈레톤 — 실주문은 페이퍼 전방 게이트(G8) 통과 후.
채점: 전방 트랙이 setup별·프로파일별 기대값을 분해 측정 → 실험 슬롯 승급/강등 심사.
```

### 과적합 방지 규율 (이 리포의 헌법)

- 모든 탐색은 설계 구간에서만. 홀드아웃 재실행 시 문서 공개(위 참조).
- 실패 실험 전부 기록(EXPERIMENTS.md E1~E14): 신호 4계열·숏 브레이크다운·확신도
  사이징·페이드/레인지·유니버스 공격 — 전부 측정 기각.
- 새 메커니즘은 "사전 표준값 + 사후 ±20% 섭동 확인"만 허용, 격자 최적화 금지.
- 성패 판정은 백테스트가 아니라 **페이퍼 전방 트랙**.

## 실행

```bash
pip install pandas numpy pytest
python3 -m pytest tests/ -q                       # 메커니즘 게이트 (43 tests)
python3 fetch_data.py data                        # OKX 히스토리 백필
python3 run_backtest.py 2023-06-01 2026-01-31     # 백테스트
python3 scripts/fn_fp.py                          # 필터 FN/FP 분석
PROFILE=base TRADE_MODE=paper python3 run_live.py # 1회 페이퍼 스텝
```

구조는 `fabletradebot/` 참조: config(전 파라미터·프로파일 단일 출처) · data_okx ·
indicators · regime(자산별 5국면) · signals(플레이북 매트릭스) · risk · engine
(백테스트=페이퍼 동일 코드) · backtest · validation · scoring · notify(Telegram) ·
journal_notion · okx_exec(4중 잠금 스켈레톤).
