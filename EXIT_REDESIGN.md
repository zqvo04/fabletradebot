# EXIT_REDESIGN — 횡보 정체 포지션 청산 개선 설계 (V6 후보, 사전 등록)

> 상태: **X-A 측정 완료 — 기각.** 설계 구간 whale 리플레이(2023-06~2026-01,
> 반쪽 교차, `scripts/exp_stall.py`)에서 stall_bars {18,24,36} × stall_trail
> {2.5,3,4} 9셀 전부 터미널 부 붕괴(baseline 67.5x → 0.8~12.1x, 최고셀도 −82%),
> MDD 8/9셀 악화(−56~−76% vs −48.9%), 격자 비단조(ret 요동 = G5 경로 노이즈).
> 근본 원인: X-A의 러너-안전 논리("러너는 best_close 갱신 → 안 조여짐")가 실증
> 반증됨 — 트레이드 수 364→445~551(+22~51%)은 조임이 추세 중간 눌림에서 대량
> 발화했다는 직접 증거. E9/E16의 "승자 조기 절단 → 복리 붕괴"가 정체 조건부로
> 위장돼 재현. base 불변, config 미변경(스위치 off 유지), pytest 69/69.
>
> **X-C 측정 완료 — 기각(사문).** X-A 실패로 §2 규율대로 활성. giveback 레그를
> `mom≤0` 동안만 재무장(giveback {0.4,0.5,0.6} × min_r {0.7,1.0}) → 6셀 **전부
> baseline 바이트 동일**(n=364 불변). 진단으로 원인 규명: 모멘텀 게이트 없는 순수
> 재무장은 발화하며 E16을 재현(gb=0.5 → 3.33x, gb=0.3 → −50%, n→535~620)하지만,
> `mom≤0` 게이트가 그 발화를 **전부 제거**. 즉 whale 승자가 peak의 40~60%를
> 반납하는 순간마다 4H 모멘텀은 아직 >0 — giveback 이벤트와 모멘텀 소진이 시간적
> 으로 겹치지 않아 모멘텀 조건화로는 축을 구조 못 함. SR-B(rot=0)와 동형의 사문
> 기각. 단순성 규율상 코드 미탑재(engine/config 바이트 불변), 진단 수치는 본 문서.
>
> **X-D(funding 캐리 청산축, 세션 신규 제안) — 설계 단계 기각(측정 불가).** funding
> 히스토리가 2026-04-07~(n=286, ~3개월)만 존재 → 설계 구간(2023-06~2026-01)에
> funding 데이터 0건이라 사전등록·측정 자체가 불가. 전방 3개월로 측정하면 홀드아웃
> 오염(I5 위반). 규모도 abs~0.4~0.8bps/8h → 5일 보유 ~0.3~0.6% equity로 트레이드
> R(±13~26%)보다 한 자릿수↓. 데이터·규모 양측에서 주 청산 트리거 부적격.
>
> **X-E(regime-decay 청산축) — 설계 단계 기각(사문).** "진입 논지 regime이 죽으면
> 청산" 아이디어. 동기 사례 반증: AVAX PBK_S 5일 횡보 내내 일봉 regime이 TREND_DOWN
> **유지**(2026-07-08~19 전 구간). 죽은 횡보가 일봉 regime을 이탈시키지 못하므로
> AVAX엔 미발화. 설계구간 측정 없이 동기 사례에서 이미 사문.
>
> **X-G(손익분기 래칫) — 측정 완료·기각.** "일찍 빼기"가 아니라 "peak≥임계 후
> 트레일 SL이 진입가 밑으로 후퇴 금지(트레일은 안 조임)". 논지: full round-trip은
> 러너와 양립 불가 → 러너 안전. **측정이 반증:** be_ratchet {0.5,1.0,1.5}R →
> ret 67.5x→14.8/25.4/44.2x, MDD 전부 악화(−51~−60%), n 364→386~523. 임계↑=덜
> 발화=baseline 수렴뿐, 도움 세팅 없음. 원인: **크립토 러너는 진입가까지 깊게
> 되돌린 뒤 재점화한다** — 8 ATR 트레일 폭이 넓은 게 바로 이걸 견디려는 의도적
> 설계였고, BE 바닥은 그 깊은-되돌림 러너를 절단(+159 재진입이 증거). 조이든(X-A)
> 바닥을 깖든(X-G) 유효 스탑을 좁히는 모든 것은 복리를 파괴. (설계자 과신을 사전등록
> 게이트가 잡은 사례.) engine/config 바이트 불변, pytest 69/69.
>
> **X-R(trend staleness → RANGE 강등, 2026-07-21 사용자 차트 관찰 계기) — 측정
> 완료, G5 미통과. 오너 오버라이드로 whale에 잠시 무장했다가 이후 철회(2026-07-21,
> 커밋 `78c1274`): 유니버스가 13→9 메이저로 재타겟되며 AVAX 등 스테일-박스 상습
> 심볼이 유니버스에서 제외됐고, X-R 배선(`trend_stale_days`)·러너-안전 테스트도
> 함께 제거됐다. 현재 코드에 trend_stale 경로 없음, pytest 69/69. 아래 블록은
> 측정 기록으로 보존한다.** 관찰: AVAX 4H가 한 달 박스인데 일봉 국면이
> TREND_DOWN 유지. 기계 원인 확증: 박스에서 EMA20-50 갭(−0.95→−0.40)과
> ATR(0.44→0.31)이 **함께 수축** → strength 비율이 1.3에 고착, 문턱 0.5를 영원히
> 상회. 마지막 신규 20D 종가 저점은 06-19 — **진입일 07-15에 age=26**. "신극값
> 없는 추세는 박스" 판정(20D Donchian, K일)을 raw state에 추가하면 PBK_S 진입
> 자체가 차단됨(동기 사례를 유일하게 해결하는 축).
> - **양방향 K {10,15,20}**: 67.5x → 7.8/12.2/66.0x. K≤15는 상승추세 휴지기의
>   PBK_L 연속매수(복리 원천)를 차단해 파괴적. **양방향 기각 확정.**
> - **down-only(E6 비대칭: 숏 12/12 무엣지 → 스테일 하락추세만 강등) K {10,15,20}**:
>   38.9/59.7/61.7x — 단조 회복, K=20에서 MDD 동등(−49.5 vs −48.9), avg_r 동등.
>   −9%는 H1 집중·H2는 개선(+9%)의 반쪽 요동이고, 차단 대상이 0.20 스케일 실험
>   숏(직접 PnL 미미)이라 대부분 경로 재배열 노이즈로 판정. 그래도 **터미널 부
>   전 셀 하회 = G5 엄격 기준 미통과.**
> - 판정: **G5 채택 아님 — 오너 오버라이드로 whale 무장(2026-07-21).** 9개 축 중
>   유일하게 (a) 동기 사례를 진입 단계에서 해결하고 (b) 기계적으로 확증된 분류기
>   결함(수축 박스에 비율 눈멂)의 정의적 수정이며 (c) 인샘플 비용이 바운디드·경로
>   노이즈성(K=20). 오너 우선순위("좌석 시간이 가장 희소한 엣지")에 근거해 whale
>   프로파일에 `trend_stale_days=20` 탑재. **base·portfolio 불변**(default 0, 가드
>   `if p.trend_stale_days>0`가 신규 경로 스킵), 러너-안전 테스트 2건, pytest 71/71.
>   라이브 AVAX는 07-11부터 RANGE → 스테일 숏 진입 차단(보유 포지션 무접촉).
>   **되돌림 규칙:** 전방 whale rolling-30 기대값 또는 터미널 부 기울기가 무장 전
>   전방 baseline 대비 30트레이드+ 악화 시 `trend_stale_days=0` 복귀(차단 대상이
>   0.20 숏이라 실제 악화는 귀속 가능, SR-D식 전방 심판).
>
> **최종 종합 (6개 독립 방향 측정 후):** 정체 좌석 문제에 대한 청산·regime·좌석
> 방향의 6개 변형(X-A·순수giveback·X-C·X-D·X-E·X-G)이 전부 기각. 하나의 벽으로
> 수렴: **봉 단위에서 죽은 횡보는 러너의 (깊은) 휴식과 모든 신호(가격·모멘텀·regime·
> vol·구조)에서 동일**하고, 좌석을 비워 넣을 한계 트레이드는 ≈0/−EV(E19b, conf⊥R).
> 정체의 기회비용은 대부분 신기루이고, 그걸 수확하려는 모든 조작이 엣지인 tail-
> capture(넓은 트레일이 깊은 되돌림 러너를 살려두는 것)를 훼손한다. **현행 청산
> 세트가 측정으로 확인된 프론티어.** 남은 진짜 레버는 청산축이 아니라 (a) SR-D로
> 더 많은 슬롯을 전방 검증(진행 중), (b) E19b가 "좌석은 병목 아님"을 보인 만큼
> 유니버스/진입 커버리지 재검토(별도 측정 주제), (c) G8 전방 회복(라이브 최우선).

작성 계기: 2026-07-15 진입한 AVAX PBK_S(whale)가 +0.97R 피크(6.447 저점 부근)를
찍고도 은행하지 못한 채 74+시간 횡보 속에 좌석을 점유. whale은 단일 좌석이므로
정체 포지션의 기회비용이 포트폴리오 모드와 비교 불가하게 크다.

원칙: 여기 있는 것은 **설계와 측정 계획**이지 채택이 아니다. 모든 후보는 설계
구간 whale 백테스트에서 G5 게이트(반쪽 부호 일관성)를 통과해야만 config에 들어간다.
E9/E16이 이미 기각한 형태(무조건 부분 TP, 무조건 giveback)는 재도입하지 않는다.

## 1. 진단 — 현행 청산 세트에 "정체(stagnation)" 축이 없다

현행 트렌드 슬롯(BRK/PBK/RCL) 청산 경로 전수 (engine.py §manage):

| 경로 | 발동 조건 | AVAX 사례에서 왜 안 걸렸나 |
|---|---|---|
| SL/Trail | 가격이 스탑 도달 | 트레일 8 ATR 샹들리에 — best_close 6.447 기준 트레일이 초기 스탑 근처(≈6.75+). 횡보에서는 영원히 안 조여짐 |
| TP | tp1 > 0 | 트렌드 슬롯은 tp_r=0 (E9: 부분 TP가 승자를 자름 — 근거 있는 선택) |
| SignalFade (수익 은행) | ① giveback 레그: peak≥1R 후 반납 — **E16에서 비활성화(hold_giveback=1.0)** ② conviction-collapse: hold_conf<0.50 ×2봉 | ①은 꺼져 있고 peak 0.97R로 arming(1.0)에도 미달. ②는 hold_conf 0.77 — 완만한 TREND_DOWN 정렬이 유지되는 횡보에서는 절대 안 떨어짐 |
| LossFade | 손실 중 + conviction<0.50 ×2봉 | 수익 중이라 해당 없음 |
| BiasFlip | 4H 바이어스 역전 ×2봉 | 횡보는 바이어스를 뒤집지 않음 (래깅) |
| Timeout | time_stop_bars>0 | 트렌드 슬롯은 0 (E9) |
| Regime | CRISIS | 해당 없음 |

구조적 결론: 모든 청산이 **가격 역행**(SL/Trail/BiasFlip) 아니면 **논지 붕괴**
(Fade류)에만 반응한다. "가격도 논지도 무너지지 않았지만 더 가지도 않는" 횡보
정체에 반응하는 축이 하나도 없다. 이것이 사용자 관찰("최저점에서 익절 못하고
너무 오래 끌고 감")의 정확한 메커니즘이며, 파라미터 미세조정이 아니라 축 하나가
빠진 문제다.

주의(정직성): E9·E16의 기각은 유효하다 — 시간컷/giveback을 **무조건** 걸면 추세
승자를 자른다. 하지만 두 측정 모두 "정체 조건부" 변형은 측정한 적이 없고, E16은
whale에서 giveback을 껐을 뿐 정체 축의 대체물을 넣지 않았다. 아래 후보들은 전부
"러너는 건드리지 않음"을 구조적으로 보장하는 조건부 형태다.

## 2. 사전 등록 후보 (각각 독립 측정, a-priori 파라미터)

### X-A — 정체 조임 트레일 (stall-tightened chandelier) · **기각(측정 열위, 상단 요약)**
best_close 갱신이 `stall_bars`(24 = 1일) 동안 없고 peak_r ≥ 0.5 이면, 그 포지션의
트레일 폭을 8 → `stall_trail_atr`(3.0, E9 초안에서 쓰던 표준폭) ATR로 조인다
(포지션별 원웨이 래칫: 한 번 조여지면 이후 신규 best_close가 나와도 8로 되돌리지 않는다).
- 러너 보호가 구조적: 달리는 추세는 계속 best_close를 갱신하므로 절대 안 조여짐.
  정확히 실패 모드(정체)에만 조건화 → 과적합 표면이 가장 작다.
- 파라미터 3개 모두 a-priori: 24봉(1일), 3 ATR(교과서 샹들리에), peak 0.5R
  (손실 포지션은 LossFade/SL 관할 — 이 축은 수익 은행 전용).
- 강건성 스윕: stall_bars {18,24,36} × stall_trail {2.5,3,4} — 최댓값 고르기가
  아니라 평탄성 확인용. 부호가 셀마다 뒤집히면 기각.

### X-B — 트렌드 슬롯 시간컷 재측정 (whale 한정)
기존 배관 그대로 `time_stop_bars`=96(4일), `time_stop_min_r`=0.3을 whale 프로파일에만
켠다. E9의 기각은 포트폴리오 BRK_L 기준 — whale의 좌석 기회비용은 그 측정에 없던
항이므로 "재측정할 자격"이 있는 것이지 채택 근거가 있는 게 아니다. 반쪽 중 한쪽이라도
터미널 자산이 악화되면 즉시 기각. 스윕 {72,96,120}은 평탄성 확인용.

### X-C — 모멘텀 조건부 giveback 재무장 · **기각(사문·측정, 상단 요약)**
E16이 기각한 giveback을 "4H 모멘텀 소진(mom ≤ 0) 동안만 무장"으로 조건화. X-A와
역할이 겹치고 조건이 하나 더 붙으므로(파라미터+1) X-A가 게이트를 통과하면 측정하지
않는다(중복 축 금지). X-A가 실패할 때만 측정.

### 기각 확정 (측정 불요)
- 트렌드 슬롯 무조건 고정 TP: E9 직접 반증. 재론 없음. ("TP가 없다"는 관찰은
  맞지만, 없는 것이 측정된 선택이다 — 문제는 TP 부재가 아니라 정체 축 부재.)
- hold_conf_exit 상향(0.50→0.6대): AVAX 사례의 hold_conf가 0.77 — 횡보에서도
  정렬 점수가 높게 유지되므로 임계 상향으로는 못 잡고, 올리면 E17에서 측정한
  conviction-collapse의 정상 작동만 훼손한다.

## 3. 측정 프로토콜 (전 후보 공통)
1. 설계 구간 whale 백테스트, 변경 전 이 문서로 사전 등록 완료 상태에서만 실행.
2. 게이트: 터미널 자산·MDD·avg_r 이 **두 반쪽 모두** 현행 대비 악화 없음(G5).
   한쪽 개선·한쪽 악화 = 경로 노이즈로 기각 (E19 선례).
3. base 프로파일 회귀: X-B는 whale 전용 배선이라 base 불변이 자명해야 하고,
   X-A는 base에서도 켜지므로 base 지표 악화 시 whale-전용으로 강등 후 재측정.
4. MC 생존 게이트(95%p MDD ≤ 30%, base) 재확인 + pytest 전수 녹색.
5. 채택 시에도 risk 변경 없음 — 청산 축 추가는 사이징/레버리지/liq-invariant를
   건드리지 않는다.

## 4. 부가 관찰 (이번 설계 범위 밖, 기록만)
- peak_r 0.97 vs arming 1.0: giveback 레그가 꺼진 현재는 무의미한 경계지만,
  X-C를 측정하게 되면 arming 경계 민감도(0.7/1.0)를 같이 봐야 한다.
- 라이브 AVAX 포지션 자체는 이 설계로 소급 처리하지 않는다 — 채택된 로직이
  다음 재시작부터 관리한다.
